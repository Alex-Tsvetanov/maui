{"category":"cpp_major","notes":["MAUI renders the refresh header controls (instructions, toggle buttons, status labels, item count) followed immediately by '50 items loaded. Pull to add more.' in a compact layout","C++ shows the same header area correctly but '50 items loaded. Pull to add more.' appears near the bottom of the visible page — the 50 list items inside the ScrollView are not rendering (blank/empty space between header and footer label)","Dark mode shows the same issue: header present, list content absent, footer displaced to bottom","Diff_pct ~3.3% is moderate but the visual defect is the missing list content inside the RefreshView's scroll area"],"model":"claude-sonnet-4-6"}
