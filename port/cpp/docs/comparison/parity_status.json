{
  "absolute_layout": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED label background on iOS (update_background was deferred to the no-op base mirror): the \"AutoSized\" white-on-blue label now renders (was invisible white-on-white). All 6 children correct: 4 edge-pinned bars (proportional pos + absolute size), centered text, auto-sized blue label at 0.2,0.7. Matches C#; residual is the maui-compare gray container card (harness artifact, port uses plain page bg).",
    "dark_note": "Same fix: AutoSized label now shows its blue background (was white text only on black). All children positioned correctly. Matches C#."
  },
  "activity_indicator": {
    "dark": "minor",
    "dark_note": "FIXED (was blank): default spinners now visible (unset Color -> nil native default, was transparent) and the BackgroundColor=Yellow full-width band renders (update_background). Labels + all spinners + yellow band match MAUI dark. Residual: minor spinner positions.",
    "light": "minor",
    "light_note": "FIXED two issues: (1) the Default/Larger/Smaller spinners now render — an unset Color was forced to a transparent tint that made them invisible; now unset -> nil restores the native default (a dynamic system color, visible). (2) BackgroundColor=Yellow now renders as a full-width band behind the spinner (added iOS update_background -> apply_background; MauiIosActivityIndicator.layoutSubviews keeps a gradient fill sized). All labels + spinners + the yellow band now match MAUI. Residual: minor spinner spacing/positions + a faint not-running spinner."
  },
  "adaptive_collection": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now renders maui-compare AdaptiveCollection: Layout: Linear (single column) + Item 1-8 (was a stale 🔴 with blank dark). Minor: the adaptive layout-switch control + exact item layout vs MAUI to spot-check.",
    "dark_note": "Now renders (was stale blank): Linear layout + items."
  },
  "alerts": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare Alerts (was stale 🔴): OnAppearing: Alert — Welcome to the Alerts Page [Hello!] + Display Alert (Alert Simple/Alert Yes/No) + Display ActionSheet (ActionSheet Simple/Cancel-Delete) + Display Prompt (Question 1/Question 2) buttons — identical to MAUI. The actual native UIAlertController dialogs are triggered by the buttons and are not shown in an at-rest screenshot (both apps). Minor.",
    "dark_note": "Now matches (stale diff cleared): alerts page buttons + OnAppearing status (native dialogs uncapturable)."
  },
  "alignment": {
    "light": "minor",
    "dark": "minor",
    "light_note": "NOW MATCHES maui-compare Alignment(): rewrote the C++ page to use a Border (red 5pt stroke, RoundRectangle CR=5, FIXED 160x40) wrapping a centered blue/white Label, per HorizontalOptions Start/Center/End/Fill, headers FontSize=20 bold — exactly the C# reference. Also fixed a framework bug: border::arrange filled the cell instead of computing its aligned/size-requested frame (compute_frame), so a Border with WidthRequest + non-Fill alignment now lands 160-wide Start(left)/Center+Fill(centered)/End(right) like MAUI. Residual: maui-compare card framing.",
    "dark_note": "Same: 160-wide aligned borders + white-on-black bold headers, matches C#. Border WidthRequest + alignment now honored (border::arrange compute_frame fix)."
  },
  "animation": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Animation (was stale 🔴): .NET bot (animation target) label + Start Animation / Start Custom Animation / Cancel Animation buttons. The C# ref happened to capture the bot MID-ROTATION (a running animation, time-based); the C++ shows it at rest — same page + controls. Minor (time-based capture difference).",
    "dark_note": "Matches (stale diff): animation target + controls; C# caught mid-animation."
  },
  "app_theme_binding": {
    "dark": "minor",
    "dark_note": "FIXED (was diff): all labels now render (dark-text cascade) — heading, green + orange AppThemeBinding paragraphs, Toggle button, Theme:Light status — matching the C# content exactly (BOTH apps show LIGHT values here: the pages theme is driven by its own toggle which defaults to Light, so MAUI_THEME/MAUI_APPEARANCE do not flip the AppThemeBinding). Residual: the page SURFACE differs — C# shows a white Light surface while C++ shows black, because the gallery force-darkens the WINDOW (overrideUserInterfaceStyle=dark) while the AppThemeBinding page resolves to Light (a window-vs-app-theme mismatch specific to AppThemeBinding demos).",
    "light": "minor",
    "light_note": "Matches MAUI: heading, the green AppThemeBinding paragraph, the orange ResourceDictionary paragraph, the blue Toggle-theme button, and the Theme:Light status all render with correct colors/order. Residual: C++ wraps some lines wider (one line vs MAUI two) + slightly larger inter-row spacing."
  },
  "application_control": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare ApplicationControl (was stale 🔴): Quits the application + Terminate Application / Open Window / Close Window buttons + Windows open: 0 | main window: none status. Minor.",
    "dark_note": "Matches (stale diff cleared): app/window control buttons + status."
  },
  "auto_size_shapes": {
    "light": "minor",
    "dark": "minor",
    "light_note": "Matches in behavior: the \"Ellipse must occupy half the available space\" header + a green/blue-stroke ellipse filling the top (yellow) half over an orange bottom half. The ellipse aspect differs (C++ nearly circular vs C# flatter) only because the maui-compare card framing reduces C#'s available height, so its 50/50 rows are shorter — both correctly fill their half. Harness-framing residual.",
    "dark_note": "Same: ellipse fills the top half over orange; aspect difference is the harness card framing reducing available height."
  },
  "basic_grouping": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare after the iOS grouped-CV supplementary fix (f40c64ec67): CV header + green group headers (Avengers/Fantastic Four/Defenders) + orange Total members: N group footers all render, bound to the group key. Was the grouped-CV supplementary gap. Minor: row spacing.",
    "dark_note": "Now matches (grouped-CV supplementary fix landed): group headers/footers render."
  },
  "basic_swipe": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare BasicSwipe after the iOS swipe arrange-host-relative fix (5198718b6f): the 5 gray SwipeView boxes (Swipe Up/Down/Right/Left + any direction) stack COMPACTLY (60px + spacing) instead of ~150px over-spaced. Root cause was swipe_view::arrange placing content at absolute coords → double-offset (same arrange-convention family as border/templated_view). Minor.",
    "dark_note": "Now matches (swipe arrange fix): compact rows."
  },
  "behaviors": {
    "dark": "minor",
    "dark_note": "FIXED (was diff): the headline now renders large two-line (FontSize 32) matching MAUI dark, with the validation Entry below. Residual: minor vertical rhythm.",
    "light": "minor",
    "light_note": "FIXED: the title \"Red when the number isnt valid\" now renders as a large two-line Headline (FontSize 32) above the Entry, matching MAUI (was a small single-line label — the gallery set the text but not the Headline-style font size). The validation behavior (Entry turns red on invalid double) is demonstrated. Residual: minor vertical position (C++ a touch higher)."
  },
  "border": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED: authored the missing border gallery page reproducing maui-compare BorderPage() 1:1 (the key fell back to value-controls) — a single Border centered both ways (red 5pt stroke, RoundRectangle CR=20, LightYellow background, Padding 16, fixed 280x160) wrapping a centered \"Bordered content\" Label. Matches C# exactly; also validates the border::arrange compute_frame fix (VerticalOptions=Center now honored). Residual: maui-compare card framing.",
    "dark_note": "Same: centered red/round/light-yellow border + centered caption, theme-aware. Faithful reproduction of BorderPage()."
  },
  "border_clip_playground": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare BorderClipPlayground after the border compute_frame fix: compact red RoundRectangle Border with asymmetric per-corner radius (Top Left 60, others 0) + Border Width 5 / Corner Radius sliders below. Was a stale pre-fix diff (full-width ~900px stretched Border). Minor: leaf stroke AA.",
    "dark_note": "Now matches (stale pre-fix diff cleared): compact red per-corner-radius Border, sliders laid out."
  },
  "border_layout": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare BorderLayout after the border compute_frame + content-arrange fixes: green-filled rounded Border with red/blue colored child segments + Center label; Stroke thickness: 5/40 slider above. Was a stale pre-fix diff (empty white Border). Minor: slider thumb chrome + leaf segment AA.",
    "dark_note": "Now matches (stale pre-fix diff cleared): colored segments + Center text render inside the green Border."
  },
  "border_playground": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare BorderPlayground after the iOS MauiIosBorder gradient-Background fix (91ded3650b): the Border fills with the cyan LinearGradientBrush (#00B4DB->#0083B0, left-to-right) under the yellow dashed RoundRectangle stroke + Just a Label content; the form section (Border Content/Shape/Background/color entries) renders below. Minor: slightly more vertical gap between the Border and the form than MAUI.",
    "dark_note": "Now matches (gradient-Background fix landed): cyan gradient fills the dashed Border. Minor: vertical spacing to the form."
  },
  "border_resize_content": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare BorderResizeContent after the border compute_frame fix: left column red-filled circle/square/triangle Borders with green stroke + blue + content, right column light-blue shapes; Content Text entry + Content Text FontSize + Image Scale sliders below (the real resize-demo controls). Was a stale pre-fix diff (stretched ellipses/wide rects, red column missing). Minor: leaf shape AA.",
    "dark_note": "Now matches (stale pre-fix diff cleared): correct shapes, both columns distinct."
  },
  "border_stroke": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare BorderStroke after the border compute_frame + content-arrange framework fixes: three orange-filled red-bordered StrokeThickness (1/5/10) boxes, Updating the Content Height section + Content height: 60 slider, three height boxes. Was a stale pre-fix diff. Minor: leaf border-stroke AA.",
    "dark_note": "Now matches (stale pre-fix diff cleared): orange fills + red borders + slider + laid-out labels all render."
  },
  "borderless": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Borderless: Style: borderless (StrokeThickness 0) caption + a switch + a borderless yellow-background Border. Minor: switch chrome.",
    "dark_note": "Matches: borderless yellow Border + switch render."
  },
  "box_view": {
    "dark": "minor",
    "dark_note": "FIXED (was diff): the white section labels (Default/Using Color/Background) now render via the dark-text cascade; blue + purple boxes + the gradient Background box all render matching MAUI dark. Residual: the gradient Background box renders full-height in C++ vs a thin line in the C# demo (sizing/demo difference); minor vertical rhythm.",
    "light": "minor",
    "light_note": "Same 4 BoxView samples, same colors/order/labels (Default blue, Using-Color purple, Background gradient, CornerRadius green-rounded). Residual: minor vertical rhythm (C++ stack a few px higher) and the Background-brush box renders at full height in C++ vs a thin collapsed line in the C# demo (a sizing/demo difference on the gradient box)."
  },
  "button": {
    "dark": "minor",
    "dark_note": "FIXED: tint text visible + colored fills render in dark too. Same remaining corner-rounding/spacing minor as light.",
    "light": "minor",
    "light_note": "FIXED: unset text -> iOS tint; explicit BackgroundColor now fills (per-state backgroundImage). Remaining minor: MAUI default buttons have rounded corners + inter-button spacing; the explicit CornerRadius button should clip to a pill (masksToBounds); C++ bars are square + flush."
  },
  "carousel_page": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare after the iOS carousel item-sizing fix (9c5208d7eb): one item (Item 1) fills the full carousel viewport per page (was: Item 1/2/3 crammed small side-by-side); Prev/Next + Position 0 — current: Item 1 work. Minor: the item label sits top-left in its full-width cell vs MAUI centering it (label-alignment detail in the cell template).",
    "dark_note": "Now matches (carousel item-sizing fix): one full-viewport item per page; minor label alignment within the cell."
  },
  "chat_example": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Improved (95d572c90c): the two messages now render with the oracle COLORS — Hi there! = green rgb(198,234,198) (local), Hello... = blue rgb(198,219,244) (remote) — with the correct padding/margin/text color. Residual (struct-cell DataTemplate framework limitation, documented): the bubbles render as FULL-WIDTH SQUARE bands rather than MAUIs compact, rounded (CornerRadius 12), right/left-aligned bubbles — the port struct-cell template seam stages setters on a single Label root only (no nested border->label) and the CV cell stretches the Label to fill width (HorizontalOptions end/start has no compacting effect). Colors match; bubble shape/alignment is the residual. Minor.",
    "dark_note": "Colors now match (green local / blue remote); bubbles are full-width square bands vs MAUI compact rounded aligned (struct-cell template limitation). Minor."
  },
  "check_box": {
    "dark": "minor",
    "dark_note": "Labels render white (dark-text cascade); checkbox circles/colors/checked-states all match MAUI dark. Residual: ~1.2x row spacing (checkbox 44pt min vs label height) clips the last row; bold labels in the C# demo (demo difference).",
    "light": "minor",
    "light_note": "Labels now render (dark-text cascade); all checkboxes match MAUI — same circle shape, colors (blue/magenta), checked states (filled purple/red with check), and label-left/checkbox-right grid layout. Residuals: C++ row pitch is ~1.2x taller than MAUI (the checkbox measures to its 44pt minimum vs the tighter label height), which pushes the last \"Is green? False\" row toward the bottom; C# demo labels are bold vs the port regular weight (demo difference); top nav-bar pill (harness artifact)."
  },
  "chrome": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Chrome: the Press or right-click me button + Ready status (the context/right-click chrome is a native long-press menu, not shown at rest). Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "clip": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare Clip after the gallery asset-bundling fix: the purple submarine Image + the RectangleGeometry / EllipseGeometry / GeometryGroup clipped variants all render (was all-blank white slots — images never loaded). C# dark ref re-captured (was a stale blank). Minor: clip-geometry crop is approximate + the unclipped Image lacks MAUI gray letterbox bg.",
    "dark_note": "Now matches (images render; C# dark ref re-captured from the stale blank)."
  },
  "clip_corner_radius": {
    "dark": "minor",
    "light": "minor",
    "light_note": "C++ now renders the oasis.jpg photo clipped by the RoundRectangleGeometry inside the LightGray container (JPEG image-load fix e672a14b24 + leaf-control clip fix 7947839a4f); per-corner sliders + title match. The maui-compare C# ref stays gray (harness cannot render oasis.jpg even after asset-add+rebuild). C++ is the correct render. Minor.",
    "dark_note": "C++ renders oasis.jpg clipped (JPEG + clip fixes); maui-compare ref gray (harness limitation). C++ correct."
  },
  "clip_gallery": {
    "dark": "minor",
    "light": "minor",
    "light_note": "C++ now correctly renders the oasis.jpg photo in all clip variants after the iOS JPEG file-image fix (e672a14b24). The maui-compare C# ref shows GRAY placeholder boxes and STILL does after adding oasis.jpg to its Resources/Images + rebuilding — the maui-compare harness cannot resolve that MauiImage (raster Resizetizer/source-name quirk). The C++ is the more-faithful-to-real-MAUI render; this is a reference-harness limitation, not a port gap. Minor.",
    "dark_note": "C++ renders the oasis.jpg photo (JPEG fix); the maui-compare C# ref cannot render it even with the asset added (harness limitation). C++ correct."
  },
  "clip_views": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare ClipViews after the iOS leaf-control Clip fix (7947839a4f): the diagonal Clip geometry now applies to Button, DatePicker (6/20/2026), Entry, Editor, Grid, SearchBar, TimePicker (12:00 AM) — all render the red skewed shape (was: only the Grid/layout clipped, leaf controls flat). Minor: invariant date/time format + leaf chrome.",
    "dark_note": "Now matches (leaf-control clip fix landed): all 7 controls clip to the diagonal shape."
  },
  "clipping": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare Clipping after the clipping_page.hpp 1:1 rewrite (0c9202c7a4): the 8 numbered buttons spread across the 2-column Grid (400x200) with the translucent-red (opacity 0.5) overflow BoxView in the right column, the width-100 purple Hey row over its LightBlue background, and the coffee-image row over LightBlue. Was a stale page-fidelity divergence (flat red bar, no grid/lightblue). Minor.",
    "dark_note": "Now matches (page rewrite): grid + opacity-0.5 red overflow square + LightBlue rows + coffee row."
  },
  "collectionview": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED: authored the missing collectionview gallery page reproducing maui-compare CollectionViewPage() 1:1 (the key fell back to value-controls) — a CollectionView over 24 captioned items (\"cover1.jpg, 0\"..\"Vegetables.jpg, 23\"), a string Header \"This is the header\", SelectionMode.Single, GridItemsLayout(3, Vertical), DataTemplate Label(Margin 6) bound to the caption. Renders the same 3-col grid + header + items as C#. Residual: the maui-compare string header renders slightly bolder, + the card framing.",
    "dark_note": "Same: 3-col grid of all 24 captioned items + header, theme-aware. Faithful reproduction of CollectionViewPage()."
  },
  "composition_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "Composition renders the same drawable scene as C#: a yellow circle + green triangle + multi-colored diagonal striped rectangle (overlapping) on a cream background, then a blue/green/red angle-line figure. Residual: the canvas panel sizing/positioning differs slightly (C++ one tall cream panel vs the maui-compare two-panel card layout) — a graphics_view canvas-layout difference, not a drawing bug.",
    "dark_note": "Same drawable composition renders; residual is the canvas panel layout + harness framing."
  },
  "containers": {
    "light": "minor",
    "dark": "minor",
    "light_note": "NOW MATCHES (stale diff cleared by the border/frame arrange-coordinate fix): the \"Inside a border\" (blue dashed) / \"Inside a frame\" (red border, light-gray fill) / \"Inside a content_view\" all render their content correctly (was invisible before). Scrolled readout + all three containers match C#. Residual: maui-compare card framing.",
    "dark_note": "Same: border/frame/content_view content renders, theme-aware. Matches C#."
  },
  "content_view": {
    "dark": "minor",
    "dark_note": "FIXED (was blank): the white labels render via the dark-text cascade and the blue Swap-content button now renders (added to the gallery page). All three labels + the button present, matching MAUI dark. Residual: minor vertical rhythm.",
    "light": "minor",
    "light_note": "FIXED: the blue Swap-content button now renders (the gallery content_view page had the swap logic but never added a button to trigger it — added a maui::controls::button wired to show_alternate_content()). Now matches MAUI: ContentView header, the wrapper Content label, the swap Content label, and the blue Swap-content button. Residual: minor vertical rhythm / centering."
  },
  "context_flyout": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare ContextFlyout: Increment by 1 (or right-click me) + Is dynamic menu enabled? switch + Right-click to see beautiful menus + the custom-context-menu entry + COOL image button + the embedded WebView content. The context flyout itself is a native UIContextMenu (long-press), not shown at rest. Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "controls_stack": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED: authored the missing controls_stack gallery page reproducing maui-compare ControlsStack() 1:1 (the key fell back to value-controls before) — bold FontSize-22 \"Controls\" header, Button \"A Button\", Entry/Editor/SearchBar placeholders, a [CheckBox checked, Switch on, ActivityIndicator running] row, Slider(40), Stepper(3), ProgressBar(0.6). Also fixed the iOS Switch on-tint: an unset OnColor wrongly painted the on-track black (to_ui_color of the collapsed-null color); now restores the system default green. Matches C#; residual is the maui-compare card framing.",
    "dark_note": "Same: full Controls showcase renders with theme-aware text, green switch. Faithful reproduction of ControlsStack()."
  },
  "custom_layout": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare CustomLayout: the custom ILayoutManager arrangement demo renders its items. Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "custom_size_swipe": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare CustomSizeSwipe after the swipe arrange fix (5198718b6f): green This is the SwipeView Content box + Test Click from Content link + RightItems revealed (open=1, threshold=0) readout, laid out compactly. Minor.",
    "dark_note": "Matches (swipe arrange fix)."
  },
  "custom_swipe_item_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare CustomSwipeItemView after the swipe arrange fix (5198718b6f): the custom swipe item view (Welcome to .NET MAUI card) renders in the revealed state. Minor.",
    "dark_note": "Matches (swipe arrange fix): custom swipe item view renders."
  },
  "cv_visual_states": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare VisualStates: the Single Selection (Item 1-3) and Multi Selection (Item 1-4) CollectionView items RENDER (was a page bug: the cell label staged TextColor=white -> white text on the white cell = invisible items; now uses the default adaptive label color). The Selected=>Yellow per-cell CommonStates VSM recolor is the documented struct-cell-template limit; the default gray selection highlight now works via the cell selectedBackgroundView fix. Minor.",
    "dark_note": "Now matches: items render (white-on-white page bug fixed)."
  },
  "data_template_selector": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare DataTemplateSelector: the CollectionView with a DataTemplateSelector picking per-item templates renders. Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "date_picker": {
    "dark": "minor",
    "dark_note": "Rendering now complete: solid blue + both gradient fills render flat matching MAUI dark (gradient-resize FIXED). Residuals: invariant US date format (deliberate deviation) vs MAUI device-locale dd.MM.yyyy; vertical rhythm; sample-data gradient color.",
    "light": "minor",
    "light_note": "Rendering now complete: solid BackgroundColor=Blue (flat fill) + both gradient Background rows (yellow->green + a second gradient) all render, matching MAUI structure (FIXED: MauiIosDatePicker now re-syncs its background sublayer on layoutSubviews via resize_background_layers, so gradient fills track bounds). Residuals: date text is invariant US (deliberate port deviation w/ iOS tests) vs MAUI device-locale dd.MM.yyyy; compact vertical rhythm; 3rd gradient is a different demo color than the C# sample."
  },
  "device": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare Device (was stale 🔴): Platform: iOS / Idiom: Phone / Version: 26.5 — same content as MAUI. Minor: MAUI centers the text vertically+horizontally; C++ top-left-aligns it.",
    "dark_note": "Now matches (content identical): device info; minor center-vs-topleft alignment."
  },
  "dispatcher": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Dispatcher: the Dispatcher (main-thread marshalling / timer) demo renders its controls + readout. Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "drag_drop": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare DragDrop (was stale 🔴): All colors (drag a swatch...) + orange/yellow/green/blue/purple/pink swatches + Rainbow: red bar + the Drag start/Drag position/Drop position relative-to readouts + Move: swatch dropped into Rainbow. Minor.",
    "dark_note": "Now matches (stale diff cleared): swatches + rainbow + drag/drop readouts."
  },
  "editor": {
    "dark": "minor",
    "dark_note": "FIXED: unset TextColor now resolves to UIColor.labelColor, so all rows render white on dark — LENGTH:0, FontSize (Large), \"I am read only\", \"123\", \"This should be on the bottom\", AUTOSIZE LENGTH:0 all legible, matching MAUI dark; placeholders gray, explicit purple preserved. Residual: vertical rhythm slightly looser than MAUI.",
    "light": "minor",
    "light_note": "Same controls/order/text/colors; rows sit a few px higher with text flush to the left edge vs MAUI's left inset (vertical rhythm)."
  },
  "effects": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Renders maui-compare Effects (was stale 🔴): Entry With Focus Routing Effect + Entry With Focus Platform Effect entries + Detach/Re-attach routing effect buttons + routing effect attached: yes status. The C# ref is a 3.1MB capture anomaly (identical light+dark) so exact pixel-compare is unavailable, but the C++ effects demo renders correctly. Minor.",
    "dark_note": "Renders the effects demo (C# ref anomalous). Minor."
  },
  "ellipse_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "NOW MATCHES: added set_horizontal_layout_alignment(start) on each ellipse (maui-compare/the C# Style sets HorizontalOptions=Start) so they LEFT-align instead of centering (Fill+explicit-width default). All 5 ellipses (basic red, circle, stroke, filled-stroke, dashed) render with the same fills/strokes/dashes/captions, left-aligned like C#. Residual: harness card framing.",
    "dark_note": "Same: left-aligned ellipses, theme-aware captions, matches C#."
  },
  "empty_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches the maui-compare EmptyView CollectionView at load: a Filter SearchBar atop the populated item list (cover1.jpg/oasis.jpg/... with index). The empty view shows only once the filter clears the list. Minor.",
    "dark_note": "Matches: Filter bar + populated list render."
  },
  "empty_view_load_simulate": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare EmptyViewLoadSimulate: the load/empty state (Nothing to display) renders. Minor.",
    "dark_note": "Matches (empty-view demo renders)."
  },
  "empty_view_null": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare EmptyViewNull: the CollectionView empty state shows Nothing to display. Minor.",
    "dark_note": "Matches (empty-view demo renders)."
  },
  "empty_view_rtl": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare EmptyViewRtl: the RTL CollectionView empty-view variant renders. Minor.",
    "dark_note": "Matches (empty-view demo renders)."
  },
  "empty_view_selector": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare EmptyViewSelector: the filter instructions + Filter SearchBar + items (Baboon - Africa & Asia ...). Minor.",
    "dark_note": "Matches (empty-view demo renders)."
  },
  "empty_view_swap": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare EmptyViewSwap: populated grid + Filter (the empty-view swaps when filtered empty). Minor.",
    "dark_note": "Matches (empty-view demo renders)."
  },
  "empty_view_template": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare EmptyViewTemplate: the populated 3-column grid (cover1/oasis/photo.jpg...) + Filter SearchBar; the empty-view template shows only when filtered to empty. Minor.",
    "dark_note": "Matches (empty-view demo renders)."
  },
  "empty_view_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare EmptyViewView: populated 3-column grid + Filter SearchBar (empty View shows when filtered empty). Minor.",
    "dark_note": "Matches (empty-view demo renders)."
  },
  "entry": {
    "dark": "minor",
    "dark_note": "FIXED dark text: unset entry text now uses the dynamic system label color (white in dark, was black-on-black). Matches C# (white text, gray placeholders, purple explicit). Minor spacing only.",
    "light": "minor",
    "light_note": "Near-match: same fields, placeholders, explicit purple text, password dots, end-alignment. Minor: slightly tighter inter-field spacing + entry border rendering vs C#."
  },
  "filter_collection": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare FilterCollection: the Filter SearchBar + the live-filtered item list render. Minor.",
    "dark_note": "Matches (renders; selection-cell highlight is the deferred CV gap where applicable)."
  },
  "filter_selection": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare FilterSelection: the instructions + Filter SearchBar + Selected: (none) + Reset + the item list. Minor.",
    "dark_note": "Matches (renders; selection-cell highlight is the deferred CV gap where applicable)."
  },
  "flex_layout": {
    "light": "minor",
    "dark": "minor",
    "light_note": "Near-exact match to C#. The full holy-grail flex renders: outer column HEADER(aqua)/BODY/FOOTER(pink), body row with CONTENT(gray, Grow=1) + nav(blue, Basis=50, Order=-1 forces front) + aside(green, Basis=50). The aqua/gray/pink LABEL backgrounds render thanks to the iOS label MapBackground fix (1d442d2451). Theme-aware text (black light). Residual: maui-compare wraps the page in a card (harness artifact); C++ fills edge-to-edge.",
    "dark_note": "Same near-exact match in dark: white-on-color headings, all flex children + Grow/Basis/Order correct. Residual is the harness card framing only."
  },
  "focus": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Focus: the Focus target entry + Focus Entry / Unfocus Entry buttons + IsFocused: false status. Minor.",
    "dark_note": "Matches: focus entry + focus/unfocus + status."
  },
  "fonts": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED to faithfully reproduce maui-compare Fonts(): a ScrollView of 9 labels — Title(34)/Subtitle(28)/Header(23)/Body(22)/Caption(18), then Bold/Italic/Bold+Italic (FontAttributes, default size), then \"Character spacing 4.0\" (was a richer divergent FontsPage demo). Also fixed an iOS font bug: to_ui_font ignored weight+slant, so FontAttributes=Bold/Italic rendered REGULAR on EVERY base control; now folds Bold->TraitBold, Italic->TraitItalic. Matches C# 1:1; residual is the harness card framing.",
    "dark_note": "Same: 9 labels at the right sizes + bold/italic now render, theme-aware. Faithful reproduction of Fonts()."
  },
  "footer_only_string": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare FooterOnlyString: the cover1/oasis/photo/Vegetables/Fruits/... item list renders with a string footer below the fold. Minor.",
    "dark_note": "Matches: item list + string footer."
  },
  "formatted_text": {
    "dark": "minor",
    "dark_note": "Near-exact match on dark: bold-red / italic-underlined / kerned spans + plain label all render with correct styling matching MAUI (the formatted_string feature + dark-text cascade make every span visible). Residual: minor vertical position offset.",
    "light": "minor",
    "light_note": "Near-exact match: the FormattedString renders all spans with correct per-span styling — \"Bold red\" (bold + red), \"italic underlined\" (italic + underline), \"kerned\" (character-spacing), then a plain text label. Same spans, colors, decorations and order as MAUI. Residual: minor vertical position (C++ stack sits a few px higher / less top inset)."
  },
  "gestures": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Gestures: the tap/pan/pinch/swipe/pointer gesture-recognizer readout demo renders (synthetic readouts, the gestures_page pattern). Minor.",
    "dark_note": "Matches: gesture readout demo renders."
  },
  "gradient": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED: authored the missing gradient gallery page reproducing maui-compare Gradient() 1:1 (the key fell back to value-controls) — two bold-captioned 60px BoxViews: a LinearGradientBrush (Yellow->Green, horizontal) and a RadialGradientBrush (Red->DarkBlue, center 0.5/0.5, radius 0.7). Both gradients + bold captions render identically to C# (bold via the to_ui_font fix). Residual: maui-compare card framing.",
    "dark_note": "Same: linear + radial gradient boxes + bold captions render, theme-aware. Faithful reproduction of Gradient()."
  },
  "grid": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED to faithfully reproduce maui-compare GridPage(): a Grid (Padding 16, spacing 6) with rows Auto/80/80 and two star columns, a bold \"Grid (2 cols x rows)\" header spanning both columns, and Red/Green/Blue/Orange boxes filling the fixed-80 cells (Red+Green row 1, Blue+Orange row 2). Matches C# 1:1; only residual is the maui-compare card framing (harness).",
    "dark_note": "Same: header + 2x2 colored grid render identically, theme-aware header. Faithful reproduction of GridPage()."
  },
  "grid_grouping": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare GridGrouping after the iOS grouped-CV supplementary fix (f40c64ec67): CV header + green group headers + orange Total members footers wrap a 2-column member grid per group. Minor.",
    "dark_note": "Now matches (grouped-CV supplementary fix landed): group headers/footers render."
  },
  "grouping_no_templates": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare GroupingNoTemplates: a grouped CollectionView with NO group header/footer templates renders the flat item rows (Thor/Captain America/.../Namor) with no group headers — same as MAUI. Minor: looser row spacing.",
    "dark_note": "Matches: flat grouped items, no headers (no templates)."
  },
  "grouping_plus_selection": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare GroupingPlusSelection after the grouped-CV supplementary fix (f40c64ec67): green group headers + orange Total members footers + grouped selectable rows. Minor.",
    "dark_note": "Now matches (grouped-CV supplementary fix landed): group headers/footers render."
  },
  "header_footer": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare HeaderFooter: Just a string as a header + cover1/oasis/photo.jpg items + This footer is also a string. Minor: MAUI renders the bare-string CV header/footer in a larger bold default font; C++ uses regular weight.",
    "dark_note": "Matches: string header + items + string footer. Minor bold default on the string header/footer."
  },
  "header_footer_grid": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare HeaderFooterGrid: CV grid layout with header + footer + items render (the grouped-CV supplementary + grid-layout machinery). Minor.",
    "dark_note": "Matches: grid CV with header/footer."
  },
  "header_footer_grid_horizontal": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare HeaderFooterGridHorizontal: Toggle Header / Toggle Footer buttons + a horizontal grid CollectionView of cover1/oasis/photo/Vegetables/... items. Minor.",
    "dark_note": "Matches: horizontal grid CV + toggle buttons."
  },
  "header_footer_template": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare HeaderFooterTemplate: a templated date/time header (peach) + cover1/oasis/photo.jpg items + templated date/time footer. Minor.",
    "dark_note": "Matches: templated header/footer render."
  },
  "header_footer_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare HeaderFooterView: CV with a View header/footer + items render. Minor.",
    "dark_note": "Matches: view header/footer render."
  },
  "hit_testing": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare HitTesting: Selected: Image + Rectangle Selection + Lorem ipsum labels + Scale=1/Scale=2/Rotation=20 + the rotated green ellipse + green rounded rectangle (shapes with transforms + hit-test selection). Minor.",
    "dark_note": "Matches: hit-testing shapes + transforms render."
  },
  "horizontal_stack": {
    "light": "minor",
    "dark": "minor",
    "light_note": "NOW MATCHES: gave every BoxView an explicit 40x40 size in BOTH the C++ gallery AND the maui-compare app (a modern MAUI BoxView with no size collapses to 0). HorizontalStackLayout renders identically — same heading, same 6 colored boxes (centered where MAUI treats Fill+explicit-size as centered, which the C++ align_horizontal mirrors). Only residual is the maui-compare card framing (harness).",
    "dark_note": "Same: both apps render the colored boxes identically with theme-aware heading. Harness card framing is the only residual."
  },
  "hybrid_web_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare HybridWebView: the HybridWebView (JS<->C# message bridge) demo renders its content + controls. Minor.",
    "dark_note": "Matches: hybrid webview demo renders."
  },
  "image": {
    "light": "minor",
    "dark": "minor",
    "light_note": "IMPROVED (asset-bundling fix): both UriSource (Microsoft building, from URL) and FileSource (purple dotnet_bot) now render (FileSource was blank before). Residual: the FileSource image sizes/positions differently from MAUI’s full-width AspectFit band (a documented iOS image measure deviation), and the Font Image Source row is below the fold. cog/thumb stay SVG-only (no rasterizer) — documented.",
    "dark_note": "Same: UriSource + FileSource render; FileSource AspectFit sizing is the documented measure deviation."
  },
  "image_button": {
    "dark": "minor",
    "dark_note": "FIXED: ImageButton BackgroundColor (green/purple) + borders + corner radii render matching MAUI dark, and the 'Custom Size' resize button now renders the purple dotnet_bot.png (assets bundled). Residual: the cog.png icon is still absent — original is SVG-only and the iOS loader cannot decode raw SVG (no faithful raster; documented in src/samples/resources/README.md), also not visible in the C# shot.",
    "light": "minor",
    "light_note": "FIXED: the ImageButton BackgroundColor fills render (green/purple per the ButtonHandler recipe) AND the 'Custom Size' resize button now shows the purple dotnet_bot.png (gallery assets bundled). Residual: the cog.png icon inside the cog buttons is still absent — the MAUI original is SVG-only (Resizetizer rasterizes cog.svg at build), and the iOS loader cannot decode raw SVG with no rasterizer available, so no faithful raster could be produced (documented, not fabricated — see src/samples/resources/README.md). C# likewise shows no prominent cog (the fill dominates)."
  },
  "indicator": {
    "dark": "minor",
    "dark_note": "Dots + colored indicators + the Colors yellow band all render matching MAUI dark (update_background added). Residual: Indicator Size dots scale larger than the C# shot (iOS-26 UIPageControl dot-resize fidelity nuance).",
    "light": "minor",
    "light_note": "Dot indicators render matching MAUI — Basic/Shape/MaximumVisible(7-of-10)/Colors all show the right dot patterns; Colors now renders on a full-width yellow band (FIXED: added iOS update_background -> apply_background, reusing MauiPageControl with a layoutSubviews resize hook for gradients). Residual: the Indicator Size demo dots scale much larger in C++ than in the C# shot (real MAUI does not visibly resize UIPageControl dots on iOS 26 while the port scale=size/6 applies) — a scaling-fidelity nuance; carousel-with-indicator at the bottom."
  },
  "input_controls": {
    "dark": "minor",
    "dark_note": "FIXED: radios now visible on dark (unset TextColor -> labelColor tints the circle + title white; was clear/black = invisible) and the selected-radio white box removed (UIButtonTypeCustom). Filled/empty circles + white labels match MAUI dark. Residual: radio group centering vs left-align. Cascades to all radio pages.",
    "light": "minor",
    "light_note": "FIXED: the radio button group (UPPER/lower) now renders — an unset TextColor was forced to a clear/black tint that hid BOTH the title AND the SF-symbol circle indicator; now unset -> UIColor.labelColor (visible light+dark). Also switched the radio UIButton from System to Custom so the iOS-26 selected-state tint fill (a white box behind the selected radio) no longer appears. LENGTH label + Entry + SearchBar + the radios all match MAUI. Residual: the C++ radio group is center-aligned vs MAUI left-aligned (minor). Cascades to all radio_button pages."
  },
  "input_transparent": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare InputTransparent: the InputTransparent demo (overlapping views where the transparent one passes touches through) renders. Minor.",
    "dark_note": "Matches: input-transparent demo renders."
  },
  "invalidate_brush": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches the maui-compare InvalidateBrush demo: green Change color button + Brush color: Green label + brush box. Minor: button chrome (harness default styles).",
    "dark_note": "Matches; Change color button + brush label render."
  },
  "invalidate_shadow_host": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare InvalidateShadowHost: Host + Update Host Size button + Shadow Offset X/Offset Y/Radius/Opacity sliders + the green-bordered host box. Minor: the C++ host box is taller than MAUIs default host height.",
    "dark_note": "Matches: Update Host Size + sliders + green host box. Minor host height."
  },
  "ios_blur_effect": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSBlurEffect: the photo + No Blur / Extra Light Blur / Light Blur / Dark Blur buttons + BlurEffect: ExtraLight status (the JPEG image renders after the file-image fix). Minor.",
    "dark_note": "Matches (iOS platform-specific demo renders)."
  },
  "ios_date_picker": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSDatePicker: the iOS date-picker platform-specifics demo renders. Minor.",
    "dark_note": "Matches: iOS date-picker demo renders."
  },
  "ios_entry": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSEntry: the iOS entry platform-specifics demo renders. Minor.",
    "dark_note": "Matches: iOS entry demo renders."
  },
  "ios_first_responder": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSFirstResponder: the two Entry+OK keyboard/first-responder demo + Focus First/Second buttons + First/Second IsFocused + CanBecomeFirstResponder readouts. Minor.",
    "dark_note": "Matches (iOS platform-specific demo renders)."
  },
  "ios_pan_gesture": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSPanGesture: the iOS pan-gesture platform-specific demo renders. Minor.",
    "dark_note": "Matches (iOS platform-specific demo renders)."
  },
  "ios_picker": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSPicker: the Select a monkey picker + Toggle Picker UpdateMode button. Minor.",
    "dark_note": "Matches: iOS picker + UpdateMode toggle."
  },
  "ios_safe_area": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSSafeArea: the Lorem ipsum text block (extending into/over the safe area) + Disable Use Safe Area toggle. Minor.",
    "dark_note": "Matches: safe-area text + toggle."
  },
  "ios_scroll_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "FIXED: the page is a FlyoutPage whose DETAIL pane content (a Slider + 'Toggle ScrollView DelayContentTouches' + 'Return to Platform-Specifics List') rendered blank because flyout_page did not override arrange — the inherited content_page::arrange arranged the unused content_ slot (null on a flyout page) and never reached the flyout_/detail_ panes. Added flyout_page::arrange to frame the page, size the split-VC host, then arrange BOTH panes host-relative ({0,0,w,h}). The slider + both buttons now render and match C#. Residual minor: the maui-compare reference wraps the detail in a white card with extra inter-control spacing; the port arranges the controls flush below the nav chevron (no card chrome).",
    "dark_note": "FIXED via flyout_page::arrange (detail-pane arrange). Slider + Toggle + Return buttons render, matching C#. Residual minor = harness white-card wrapper + spacing only."
  },
  "ios_search_bar": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSSearchBar: Enter search term SearchBar + Toggle SearchBar Style + Toggle Background buttons. Minor.",
    "dark_note": "Matches (iOS platform-specific demo renders)."
  },
  "ios_slider_update_on_tap": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSSliderUpdateOnTap: the slider with UpdateOnTap platform-specific + controls render. Minor.",
    "dark_note": "Matches (iOS platform-specific demo renders)."
  },
  "ios_swipe_transition": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSSwipeTransition: the iOS swipe-transition-mode demo renders. Minor.",
    "dark_note": "Matches (iOS platform-specific demo renders)."
  },
  "ios_time_picker": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare iOSTimePicker: the iOS time-picker platform-specifics demo renders. Minor.",
    "dark_note": "Matches (iOS platform-specific demo renders)."
  },
  "items": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches the maui-compare Items CollectionView: Today header + the three task rows (Water the plants / Review the port / Ship wave 2) + Pick a task footer. CV header/footer render correctly (non-grouped). Minor: row spacing.",
    "dark_note": "Matches: CV header + items + footer render."
  },
  "items_updating_scroll_mode": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare ItemsUpdatingScrollMode: the scroll-mode buttons + Add/Insert/Scroll controls + item list render. Minor.",
    "dark_note": "Matches: scroll-mode demo renders."
  },
  "label": {
    "dark": "minor",
    "dark_note": "FIXED dark text: unset labels now use the dynamic system label color (white in dark) instead of black-on-black. Residual: same missing gray BackgroundColor boxes as light.",
    "light": "minor",
    "light_note": "Text + layout match; C# fills several alignment-demo labels with a gray BackgroundColor box that the C++ port doesn't render — label background-fill diff only."
  },
  "layout_is_enabled": {
    "light": "minor",
    "dark": "minor",
    "light_note": "NOW MATCHES (the IsEnabledCore parent->child cascade is implemented — view<>::is_enabled() coerces against the ancestor chain + refresh_is_enabled_subtree re-pushes on change). The right column (right_layout starts disabled) and the \"disabled because layout is disabled\" pink sub-stacks render GRAYED, exactly like C# (was the stale diff). Directly-set states correct too. Residual: maui-compare card framing.",
    "dark_note": "Same: parent-disabled cascade grays the right column + disabled-parent sub-stacks, theme-aware. Matches C#."
  },
  "line_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: 3 lines: basic purple, orange dash, thick StrokeThickness render identically to maui-compare. The maui-compare line_gallery (FromReflection page) does NOT set HorizontalOptions=Start, so the C++ centering (Fill+explicit-width) already matches — no code change. Residual: harness card framing. (C# rectangle ref re-captured from a stale blank.)",
    "dark_note": "Same: shapes render identically, theme-aware captions. Matches C#."
  },
  "line_join_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: three cyan chevrons with Miter(default)/Bevel/Round line joins render identically to maui-compare. Shapes draw correctly; residual is harness card framing (C++ fills full height so it shows more below the fold than the maui-compare card). No code change.",
    "dark_note": "Same: shapes render identically, theme-aware captions. Matches C#."
  },
  "measure_first_strategy": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare MeasureFirstStrategy: the CollectionView MeasureFirst item-sizing strategy demo renders. Minor.",
    "dark_note": "Matches: measure-first CV demo renders."
  },
  "menu_bar": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare MenuBar: iOS has NO menu bar (MenuBarItems are a desktop/Catalyst concept; UIKit phone renders none) — both MAUI and the port show just the page content, which matches. Faithful not-supported. Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "modal": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Modal: Modal Page 1 + Push Page / Push Modal Page / Push Modal Navigation Page / Push Modal Flyout Page / Pop Modal Page + Modal depth: 0 | page stack depth: 1 status. The actual modal presentation is triggered by the buttons (native). Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "multiple_bound_selection": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare MultipleBoundSelection after the iOS CV selection-highlight fix (selectedBackgroundView + re-apply-after-reload): the preselected Item 1/Item 2 cells show the gray selected-cell highlight + the Selected: Item 1, Item 2 label binds. Minor.",
    "dark_note": "Now matches: selected cells highlighted + Selected label."
  },
  "navigation_gallery": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare NavigationGallery: Stack depth: 1 | top: PAGE NUMBER 1 status + Push/Pop Page, Insert/Remove Page Before Current, Pop To Root, Toggle Secondary Toolbar Item controls. Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "nested_collection": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare NestedCollection: Its CollectionViews all the way down + the outer Source 0-N list render. Minor (inner nested-CV exact layout vs MAUI to spot-check).",
    "dark_note": "Matches (renders; selection-cell highlight is the deferred CV gap where applicable)."
  },
  "pan_gesture_events": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare PanGestureEvents (was stale 🔴): StatusType: Completed, TotalX: 0, TotalY: 0 readout + the green (top) and red (bottom) pan boxes. Minor.",
    "dark_note": "Now matches (stale diff cleared): pan status + green/red boxes."
  },
  "path_aspect_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: the heart/EKG Path at four Aspect modes (None/Fill/Uniform/UniformToFill) scaling correctly render identically to maui-compare. No code change. Residual: harness card framing (C++ fills full height) + a minor slider-value number format (\"1.0\" vs \"1\").",
    "dark_note": "Same: renders identically, theme-aware. Matches C#."
  },
  "path_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: PathGeometry shapes: LineSegment, triangle, Cubic Bezier wave, composite concentric circles, overlapping-rectangles, EllipseGeometry render identically to maui-compare. Shapes draw correctly; residual is harness card framing (C++ fills full height so it shows more below the fold than the maui-compare card). No code change.",
    "dark_note": "Same: shapes render identically, theme-aware captions. Matches C#."
  },
  "path_transform_string": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: Without/With RenderTransform variants of a path-string shape render identically to maui-compare. No code change. Residual: harness card framing (C++ fills full height) + a minor slider-value number format (\"1.0\" vs \"1\").",
    "dark_note": "Same: renders identically, theme-aware. Matches C#."
  },
  "picker": {
    "dark": "minor",
    "dark_note": "Matches: all query text white (handler already left system default), blue title, gray placeholders; BackgroundColor=Yellow/Green now render as full flat fills matching MAUI dark (FIXED via UITextField backgroundColor property). Residual: vertical rhythm (C# taller rows).",
    "light": "minor",
    "light_note": "Matches: text colors (black query, blue TitleColor, gray placeholders) + BackgroundColor fills now render — Yellow/Green pickers show full flat fills (FIXED: iOS picker now pushes solid BackgroundColor to the UITextField backgroundColor property, suppressing the RoundedRect bezel like MAUI). Residual: C++ more compact vertical rhythm (fits more rows than MAUI) + top nav-bar blob (harness artifact)."
  },
  "pickers": {
    "dark": "minor",
    "dark_note": "Near-exact match in dark: all text white/legible, placeholder gray, time \"09:00\" and readout label match MAUI exactly. Sole residual: DatePicker field invariant US 6/19/2026 (deliberate deviation) vs MAUI 19.06.2026; minor vertical rhythm.",
    "light": "minor",
    "light_note": "Near-exact match: picker placeholder \"Pick a room\", time field \"09:00\" (explicit HH:mm format), and the bound readout label \"No room on 6/19/2026 at 09:00\" all match; rounded field borders match. Sole residual: the DatePicker field shows invariant US 6/19/2026 (deliberate port deviation w/ iOS tests) vs MAUI device-locale 19.06.2026; minor top vertical-rhythm offset."
  },
  "pointer_gesture": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare PointerGesture: the pointer gesture-recognizer demo renders (entered/moved/exited/pressed/released readouts). Minor.",
    "dark_note": "Matches: pointer gesture demo renders."
  },
  "polygon_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: 4 polygons: basic + dash green triangle, EvenOdd + NonZero pentagram stars render identically to maui-compare. The maui-compare polygon_gallery (FromReflection page) does NOT set HorizontalOptions=Start, so the C++ centering (Fill+explicit-width) already matches — no code change. Residual: harness card framing. (C# rectangle ref re-captured from a stale blank.)",
    "dark_note": "Same: shapes render identically, theme-aware captions. Matches C#."
  },
  "polyline_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: a basic + a dashed red polyline render identically to maui-compare. Shapes draw correctly; residual is harness card framing (C++ fills full height so it shows more below the fold than the maui-compare card). No code change.",
    "dark_note": "Same: shapes render identically, theme-aware captions. Matches C#."
  },
  "preselected_item": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare PreselectedItem — renders the CollectionView + items + the Selected-bound label correctly; the only residual is the missing gray SELECTED-CELL highlight (the DEFERRED iOS CV selectedBackgroundView gap, same family as preselected_items 🔴). Minor.",
    "dark_note": "Matches (renders; selection-cell highlight is the deferred CV gap where applicable)."
  },
  "preselected_items": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare PreselectedItems after the iOS CV selection-highlight fix: the preselected cells (photo.jpg 2 / Fruits.jpg 4 / FlowerBuds.jpg 5) show the gray selected-cell highlight, matching MAUI. Fix = a selectedBackgroundView (systemGray4) on MauiCollectionViewCell + re-applying the selection mirror after reloadData (a preselected selection set before the ItemsSource maps was lost; reloadData now re-applies it). Minor.",
    "dark_note": "Now matches (selection-highlight fix): preselected cells show the gray highlight."
  },
  "progress_bar": {
    "dark": "minor",
    "dark_note": "FIXED: Default/Disabled blue fills render (unset ProgressColor -> nil native default, was transparent); orange ProgressColor + gray tracks all match MAUI dark, full-width below each label. The earlier apparent layout offset was the invisible fill, now resolved. Residual: bold labels in the C# demo (demo difference).",
    "light": "minor",
    "light_note": "FIXED: the Default/Disabled bars now show their blue default fill (unset ProgressColor was forced to a transparent tint, hiding the fill and making the bar look mis-positioned/right-aligned; now unset -> nil restores the native blue, C# ResetProgressTintColor). All bars now render full-width below their label matching MAUI — blue default + orange ProgressColor + gray tracks + empty ProgressTo. Residual: C# demo styles labels bold vs the port regular weight (demo difference)."
  },
  "radio_button_border": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RadioButtonBorder: the radio buttons with borders render. Minor.",
    "dark_note": "Matches (radio demo renders; C# ref re-captured where stale)."
  },
  "radio_button_content": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RadioButtonContent: the string-Content options (Option A/C), the View-Content fallback (Cant use View..., so just plain old text), the coffee.png string fallback, and the weird custom ControlTemplate group rendering the coffee-cup icons (black/red lines + cup). Content variations + custom templates render. Minor.",
    "dark_note": "Matches: Content string/View/template variations + coffee-cup custom templates render."
  },
  "radio_button_group": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RadioButtonGroup: the GroupName=foo explanation + Selected: (none) + Option A/B/C radios in the StackLayout + the This RadioButton is inside a Grid / Option D row. Radio circles render. Minor: radio chrome.",
    "dark_note": "Matches: grouped radios + Option D render."
  },
  "radio_button_group_binding": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RadioButtonGroupBinding (C# ref re-captured from stale blank): the GroupName=group1 + Selection bound to a ViewModel explanation + Option A/B/C/D radios in a Grid + Set selection to B / Clear selection to null buttons. Minor.",
    "dark_note": "Matches (radio demo renders; C# ref re-captured where stale)."
  },
  "radio_button_group_gallery": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RadioButtonGroupGallery (C# ref re-captured): the grouped radio gallery renders. Minor.",
    "dark_note": "Matches (radio demo renders; C# ref re-captured where stale)."
  },
  "radio_content_properties": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RadioContentProperties (C# ref re-captured): the RadioButton Content property variations render. Minor.",
    "dark_note": "Matches (radio demo renders; C# ref re-captured where stale)."
  },
  "radio_template_from_style": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RadioTemplateFromStyle (C# ref re-captured): the ControlTemplate-from-Style radios render. Minor.",
    "dark_note": "Matches (radio demo renders; C# ref re-captured where stale)."
  },
  "rectangle_gallery": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: 5 rectangles: basic red, square, stroke, stroke-dash, curved-corners render identically to maui-compare. The maui-compare rectangle_gallery (FromReflection page) does NOT set HorizontalOptions=Start, so the C++ centering (Fill+explicit-width) already matches — no code change. Residual: harness card framing. (C# rectangle ref re-captured from a stale blank.)",
    "dark_note": "Same: shapes render identically, theme-aware captions. Matches C#."
  },
  "refresh_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare RefreshView: Pull to refresh the ScrollView + Number of items: 50 + Toggle Refresh Color/Background Color/Refresh/Is Enabled buttons + Is Refreshing: False + Is Enabled: True + 50 items loaded. Minor.",
    "dark_note": "Matches: refresh-view controls + status."
  },
  "relative_layout": {
    "light": "minor",
    "dark": "minor",
    "light_note": "Documented deviation: RelativeLayout is Compatibility/legacy (out of scope), so the port reproduces the scene with absolute_layout + proportional flags. Renders correctly in both apps: 4 corner boxes (red TL/green TR/blue BL/yellow BR) + silver center box + small black box at its origin. C# ref re-captured (was a stale blank). Residuals: the black box approximates a sibling-relative constraint (absolute_layout cannot reference a sibling — noted in the page), the silver-box proportion differs slightly from the maui-compare demo, and the harness card framing.",
    "dark_note": "Same in dark: both render the 4 corners + silver + black scene. C++ silver is exactly 0.33x0.33 of the parent; the maui-compare demo sizes it a bit smaller. Residual is the documented sibling-reference deviation + demo proportions + harness card."
  },
  "scattered_radio_button": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare ScatteredRadioButton (C# ref re-captured): RadioButtons dont have to be in the same container + Selected:(none) + A/B/C in a nested StackLayout + D outside with the same GroupName. Minor.",
    "dark_note": "Matches (radio demo renders; C# ref re-captured where stale)."
  },
  "scroll_mode_test": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare ScrollModeTest (ItemsUpdatingScrollMode): the mode buttons (KeepItemsInView/KeepScrollOffset) + Scroll To Middle / Add Item Above/Below/To End + Mode: KeepItemsInView - Items: 20 readout + the item list. Minor.",
    "dark_note": "Matches: scroll-mode controls + item list."
  },
  "scroll_to_group": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare ScrollToGroup: Group:/Item: index entries + Go, Group Name:/Item Name: entries + Go, No scroll requested yet readout, then the grouped list with the green Avengers group header + members. Minor.",
    "dark_note": "Matches: scroll-to-group controls + grouped list with header."
  },
  "scroll_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare ScrollView: Scrolled to: 0 / 0 (done) status + the Row 0..40 scrollable list. Minor.",
    "dark_note": "Matches: scroll status + row list."
  },
  "search_bar": {
    "dark": "minor",
    "dark_note": "FIXED: unset TextColor now resolves to UIColor.labelColor, so query text renders white on dark, matching MAUI dark (green/pink explicit preserved, gray placeholders). Residual: search-bar row height differs (C# taller). (C# dark reference was a blank/black broken capture — re-captured.)",
    "light": "minor",
    "light_note": "Matches: same controls/order/text/colors (green/pink explicit, gray placeholders, black query text). Residual: C++ has a black rounded nav-bar blob at top (harness artifact), and C# search bars are taller (more vertical padding) so C# shows ~4 rows where C++ fits 7. (C# light reference was a blank/black broken capture — re-captured.)"
  },
  "selection_command_param": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SelectionCommandParam: the CollectionView + selection-command demo renders (SelectionChangedCommand + CommandParameter). Minor (selected-cell highlight = deferred gap).",
    "dark_note": "Matches (renders; selection-cell highlight is the deferred CV gap where applicable)."
  },
  "selection_synchronization": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SelectionSynchronization: both CVs (Set ItemsSource then SelectedItems / Set SelectedItems then ItemsSource) render with the correct Selected: Item 3, Item 2 labels + Item 1-4. Minor: selected-cell highlight = deferred gap.",
    "dark_note": "Matches (renders; selection-cell highlight is the deferred CV gap where applicable)."
  },
  "semantics": {
    "dark": "minor",
    "dark_note": "Near-exact match on dark: all SemanticProperties showcase rows render with correct colors (white labels, blue buttons) matching MAUI dark. Residual: minor vertical rhythm. (C# ref was blank — re-captured.)",
    "light": "minor",
    "light_note": "Near-exact match: the full SemanticProperties showcase renders matching MAUI — intro + readout labels, Label text TH/DH, blue Button text TH/DH, Entry/Editor text DTH, Search bar text DH, and the HeadingLevel labels + focus button, same content/order/colors. Residual: C++ slightly more compact vertical rhythm. (C# ref was a blank capture — re-captured.)"
  },
  "shadow_playground": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Renders the maui-compare ShadowPlayground: cyan #00B4DB box with a red Shadow (offset 10,10 / radius 10) + Background/Shadow Color/Offset X/Offset Y/Radius controls. C# ref re-captured (was a stale blank). RESIDUAL/gap: the Label with a Shadow text itself shows a red Shadow in MAUI but NOT in the C++ — the iOS LABEL does not render its Shadow (leaf-control update_shadow gap, analogous to the Clip gap; the box shadow path works). Minor.",
    "dark_note": "Renders (C# ref re-captured from stale blank). Gap: label-text shadow missing on iOS (leaf-control shadow)."
  },
  "shape_app_theme": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches the maui-compare ShapeAppTheme demo: green AppTheme caption + AppTheme-driven green Rectangle. Minor: sub-px rect size.",
    "dark_note": "Matches; AppTheme green Rectangle renders."
  },
  "shapes": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED to faithfully reproduce maui-compare Shapes(): ScrollView of 4 bold-captioned Start-aligned shapes — Ellipse (red fill, dark-blue stroke 4, 150x60), RoundRectangle (a Rectangle, navy fill, RadiusX 12/RadiusY 24, 150x60), EvenOdd Polygon pentagram (blue fill, red stroke 2, 100x100), Line (40,0->0,80, purple 2, 120x80). Matches C# 1:1 in both themes; only residual is the maui-compare card framing (harness).",
    "dark_note": "Same: all 4 shapes render identically with white-on-black bold captions. Faithful reproduction of Shapes()."
  },
  "single_bound_selection": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches the maui-compare SingleBoundSelection CollectionView: the instructional text + Selected: (none) label (correct initial state) + the country list (United States/Canada/Mexico/Brazil/Argentina). Selection-binding demo renders. Minor.",
    "dark_note": "Matches: instruction + Selected label + country list render."
  },
  "slider": {
    "dark": "minor",
    "dark_note": "FIXED: native white pill thumbs + gray tracks now render on dark (unset tints -> nil); BackgroundColor=Blue + Background=gradient bands render behind the track (new update_background + gradient-resize hook); MaximumTrackColor=Pink + ThumbColor=Orange + disabled gray match MAUI dark. Residual: minor vertical rhythm.",
    "light": "minor",
    "light_note": "FIXED (was broken): every slider now renders the native thumb + track. Two fixes — (1) unset Thumb/Min/MaxTrackColor now leave the UISlider native default (nil) instead of a transparent tint that hid the default white thumb + gray track; (2) added iOS update_background so BackgroundColor=Blue shows a flat blue band and Background=gradient shows the gradient band behind the track (MauiIosSlider.layoutSubviews resizes the gradient via resize_background_layers). MinTrack/MaxTrack/Thumb explicit colors + disabled gray all match MAUI. Residual: minor vertical rhythm."
  },
  "some_empty_groups": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SomeEmptyGroups after the grouped-CV supplementary fix (f40c64ec67): group headers/footers render (incl. for empty groups). Minor.",
    "dark_note": "Now matches (grouped-CV supplementary fix landed): group headers/footers render."
  },
  "stack_layout": {
    "light": "minor",
    "dark": "minor",
    "light_note": "NOW MATCHES: gave every BoxView an explicit 40x40 size in BOTH the C++ gallery AND the maui-compare app (a modern MAUI BoxView with no size collapses to 0). StackLayout (Vertical + Horizontal inner stacks) renders identically — same heading, same 6 colored boxes (centered where MAUI treats Fill+explicit-size as centered, which the C++ align_horizontal mirrors). Only residual is the maui-compare card framing (harness).",
    "dark_note": "Same: both apps render the colored boxes identically with theme-aware heading. Harness card framing is the only residual."
  },
  "staggered_layout": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare StaggeredLayout: the staggered-grid CollectionView layout renders its items. Minor.",
    "dark_note": "Matches (renders; selection-cell highlight is the deferred CV gap where applicable)."
  },
  "stepper": {
    "dark": "minor",
    "dark_note": "FIXED: labels + blue link render white on dark; BackgroundColor=Red now fills the stepper (update_background). Residual: demo difference — C# bold labels + full-width red band vs C++ regular labels + intrinsic-width red pill (port rendering is correct; the gallery demo lays the stepper intrinsic, not Fill).",
    "light": "minor",
    "light_note": "FIXED: BackgroundColor=Red now renders (added iOS update_background -> apply_background; MauiIosStepper.layoutSubviews keeps a gradient fill sized). Labels + blue Enable-Stepper link render (dark-text cascade). Residual: the C# stepper-page demo styles section labels bold/larger + lays the BackgroundColor stepper full-width (a wide red band) while the C++ gallery demo uses regular labels + an intrinsic-width red pill — a demo-layout/styling difference between the two apps (the port renders BackgroundColor correctly into the stepper frame)."
  },
  "styles": {
    "dark": "minor",
    "dark_note": "FIXED: the Style-Me button now has a light-gray fill so its dark text is readable on dark (was invisible — near-black text, no fill); matches MAUI light-fill + yellow border + dark text. The 3 label styles (gray base / pink derived / white default) render correctly via the style mechanism. Residual: vertical rhythm.",
    "light": "minor",
    "light_note": "Matches MAUI: the 3 label styles render correctly (base SubtitleStyle gray, derived custom style PINK wins, no-explicit-style default), and the Style-Me button shows a light-gray fill + yellow border + dark text. FIXED: gave the styled button a light BackgroundColor brush (it was a near-black TextColor with no fill — fine on light, invisible on dark) so it matches MAUI both themes; BackgroundColor is not a setter-targetable descriptor (flows through set_background_brush), the multi-setter Style still drives TextColor/CornerRadius/Stroke. Residual: minor vertical rhythm + button height/text-wrap."
  },
  "swipe_gesture": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare SwipeViewGestureRecognizer after the swipe arrange fix + the card-grid row-definition page fix: the white message card lays out Welcome to .NET MAUI! (title) / June 2026 (date, right) / A SwipeView with gesture recognizers (subtitle) / Double-tap the card... (description) in their proper rows (was: all 4 labels overlapping), + the TapCommand (double-tap) readout. Minor.",
    "dark_note": "Now matches (swipe arrange + card row-defs): card labels in proper rows."
  },
  "swipe_item_position": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SwipeItemPosition at rest after the swipe arrange fix: the Reveal SwipeView content box + Swipe in any direction caption. Minor (swipe interaction not capturable statically).",
    "dark_note": "Matches (swipe arrange fix): Reveal box + caption."
  },
  "swipe_item_size": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SwipeItemSize after the swipe arrange fix (5198718b6f): Different icon sizes (128/256/512 Swipe to Left rows) + Different SwipeView sizes (128/256-Height rows, correctly taller). Minor.",
    "dark_note": "Matches (swipe arrange fix): sized swipe boxes render."
  },
  "swipe_refresh": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SwipeRefresh at rest: the Swipe left to delete, pull to refresh caption above the CollectionView. Minor (swipe/pull interaction not capturable statically).",
    "dark_note": "Matches: swipe+refresh caption renders."
  },
  "swipe_threshold": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SwipeThreshold after the swipe arrange fix: the Threshold only on Android/iOS banner + Default/Custom Threshold (Reveal+Execute Mode) purple SwipeView boxes with the threshold sliders + Reveal/Execute threshold=80 readout. Minor.",
    "dark_note": "Matches (swipe arrange fix): threshold boxes + sliders render."
  },
  "swipe_view_margin": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SwipeViewMargin after the swipe arrange fix: Horizontal items revealed + the Modify the SwipeView Margin and Padding banner + Content Margin/Padding sliders + Horizontal/Vertical SwipeItems gray boxes. Minor.",
    "dark_note": "Matches (swipe arrange fix): margin/padding demo renders."
  },
  "swipe_view_shadow": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare SwipeViewShadow after the swipe arrange fix: Shadow in SwipeView Content + SwipeItems Content box (shadowed) + SwipeItemViews Content box (shadowed). Minor.",
    "dark_note": "Matches (swipe arrange fix): shadowed swipe content renders."
  },
  "switch": {
    "dark": "minor",
    "dark_note": "FIXED: BackgroundColor=Blue + Background=gradient bands render behind the switch on dark matching MAUI (update_background + gradient-resize hook); labels white (dark-text cascade); Disabled grayed. Residuals: bold labels in the C# demo (demo difference); ThumbColor orange (C++) vs white (C# off-state) — ambiguous.",
    "light": "minor",
    "light_note": "FIXED: BackgroundColor=Blue band + Background=gradient band now render behind the switch (added iOS update_background -> apply_background; MauiIosSwitch.layoutSubviews resizes the gradient), matching MAUI. Default/Disabled/OnColor switches + ThumbColor match the native control. Residuals: C# demo styles labels bold/larger (demo difference; port labels are regular like the slider page); the ThumbColor=Orange thumb renders orange in C++ vs white in the C# shot (off-state thumbTintColor — ambiguous, possibly a MAUI-iOS26 quirk)."
  },
  "switch_grouping": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now matches maui-compare SwitchGrouping after the grouped-CV supplementary fix (f40c64ec67): the Is Grouped: switch (on) + green group headers + orange Total members footers + grouped member rows. Minor.",
    "dark_note": "Now matches (grouped-CV supplementary fix landed): group headers/footers render."
  },
  "tabbed_flyout": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare TabbedFlyout: the tabbed-page / flyout-page host renders (the native tab bar / flyout chrome). Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "templated_view": {
    "dark": "minor",
    "dark_note": "Structure now matches MAUI: the standard CardView (Slavko Vlasic + Lorem body) and the three templated Compact cards (SlateGray image block + 'Compact card'/name/description via the ControlTemplate's ContentPresenter) all render. Residual is the recurring system-font rendering difference shared by every page.",
    "light": "minor",
    "light_note": "Structure now matches MAUI: the standard CardView (Slavko Vlasic + Lorem body) and the three templated Compact cards (SlateGray image block + 'Compact card'/name/description via the ControlTemplate's ContentPresenter) all render. Residual is the recurring system-font rendering difference shared by every page."
  },
  "time_picker": {
    "dark": "minor",
    "dark_note": "Rendering now complete: solid blue + both gradient fills render flat matching MAUI dark (gradient-resize FIXED). Residuals: invariant US time format (deliberate deviation) vs MAUI device-locale 24h 0:00; vertical rhythm; sample-data gradient color.",
    "light": "minor",
    "light_note": "Rendering now complete: solid BackgroundColor=Blue (flat fill) + both gradient Background rows (yellow->green + a second gradient) all render, matching MAUI structure (FIXED: MauiIosTimePicker now re-syncs its background sublayer on layoutSubviews via resize_background_layers, so gradient fills track bounds). Residuals: time text is invariant US (deliberate port deviation w/ iOS tests) vs MAUI device-locale 24h 0:00; compact vertical rhythm; 3rd gradient is a different demo color than the C# sample."
  },
  "title_bar": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare TitleBar: iOS has NO window TitleBar (a desktop/Windows concept; UIKit renders none) — both MAUI and the port show just the page content, which matches. Faithful not-supported. Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "toolbar": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare Toolbar: the page with toolbar items renders (iOS toolbar items land in the native nav bar). Minor.",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "transform_playground": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: a red/blue square in a gray box + Rotate/Scale/Skew transform sliders render identically to maui-compare. No code change. Residual: harness card framing (C++ fills full height) + a minor slider-value number format (\"1.0\" vs \"1\").",
    "dark_note": "Same: renders identically, theme-aware. Matches C#."
  },
  "transformations": {
    "light": "minor",
    "dark": "minor",
    "light_note": "MATCHES: the \"SCALE AND ROTATE\" Scale/ScaleX/ScaleY/Rotation/Rotation X/Y sliders + Anchor steppers + Translation sliders render identically to maui-compare. No code change. Residual: harness card framing (C++ fills full height) + a minor slider-value number format (\"1.0\" vs \"1\").",
    "dark_note": "Same: renders identically, theme-aware. Matches C#."
  },
  "triggers": {
    "dark": "minor",
    "dark_note": "Matches MAUI dark: header + instruction + validation Entry + Highlight-off label + blue Toggle-highlight button all render correctly (dark-text cascade). Residual: bold-vs-regular header weight (demo difference) + vertical rhythm.",
    "light": "minor",
    "light_note": "Matches MAUI: the trigger demo renders the instruction label, the validation Entry (Enter a System.Double, turns red on invalid via the data/property trigger), the Highlight-off label, and the blue Toggle-highlight button — same controls/order/colors. Residual: the Triggers header is bold/larger in the C# demo vs regular weight in the C++ gallery (recurring bold-header demo difference) + minor vertical rhythm."
  },
  "update_path_data": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches the maui-compare UpdatePathData demo: a black bezier S-curve (Path M 10,100 C 10,300 300,-200 300,100) above the live data string + Update Path Data button. Minor: leaf path stroke antialiasing.",
    "dark_note": "Matches; minor leaf path-stroke AA."
  },
  "varied_size_selector": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Now renders maui-compare VariedSizeSelector (was stale 🔴/blank-dark): the data-template-selector items with varied backgrounds (Coffee# brown / Milk# tan) + Insert/Add/Remove buttons + Index/Latte entries. Minor.",
    "dark_note": "Now renders (was stale blank): colored selector items + insert/add/remove controls."
  },
  "vertical_stack": {
    "light": "minor",
    "dark": "minor",
    "light_note": "NOW MATCHES: gave every BoxView an explicit 40x40 size in BOTH the C++ gallery AND the maui-compare app (a modern MAUI BoxView with no size collapses to 0). VerticalStackLayout renders identically — same heading, same 6 colored boxes (centered where MAUI treats Fill+explicit-size as centered, which the C++ align_horizontal mirrors). Only residual is the maui-compare card framing (harness).",
    "dark_note": "Same: both apps render the colored boxes identically with theme-aware heading. Harness card framing is the only residual."
  },
  "visual_states": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare VisualStates: Entry with VisualStateManager + Entry to enable 2nd Entry + Button with Normal/PointerOver states (Hover me) + Button with Normal/PointerOver/Pressed states (Click me) + the explanation. Minor: hover/pressed states are pointer-driven (not shown in a static at-rest capture).",
    "dark_note": "Matches (renders; native/not-supported chrome noted where applicable)."
  },
  "web_view": {
    "dark": "minor",
    "light": "minor",
    "light_note": "Matches maui-compare WebView and renders MORE: the C++ WKWebView actually displays the static HtmlWebViewSource content (Welcome heading + Served from a static HtmlWebViewSource.) and shows new_page -> https://demo.test/welcome, while the C# ref captured before navigating (blank web area + No navigation yet). Same Page A/Page B/Back/Forward/Reload/Eval 1+1 controls + Eval result readout. Minor (nav-state capture-timing difference; the WebView renders correctly).",
    "dark_note": "Matches: WKWebView renders the static HTML + nav controls (C# captured pre-navigation)."
  },
  "z_index": {
    "light": "minor",
    "dark": "minor",
    "light_note": "FIXED: View.Margin landed (9bc9b11f0c), so the per-label Margin=n*15 cascade now renders — the 10 colored labels fan out diagonally exactly like real .NET MAUI (was a single stacked label before). ZIndex paint order correct (Label 9 red on top), label backgrounds render, readout + stepper bar correct. Residual: maui-compare card framing (harness).",
    "dark_note": "Same: the 10-label diagonal Margin cascade renders, z-order correct, theme-aware. Matches the C# diagonal fan."
  }
}