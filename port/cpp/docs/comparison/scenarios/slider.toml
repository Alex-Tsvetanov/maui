# Scenario for the `slider` page: capture at rest, then drag the Default slider's THUMB to the right.
#
# RESTORED after d51acde0e8 dropped it as undrivable. That was wrong, and the reason is worth keeping
# because it is a measurement trap, not a typo:
#
#   the old `at` was [0.43, 0.122], commented "default_slider_ THUMB. MEASURED: the filled blue track
#   runs x[0,516]". A thumb at x=516px IS 0.43 of a 1206px frame — but a Default slider at REST has no
#   filled track at all: value 0, thumb hard left at x~55px (0.045). The 516 was measured off an
#   ALREADY-DRAGGED frame, back when the lane published the REACTED frame as the board still. 89261d905a
#   fixed that ordering, the published still became the true at-rest one, and the coordinate was left
#   pointing at bare track.
#
#   An iOS UISlider only drags when the gesture STARTS on the thumb; a drag beginning mid-track does
#   nothing. So the drag moved neither column, and "neither column reacts" was read as "idb cannot
#   actuate a UISlider". MEASURED 2026-08-07, both columns, iPhone17-265 / iOS 26.5:
#     at=[0.43,0.122] (mid-track)  maui bbox=None            cpp bbox=None
#     at=[0.045,0.122] (thumb)     maui bbox=(0,42,648,206)  cpp bbox=(0,42,660,206)
#   and the port's thumb visibly lands mid-track with a blue filled run behind it. Both columns react.
#
# TO RECALIBRATE: find the thumb in captures/ios/<col>/slider_light.png — the rounded white cap at the
# LEFT end of the grey track — and take its centre. Do NOT measure the blue fill: at rest there isn't one.

tag = "slider"
themes = ["light"]
timeout_seconds = 30

[[steps]]
name = "initial"

[[steps]]
name = "dragged-right"
action = "drag"
at = [0.070, 0.122]      # the Default slider's THUMB at rest, NOT the track. RE-MEASURED, not nudged:
                         # 0.045 (=18pt) was inside the lane's 20pt edge-margin guard and the run
                         # rejected it. Scanning row y=320px of slider_light.png, the near-white thumb
                         # runs x[0..110]px = [0..36.7]pt in BOTH columns, so its drivable band is
                         # 20..36.7pt and 0.070 (=28pt) is the middle of that band — still on the thumb,
                         # 8pt clear of the guard.
to = [0.50, 0.122]       # dragged right along the track
steps = 20               # a deliberate pan, not a flick: the drag needs intermediate move events
duration = 0.8           # 40ms per move — above the ~20ms floor where both OSes coalesce moves

# CATALYST NEEDS ITS OWN AIM, BOTH ENDS. The pair above is a fraction of a 402x874 PORTRAIT PHONE;
# the same fractions of Catalyst's 1024x800 LANDSCAPE window resolve to (72,98) -> (512,98), and
# y=98 is the BackgroundColor *label* row, not a slider. MEASURED 2026-08-07 on the maccatalyst
# captures: at row y=65 the Default slider's near-white thumb is the run x[0..27], identically in
# maui, cpp AND xaml. Centre (14,65) = this fraction of the 1024x800 rect.
#
# The END is overridden too, not just the start. A drag that begins on the thumb and ends 33px off
# the track row is not a shorter drag, it is a different gesture — and the end is CLAMPED rather
# than rejected (run_comparison.drag_endpoints), so a wrong `to` fails silently.
#
# Fractions, not pixels: see the note in switch.toml — an absolute pair in an at_<env> key would
# reclassify the whole file and drop it from the iOS lane.
# ANDROID NEEDS ITS OWN AIM TOO, AND FOR A REASON THE OTHER LANES DO NOT HAVE: the portable `at`
# above is INSIDE ANDROID'S BACK-GESTURE EDGE ZONE, so the OS eats the drag before the Slider sees it.
# 0.070 x 1080 = 75.6 px from the left edge, dragged rightward, IS the system Back gesture.
#
# MEASURED on run 2026-08-18-00_32_10 (light, 1080x2340), maui_xaml vs cpp, per frame:
#   1 'initial' 0.03% | 2 'at-rest' 0.28% | 3 'gif01' 0.03% | 4 'gif02' 0.04% | 5 'gif03' 49.42%
#   6-14 45.34% (FLAT -- not a moving slider, a DIFFERENT PAGE)
# The back-gesture arrow bubble is visible on the left edge of frame 4 in BOTH columns; by frame 6 the
# app has popped the page and maui_xaml is showing a Z-Index demo while cpp shows a WebView demo. The
# columns landed on different pages, which is the whole of the flat ~45%. At rest they agree to 0.03%.
#
# This is STRUCTURAL, not a mis-measure: at rest the Default slider's thumb IS at the far left (value 0),
# which is exactly where Android's back zone lives. This page cannot be driven at that aim on Android.
# So aim at the "Minimum (5) and Maximum (15)" slider instead -- its value is 10 at rest, so its thumb
# sits MID-TRACK, clear of BOTH edge zones (gesture nav grabs left and right).
#
# MEASURED on frame 0002 (at-rest, light), thumb circle bounds, ALL THREE COLUMNS:
#   maui_xaml x[523,555] y[812,842] | cpp x[524,555] | cpp_xaml x[524,555]  -> centre (539, 827) +/-1 px
# Track runs x[44,1035] at y[826,830], so the `to` below stays on it with 180 px of right-edge clearance.
# TO RECALIBRATE: rescan frame 0002 rows 780..900 for the compact dark run; do NOT move this toward
# either edge.
at_android = [0.499, 0.353]      # 539/1080, 827/2340 -- the Min/Max slider's thumb, mid-screen
to_android = [0.833, 0.353]      # 900/1080 -- rightward along the track, clear of the right edge zone

at_macos-arm64 = [0.014, 0.081]
to_macos-arm64 = [0.50, 0.081]

# WINDOWS NEEDS ITS OWN AIM TOO -- the portable `at` above (0.070, 0.122) was never actually
# calibrated for this lane (no at_windows-arm64 existed at all until this override), and on a
# 1024x800 window it lands at (72, 98): 55px right of and 20px below the Default slider's thumb, on
# bare whitespace between the thumb row and the "BackgroundColor" row beneath it. A drag starting
# there presses nothing, which is indistinguishable from "the gesture mechanism doesn't work" --
# it is the SAME symptom the touch-vs-mouse investigation below fixed, but this page needed a second,
# unrelated fix (a miscalibrated coordinate) on top before it could show it.
#
# MEASURED on frame 0001 (at-rest, light), thumb circle bounds, ALL THREE COLUMNS (byte-identical):
#   x[12,21] y[74,81] -> centre (16.5, 77.5), i.e. this fraction of the 1024x800 rect.
# TO RECALIBRATE: rescan frame 0001 rows 60..95, columns 0..40 for the near-(0,103,192) blue run.
at_windows-arm64 = [0.016, 0.097]
to_windows-arm64 = [0.50, 0.097]
