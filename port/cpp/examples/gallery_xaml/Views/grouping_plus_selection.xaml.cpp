// grouping_plus_selection.xaml.cpp — implements examples::Views::grouping_plus_selection_page() (declared in
// grouping_plus_selection.xaml.hpp).
//
// LEGACY LOCAL TWIN (pre-migration): embeds the local Views/grouping_plus_selection.xaml, which has not yet moved to
// the canonical shared pages dir (port/maui-reference/pages). Migration: move the markup there (adding x:Class per
// docs/AUTHORING.md), delete Views/grouping_plus_selection.xaml, re-run `e2e.py gen`.
//
// GENERATED by port/tools/e2e/e2e.py gen — do not edit by hand; re-run the generator instead.

#include "Views/grouping_plus_selection.xaml.hpp"

#include "maui/fixed_string.hpp"
#include "maui/xaml_build.hpp"

namespace
{
    // The raw markup, embedded by the compiler (resolved relative to this source file). The two-line
    // embed is the canonical form (a #embed directive cannot live inside a macro body).
    constexpr unsigned char grouping_plus_selection_xaml_bytes[] = {
#embed "grouping_plus_selection.xaml"
    };
    constexpr maui::fixed_string grouping_plus_selection_xaml{grouping_plus_selection_xaml_bytes};
} // namespace

namespace examples::Views
{
    std::unique_ptr<maui::controls::content_page> grouping_plus_selection_page()
    {
        // unique_ptr<page_impl<no_view_model>> upcasts to unique_ptr<content_page> on return.
        return maui::build_page<maui::no_view_model, grouping_plus_selection_xaml>();
    }
} // namespace examples::Views
