// clip_gallery.xaml.cpp — implements examples::Views::clip_gallery_page() (declared in clip_gallery.xaml.hpp).
//
// LEGACY LOCAL TWIN (pre-migration): embeds the local Views/clip_gallery.xaml, which has not yet moved to the
// canonical shared pages dir (port/maui-reference/pages). Migration: move the markup there (adding
// x:Class per docs/AUTHORING.md), delete Views/clip_gallery.xaml, re-run `e2e.py gen`.
//
// GENERATED by port/tools/e2e/e2e.py gen — do not edit by hand; re-run the generator instead.

#include "Views/clip_gallery.xaml.hpp"

#include "maui/fixed_string.hpp"
#include "maui/xaml_build.hpp"

namespace
{
    // The raw markup, embedded by the compiler (resolved relative to this source file). The two-line
    // embed is the canonical form (a #embed directive cannot live inside a macro body).
    constexpr unsigned char clip_gallery_xaml_bytes[] = {
#embed "clip_gallery.xaml"
    };
    constexpr maui::fixed_string clip_gallery_xaml{clip_gallery_xaml_bytes};
} // namespace

namespace examples::Views
{
    std::unique_ptr<maui::controls::content_page> clip_gallery_page()
    {
        // unique_ptr<page_impl<no_view_model>> upcasts to unique_ptr<content_page> on return.
        return maui::build_page<maui::no_view_model, clip_gallery_xaml>();
    }
} // namespace examples::Views
