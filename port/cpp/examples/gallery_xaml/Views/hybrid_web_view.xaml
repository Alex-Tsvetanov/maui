<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="HybridWebView">
    <!-- HybridWebView has no supported leaf; the row-1 web view is degraded to a Border placeholder. -->
    <Grid Padding="12" RowSpacing="8" ColumnSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Editor Grid.Row="0" Grid.Column="0" Text="HybridWebView here" IsReadOnly="True" MinimumHeightRequest="200" />

        <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="8">
            <Button Text="Send message to JS" />
            <Button Text="Invoke JS" />
            <Button Text="Invoke Async JS" />
            <Button Text="Test JS Exception" />
            <Button Text="Test JS Async Exception" />
        </VerticalStackLayout>

        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Stroke="Gray" StrokeThickness="1"
                BackgroundColor="WhiteSmoke">
            <Label Text="HybridWebView (HybridRoot=HybridSamplePage)" HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center" />
        </Border>
    </Grid>
</ContentPage>
