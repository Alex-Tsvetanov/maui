// scattered_radio_button.xaml.cpp — implements examples::Views::scattered_radio_button_page() (declared in
// scattered_radio_button.xaml.hpp).
//
// LEGACY LOCAL TWIN (pre-migration): embeds the local Views/scattered_radio_button.xaml, which has not yet moved to the
// canonical shared pages dir (port/maui-reference/pages). Migration: move the markup there (adding
// x:Class per docs/AUTHORING.md), delete Views/scattered_radio_button.xaml, re-run `e2e.py gen`.
//
// GENERATED by port/tools/e2e/e2e.py gen — do not edit by hand; re-run the generator instead.

#include "Views/scattered_radio_button.xaml.hpp"

#include "maui/fixed_string.hpp"
#include "maui/xaml_build.hpp"

namespace
{
    // The raw markup, embedded by the compiler (resolved relative to this source file). The two-line
    // embed is the canonical form (a #embed directive cannot live inside a macro body).
    constexpr unsigned char scattered_radio_button_xaml_bytes[] = {
#embed "scattered_radio_button.xaml"
    };
    constexpr maui::fixed_string scattered_radio_button_xaml{scattered_radio_button_xaml_bytes};
} // namespace

namespace examples::Views
{
    std::unique_ptr<maui::controls::content_page> scattered_radio_button_page()
    {
        // unique_ptr<page_impl<no_view_model>> upcasts to unique_ptr<content_page> on return.
        return maui::build_page<maui::no_view_model, scattered_radio_button_xaml>();
    }
} // namespace examples::Views
