# STATUS — MAUI → C++23 port progress

> One row per component. Update at step 7 of the per-component loop (`CLAUDE.md`). Never mark a
> partial port as done silently — use the Notes column.
> Legend: ✅ done · 🚧 in progress · ⬜ not started · — n/a

## Per-view safe area (U20 completion) — ✅ DONE (contract ✅ / application ✅) (2026-07-15)

Closing the last MAUI safe-area fidelity gap, surfaced by the maccatalyst parity sweep: the port's content
sits a **uniform ~32px below MAUI** on Mac Catalyst (41pt safeAreaInsets.top × the ~0.77 Catalyst UIKit scale).

**Root cause (source-derived, not inferred):** MAUI applies the safe area **per view**, gated by
`MauiView.cs:771` `_appliesSafeAreaAdjustments = !IsParentHandlingSafeArea() && RespondsToSafeArea() &&
!_safeArea.IsEmpty` (`RespondsToSafeArea()` = *not a UIScrollView descendant*; the parent check is
edge-aware), with each control's `SafeAreaEdgesDefaultValueCreator` deciding participation:
`ContentPage`/`Border`/`ContentView` → **None** (edge-to-edge), `Layout` → **Container**, `ScrollView` →
**Default** (→ `ContentInsetAdjustmentBehavior.Automatic`), and `UICollectionView` **bypasses MAUI's arrange
chain entirely** (MauiView.cs:67). The port instead insets ONCE at the **page/host** level, which is wrong for
CollectionView-rooted pages (full 32px off) and only approximates Layout-rooted pages (25px off).

- ✅ **Contract** (commit `65ff28596f`): `layout` gains `SafeAreaEdges` (default **Container**) +
  `i_safe_area_view2` with `Layout.cs`'s verbatim `GetSafeAreaRegionsForEdge` rule; shared non-template
  descriptor (`layout_safe_area_edges_property`); `set_safe_area_insets` no-op per Layout.cs. Tests ported
  from `SafeAreaTests.cs` (8 cases, red→green). Full headless **3754/3754**. Inert — no consumer yet.
- ✅ **Application**: (1) `app_host::drive_layout` stops pre-insetting — the page always arranges over FULL
  bounds, and the host's only safe-area job is now C# `MauiView`'s: report the realized insets to the
  `i_safe_area_view2` it hosts (MauiView.cs:764). UIKit derives insets from the frame, which isn't set until
  arrange, so the host seeds the root content layout from the full-vs-safe rect difference it already had.
  (2) `layout` folds the safe area into `measure` (subtract from the constraint, add back to the reported
  size — MauiView.cs:605) and `arrange` (inset the CHILDREN's bounds — MauiView.cs:634 `AdjustForSafeArea`),
  never into its own `frame_`, so layout BACKGROUNDS stay edge-to-edge. (3) `set_safe_area_insets` now
  STORES (documented deviation: Layout.cs no-ops it because native MauiView holds `_safeArea`; the port
  arranges cross-platform so the value must reach the object). (4) the gate is ported whole:
  `responds_to_safe_area()` (scroll-descendant walk) + edge-aware `is_parent_handling_safe_area()` +
  `applies_safe_area_adjustments()` on `i_safe_area_view2` (C# `MauiView.AppliesSafeAreaAdjustments`,
  MauiView.cs:496; default false = never insets/never blocks, exact for every non-layout implementer).
  6 new `layout_safe_area_apply` tests, red→green. Full headless **3760/3760**.
- ✅ **Deleted the `insets.top = 59.0` guess** in `host_run.mm`. UIKit hasn't populated insets when the port
  drives its FIRST layout (measured: first pass reads `t=0`, later passes the real `t=41`), and that hack
  papered over it — inventing a 59pt inset on Catalyst, whose title bar is chrome outside the safe area.
  Replaced by the real mechanism: `MauiRelayoutView.safeAreaInsetsDidChange` re-drives layout when UIKit
  reports the true insets (what MAUI gets free from UIKit-driven layout). Can't spin — the safe area never
  moves a layout's own frame, so re-driving leaves every native frame put.
- **Verified (maccatalyst, measured not eyeballed):** `vertical_stack_layout` and `basic_grouping` both
  **0px shift / 0.0 residual — pixel-perfect** vs MAUI. **iOS re-verified (shared change):** `button`
  unchanged (+24px/15.23 → +24px/15.24 = noise; the 59pt guess happened to equal this sim's real Dynamic
  Island inset, so removing it is a no-op there); `basic_grouping` **improved** — group header/footer
  background bands now render (the long-standing "missing bg-fills" cluster: the port was double-counting
  insets on the UICollectionView path MAUI deliberately bypasses).
- 🛠️ **Capture-tooling bug fixed in the same slice** (it silently invalidated the first 3 measurements):
  Catalyst apps restore their last window via macOS saved state and IGNORE `MAUI_SAMPLE_PAGE` → plausible
  screenshots of the WRONG PAGE, scored against the right MAUI reference. `vm_agent_macos.py` now clears
  `~/Library/Saved Application State/<bundleid>.savedState` + sets `NSQuitAlwaysKeepsWindows=false` on EVERY
  launch. Lesson: a measurement that disagrees with the source is a reason to distrust the CAPTURE, not only
  the code — three "offset" theories (59px, 47px) were pure artifacts of this.
- ✅ **MauiScrollView's safe area (the second half the host change exposed).** Removing the host pre-inset
  left every ScrollView-rooted page 32px too high (`slider` / `picker` / `layout_is_enabled`: measured
  −32px at residual ~1.3–1.9 = identical content, pure offset). `layout`'s fold cannot fix it —
  `responds_to_safe_area()` correctly refuses to inset scroll DESCENDANTS (MauiView.cs:196), so the scroll
  view itself must do it. Ported `MauiScrollView`:
  - `scroll_view::effective_safe_area()` = `MauiScrollView.ValidateSafeArea` (cs:383-386) + the cs:389
    gate. **The branch is the whole point**: iOS sets `AdjustedContentInset` ONLY when the content
    overflows; when it FITS, that stays zero and the scroll view must inset its own content (GetInset,
    per-edge via GetManualInsetForEdge). Verified on-device, not assumed: logging the live Catalyst
    scroller showed `safeAreaTop=41 adjInsetTop=0 behavior=automatic` once the window grew enough for the
    content to fit — UIKit had silently stopped adjusting.
  - `scroll_view::arrange` = `MauiScrollView.CrossPlatformArrange` (cs:432-455): manual branch arranges at
    the inset ORIGIN; system branch arranges at 0-origin with only the SIZE reduced (UIKit's contentInset
    already supplies the offset — honoring the origin too would double it). `measure` = cs:548-562.
  - `MauiIosScrollView` (new) pushes `safeAreaInsets` + `adjustedContentInset` and re-arranges in place.
  - **The anti-flip-flop clamp (cs:474-490) is load-bearing, not an optimization.** Content sized between
    "fits alone" and "fits with the safe area" makes UIKit flip between the two branches, and since our
    contentSize depends on the branch, they chase each other forever. This is not theoretical: without it
    the iOS gallery HUNG on exactly the ScrollView-rooted pages (launched, went white, SpringBoard killed
    it). MAUI's fix — force contentSize past the bounds to pin UIKit in scrollable mode — is ported
    verbatim, plus a re-entrancy latch.
  - 5 new `scroll_view_safe_area_apply` tests. Full headless **3765/3765**.
  - **Measured:** `slider` −32→**0 / 0.0**, `picker` −32→**0 / 0.0** (both pixel-perfect), `layout_is_enabled`
    −32→**0 / 0.4**. iOS re-verified: `button` unchanged (+24/15.21 vs +24/15.23 = noise); `slider`/`picker`
    render correctly with **0px** horizontal/vertical shift vs HEAD (their residual is runtime state — a
    slider value, a picker selection).
## Button BorderWidth measure (macOS + iOS) — ✅ DONE (2026-07-16)

`button` sat ≈6px high from y≈280 down (rows 40-280 matched EXACTLY, residual 0.00 — a single short
element shifting everything below it). Root-caused by measuring the colored bands: only the
`BorderWidth="4"` button differed — MAUI 30px vs port 24px (2 x 4pt x the 0.77 Catalyst scale = 6px);
the other four buttons were byte-identical.

**C# rule** (`Button.iOS.cs` ICrossPlatformLayout.CrossPlatformMeasure :44, :59-83, :124-130): the border
gets space via the content edge insets — `contentEdgeInsets = new Thickness(borderWidth)` (+ padding for
the `image is null` branch), and the returned height is `titleRectHeight + padding.Top + padding.Bottom +
borderWidth * 2`. It applies to TEXT-ONLY buttons, not just the image path.

**The port's bug was subtle**: `get_desired_size` had that exact formula — but only on the IMAGE path. A
text-only button early-returned a bare `-[UIButton sizeThatFits:]`, which sees only the insets
`map_padding` left behind, and the mapper order is Padding BEFORE StrokeThickness (mirroring C#'s), so
`layer.borderWidth` was still 0 and the border never reached the insets. C# survives the same order
because its Controls-level Button REPLACES MapPadding on iOS with a measure-invalidate
(`Button.Mapper.cs:19-21`) and recomputes the insets inside CrossPlatformMeasure — i.e. at MEASURE time,
from the live BorderWidth. Fix: `apply_content_edge_insets(button, padding, border_width)` with the
border passed IN, called from the text-only measure path — order-independent by construction.

- 2 new `ios_button_seam` tests (border grows by 2x; negative clamps to 0), red -> green. Headless 3765/3765.
- **Measured:** maccatalyst `button` -6px/10.5 -> **0/0.5**, band heights now `[25,24,25,30,25]` == MAUI
  EXACTLY. Board pixel_xaml red -> **green** (0.40%).
- **iOS (shared code) also fixed**: `button` **+24px/15.23 -> 0/2.09** vs the ruling-6 ground truth. The
  iOS board's own verdict was NOT published for it — see the caveat below.

**⚠️ iOS board scores against a STALE MAUI reference (pre-existing; for the iOS phase).**
`pixel_score.py` reads `captures/ios/maui/`, but nothing refreshes it — `capture_ios_clean.py --app maui`
writes to `port/maui-reference/captures/ios/` (the ruling-6 ground truth). The two differ by mean **54.6**
on `button` alone, so iOS pixel verdicts are measured against the wrong reference (it scored the FIXED
button as red 27.48% while the fresh reference measures 0/2.09). Ruling 6 says never write to the frozen
tree, so the fix is to point the scorer at `port/maui-reference/` — iOS-phase work, not re-litigated here.

- **Known remaining (macOS):** grid_grouping 45%, border_playground 25.7%, context_flyout 25.6%,
  entry 19.9%, swipe_view_margin 10.6%, radio_button_border 5.6%, nested_collection 3.6%.
- **Known caveat:** 5 `xaml` maccatalyst captures (`gap_menu_bar`, `gap_swipe_view_items`, `gap_title_bar`,
  `swipe_transition_mode`) are 2048x1536 leftovers from an older capture era and are not in the sweep's
  page list, so they went unrefreshed. Pre-existing; not this slice's.

## DevFlow test/automation agent (`maui::devflow`) — ✅ DONE (headless), gallery opt-in compile-checked (2026-07-12)

A minimal, **debug-gated** in-app test/automation agent modeled on Microsoft's experimental .NET MAUI DevFlow
agent — a localhost JSON-over-HTTP API an external driver (the parity runner's Python `DevFlowDriver`) uses to
introspect + poke the running UI. New:
- **`include/maui/devflow/agent.hpp` + `src/devflow/agent.cpp`** — the module. Compiled ONLY under the CMake
  option `MAUI_DEVFLOW` (ON for dev/test, `-DMAUI_DEVFLOW=OFF` for release ⇒ entirely absent; the port's
  `#if DEBUG` + `AddMauiDevFlowAgent()` analog). Two pieces: `handle_command(...)` — the PURE synchronous
  command core over the existing element-tree walk (`element::visit_logical_children`), `AutomationId`
  (`view::automation_id`), arrange bounds (`i_view::frame`), and activation (`i_button::send_clicked`) — no
  parallel tree; and `agent` — a dependency-free localhost HTTP/1.1 server (POSIX sockets, one background
  poll-based accept thread so `stop()` never races `accept`, bind 127.0.0.1 only). Requests marshal onto the
  UI thread via a caller-supplied `ui_executor` (inline default; a dispatcher-backed sync executor on native).
  Never auto-started — a host opts in via `start_agent(root_provider, port, info, run_on_ui)`.
- **Commands (5, minimal — the rest deferred):** `GET /ready` (mounted + past first layout + app/version/
  commit), `GET /tree` (type/automation_id/bounds[x,y,w,h]/children JSON), `POST /tap {automation_id}`
  (AutomationId match then x:Name `FindByName` fallback → `send_clicked`), `GET /screenshot` (501 — the
  external screencapture is ground truth), `POST /shutdown` (200 then clean exit). Protocol + a Python client
  stub: **`port/cpp/docs/DEVFLOW_PROTOCOL.md`**.
- **Wire-in:** `maui_hosting` gains the source + a PUBLIC `MAUI_DEVFLOW` compile-def (so a consumer's opt-in
  block compiles). The maccatalyst `examples/gallery` opts in behind `#if defined(MAUI_DEVFLOW)`, started at
  runtime only when `MAUI_DEVFLOW_PORT` is set (so normal parity captures are undisturbed), with a dispatcher-
  backed UI executor. Compile-checked on headless (`-fsyntax-only`, pure-C++ dispatcher usage — identical on
  every backend); runtime device test deferred.
- **Tests:** `tests/devflow/agent_tests.cpp` (11 cases in `maui_hosting_tests`, self-gated on `MAUI_DEVFLOW`) —
  the 10 core cases (ready before/after mount + null root, tree lists the button w/ id+type+bounds, tap by
  automation_id + short `id` key + unknown-not-found, screenshot 501, shutdown callback, 404) are backend-
  agnostic; 1 socket end-to-end case (headless only) drives `/ready` + `/tap` over a real TCP connection with
  an in-test HTTP client — the exact wire the Python driver speaks. Verify: `tools/dev.sh devflow` ⇒ 11/11
  green on headless.

## Cross-platform entry point — Stage 5b (gallery → examples; src/samples retired) — ✅ DONE (2026-06-24)

The final, destructive packaging stage: the runnable demo gallery moved OUT of the framework tree into the
standalone `examples/` project as a **pure-C++** app, and **`port/cpp/src/samples` is entirely gone** (the 5
`.mm` hosts + `gallery_host.hpp` + the plist templates + all its CMake build blocks). New/changed:
- **`examples/gallery/`** — `main.cpp` (a pure-C++ `gallery_app : application`, no `.mm`/platform headers),
  `gallery_host.hpp` (the `MAUI_GALLERY_PAGES` X-macro + the `gallery_pre_mount`/`gallery_post_mount` hook
  helpers, `git mv`'d from `src/samples`), `pages/` (178 page headers, `git mv`'d), `resources/` (`git mv`'d),
  `gallery_info.plist.in` (the UIAppFonts variant), and `CMakeLists.txt`. Registered in `examples/CMakeLists.txt`.
  The gallery selects its page at runtime from `MAUI_SAMPLE_PAGE` (default `value_controls`) via the X-macro
  dispatch behind a small type-erased `gallery_page_holder`, and seeds the CROSS-PLATFORM app theme from
  `MAUI_APPEARANCE` via `application::set_platform_app_theme` — NO UIKit/AppKit call in the example.
- **Generic-mount lifecycle hooks** — `application::on_pre_mount`/`on_post_mount` (default no-ops) added to the
  framework; `maui::hosting::mount_window` now calls them around the window mount. `gallery_app` overrides them
  to forward the selected page's optional `register_handlers`/`on_mounted`. This generalizes the gallery's
  former per-`.mm` plumbing onto the application the builder mints, so the pure-C++ `run_app` flow drives them.
- **Native interface style from the app theme** — `src/platform/ios/host_run.mm` (and the macOS twin
  `src/platform/apple/host_run.mm`) now read `application::requested_theme()` after boot and force the native
  window's `overrideUserInterfaceStyle` / `NSAppearance`. This preserves the parity dark/light captures the
  old `ios_gallery.mm` produced by forcing UIKit directly — WITHOUT any platform code in the example.
- **`maui_add_app`** gained optional `IDENTIFIER` + `PLIST` args; the gallery keeps bundle id
  `dev.maui-cpp.ios-gallery` so the parity tooling launches it unchanged.
- **Parity pipeline re-pointed** — `tools/parity/capture_all.py` documents the new examples-based iOS build
  command at its top (bundle id + env vars + `page_keys.txt` unchanged); `gen_comparison_readme.py` reads page
  header comments from `examples/gallery/pages`. The 11 in-tree page-wiring tests now `#include` the pages from
  `examples/gallery/pages` (the framework test target already has the repo root on its include path).

Verify: **headless 3080/3080 (unchanged from baseline — pages are gallery headers, not framework libs/tests)**;
the gallery example builds + boots on headless and on iOS (installed as `dev.maui-cpp.ios-gallery`), with the
light+dark parity captures reproduced. `gate.sh --fast`: see the commit.

## Cross-platform entry point — Stage 2 (apple + ios native run loops) — ✅ DONE (2026-06-24)

The SAME pure-C++ `samples/hello_world/main.cpp` (Stage 1) now opens a REAL native window on the apple
(macOS/AppKit) and ios (UIKit) backends, with **zero Objective-C in the user's code** — all the
`NSApplication`/`UIApplicationMain`/AppDelegate glue lives in the framework's `run_app` lane now. New/changed:
- **`src/platform/apple/host_run.mm`** — `run_app` for AppKit: stands up `NSApplication` + an app delegate
  whose `applicationDidFinishLaunching` builds the app from the configurator, asks it for its window
  (`IApplication.CreateWindow`), runs the Stage-1 generic mount (`mount_window` + `drive_layout` over a
  480×720 default, REUSED as-is — NO per-control code), shows the `NSWindow` key+front, and enters
  `[NSApp run]`. Returns the run-loop exit code. The built `maui_app` is held in a function-local-static
  `host_state` (PROFILE §8 — outlives the whole run loop). Verified: launches, mounts, shows an NSWindow
  titled "Hello" (stderr+os_log mount trace), runs until the window closes.
- **`src/platform/ios/host_run.mm`** — `run_app` for UIKit: a classic (non-scene) `UIApplicationDelegate`
  whose `didFinishLaunchingWithOptions` builds + mounts the app and lays it out over the root VC's
  **safe-area** bounds (the recipe factored verbatim from `ios_gallery.mm` boot_page — `layoutIfNeeded` +
  status-bar/Dynamic-Island fallback + the VC-backed-root-page full-controller-bounds branch), makes the
  `UIWindow` key+visible, returns YES. `main()` is `UIApplicationMain`. **The configurator hand-off:**
  `UIApplicationMain` instantiates the delegate itself, so `run_app` stashes the `configure` function
  pointer in a file-scope `host_state` (a function-local-static struct) BEFORE calling `UIApplicationMain`,
  and the delegate reads it from `didFinishLaunching` (the apple lane uses the same struct for symmetry).
  Boot wrapped in the gallery's try/@catch (C++ throw → NO, NSException → NO). Verified on the sim
  (UDID `C4926671…`): the "Hello, MAUI C++!" label renders at the safe-area top-leading (screenshot).
- **CMake**: the `maui_hosting` `host_run` generator expression gains the `apple`→`.mm` and `ios`→`.mm`
  lanes (ARC-compiled), beside the existing headless `.cpp` — exactly one `run_app` links per backend.
  `maui_hosting` now lints both languages (`OBJCXX_CLANG_TIDY`). `maui_hello_world` builds on all three
  backends from the **unchanged** pure-C++ source: a plain macOS executable on apple, a minimal `.app`
  bundle (reusing `ios_app_sample_info.plist.in`, id `dev.maui-cpp.hello-world`) on ios, the console binary
  + boot ctest on headless.

Gate: `gate.sh --fast` (headless 2516/2516 · tidy 0 findings · apple build) **PASS**; ios `maui_hello_world`
builds + installs + renders on the simulator. Stage-2 delta vs the headless lane: a native run loop
(`[NSApp run]` / `UIApplicationMain`) instead of boot+return, and safe-area-derived bounds instead of the
fixed default. **Deferred to later stages (untouched here):** re-driving `drive_layout` on native resize
(no resize hook yet — first layout only), windows/android `run_app`, and the `examples/` packaging tree
(Stages 3–5; the existing `src/samples` gallery `.mm` stays until Stage 5 retires it).

## Cross-platform entry point — Stage 1 (headless) — ✅ DONE (2026-06-24)

A framework user can now write a PURE C++ `main.cpp` (no Obj-C, no `.mm`, no platform headers) that looks
like a MAUI `MauiProgram`: define an `application` subclass + a `use_shared_maui_app` configurator,
`#include "maui/maui_main.hpp"`, and get a working `main()`. Stage 1 targets the **headless** backend and is
proven end-to-end. New/changed:
- **`include/maui/hosting/host_run.hpp`** — the PURE-C++ entry seam `run_app(argc, argv, configure)` each
  backend implements (`app_configurator = maui_app_builder (*)(maui_app_builder)`). Names no platform type.
- **`include/maui/maui_main.hpp`** — the convenience header: includes host_run + maui_app_builder, declares
  the USER-defined `use_shared_maui_app`, and defines `main()` → `run_app(..., &use_shared_maui_app)`.
  Include in exactly ONE TU (it defines main(), like CATCH_CONFIG_MAIN).
- **`include/maui/hosting/app_host.hpp` + `src/hosting/app_host.cpp`** — the GENERIC recursive mount + layout
  driver: `mount_tree` (depth-first post-order: attach a handler to every element via its runtime
  `handler_type_tag`, then re-host its children via `mount_into_handler`), `mount_window` (mount the page
  tree → attach window handler → open_window), `drive_layout` (one measure+arrange over the content bounds).
  NO per-control special-casing — generalizes the per-page `attach_handlers`/`gallery_rehost_*` pattern.
- **`src/platform/headless/host_run.cpp`** — the headless `run_app` body (build → get the app's window via
  `create_window()` → `mount_window` → `drive_layout` at 402×874 → return 0). Headless-keyed in CMake; the
  apple/ios/windows/android `run_app` (Stage 2) reuses `mount_window`/`drive_layout` + adds the native run
  loop and safe-area-derived bounds.
- **Minimal generic-mount accessors on `controls::element`** (the only framework edit the generic traversal
  needed): public `visit_logical_children` (the public window onto the already-overridden protected
  `for_each_logical_child`), virtual `handler_type_tag()` (the CONCRETE control's handler-registry key for
  runtime resolution — defaults to the existing `style_target_type_`, which every registered control already
  sets, + a `set_handler_type_tag<T>()` for `collection_view`, the lone control with a handler but no implicit
  style), and virtual `mount_into_handler()` (re-fires the container's own host command). Overridden on
  `border` / `content_page` / `templated_view`(→content_view) / `scroll_view` / `refresh_view` / `swipe_view`
  / `layout` to re-host their children.
- **`samples/hello_world/main.cpp`** — the pure-C++ proof program (the target UX verbatim) + a headless
  `maui_hello_world` target.
- **Tests:** `tests/hosting/app_host_tests.cpp` (6 cases) — handlers attach across the tree, the label's text
  reaches its headless handler mirror, `drive_layout` sizes the page, a DEEP nested tree (page→scroll→stack→
  {label, border→label}) mounts every node with the deepest leaf's text pushed, and `run_app` boots+returns 0;
  plus a `maui_hello_world_boots` ctest booting the proof binary end-to-end.

Gate: **headless 3081/3081** (was 3031; +6 new + existing). `gate.sh --fast` (headless+tidy+apple): see the
commit. **Stage 2 deltas the headless run_app leaves for apple/ios:** a native run loop (UIApplicationMain
et al.) instead of boot+settle-then-return; safe-area-derived layout bounds instead of the fixed 402×874; and
re-driving `drive_layout` on every native resize. The generic `mount_tree`/`mount_window`/`drive_layout` are
backend-agnostic and reused as-is.

## LayoutOptions gap — ✅ DONE (2026-06-19)

`View.HorizontalOptions` / `VerticalOptions` are now **settable + honored**. Previously
`view<>::horizontal_layout_alignment()` / `vertical_layout_alignment()` were hardcoded `return fill`
with no setter/storage, so every view always reported Fill (the contract getters + the
`layout_alignment` enum already existed). Fix (storage + arrange):
- **Storage/surface** — two shared NON-template bindable descriptors (`horizontal_layout_alignment_property()`
  / `vertical_layout_alignment_property()`, default `fill`) + `set_horizontal_layout_alignment` /
  `set_vertical_layout_alignment` on `view<>`; the getters now return the stored value. Bindable, so
  binding/styles/setters apply (C# `View.HorizontalOptionsProperty`/`VerticalOptionsProperty`, default
  `LayoutOptions.Fill`; the legacy StackLayout-only `Expands` bit is **not** in the IView contract, so
  the port stores the resolved `Primitives.LayoutAlignment` directly — `HorizontalOptions.ToCore()`).
- **Arrange honors it** — `view<>::arrange` now resolves the frame via a new `compute_frame` helper (a
  faithful port of `LayoutExtensions.ComputeFrame` + `AlignHorizontal` / `AlignVertical`), mirroring C#
  `VisualElement.ArrangeOverride` (`Frame = ComputeFrame(bounds); PlatformArrange(Frame)`). The layout
  managers were **already correct** (they pass the full cell/slot bounds to `child.arrange`, exactly as
  C# does — alignment is applied by the child's own arrange, not the manager). `layout::arrange` uses
  the same helper so a nested Start/Center/End layout is positioned within its parent cell.
- **Tests** — `tests/layouts/layout_alignment_tests.cpp` (18 cases: the `LayoutExtensionTests`
  Start/Center/End/Fill + offset + explicit-width/max-overrides-fill-from-center oracle, plus grid-cell
  + default-fill integration). `size_request.frame_is_independent_of_request` updated to the true
  ComputeFrame result (explicit-width child is sized to the request and centered, not stretched to raw
  bounds — the old assertion encoded the pre-ComputeFrame passthrough).

Gate: **headless 3031 / AppKit 2477 / iOS-on-sim 2248 — 0 failures; clang-tidy 0 (fresh build).**

## Audit-gap closure batch — ✅ DONE (2026-06-19, gate `b8ff9b9919`)

The ~9 genuine gaps the 2026-06-18 re-audit confirmed (the tail recorded in the section below) are **all
closed** — 8 units, agent-per-gap via worktree `code-changes` agents + coordinator cherry-pick/gate.
**Final gate: headless 3012 / AppKit 2457 / iOS-on-sim 2228 / ASan 3012 / TSan 3012 / clang-tidy 0
(clean build).** Units:
- **U-SA** `SafeAreaEdges` (ISafeAreaElement) on scroll_view / border(→frame) / content_view (mirrors content_page).
- **U-SL** generic `maui::controls::stack_layout` + `stack_orientation` enum + `stack_layout_manager` (+AndExpand), registered in the handler table + XAML types.
- **U-IB** `image_button` `IsAnimationPlaying` re-push after async load (mirrors image_handler).
- **U-TN** Setter/Trigger `TargetName` resolution via element-side NameScope (`element::find_by_name`/`find_target_by_name`; `name_scope` made header-only to avoid a controls→xaml cycle).
- **U-AFS** iOS Entry `AdjustsFontSizeToFitWidth` mapper → `UITextField.adjustsFontSizeToFitWidth` (iOS-only, faithful).
- **U-USE** `Binding.UpdateSourceEventName` over the reflection-free named-event seam (element targets; C#'s reflective native-view case has no analog — documented deviation).
- **U-IP** `image_paint` + `i_pattern`/`pattern_paint`/`picture_pattern` abstractions + CoreGraphics `fill_with_image`/`fill_with_pattern` (shared apple+ios canvas; the drawing-canvas path only — the view-background ImageBrush path stays faithfully deferred on iOS).
- **U-CMD** `i_command` primitive + `command` + Command/CommandParameter on Tap/Pointer/Drag/Drop recognizers (Tap/Pointer gate on CanExecute, Drag/Drop don't — per C#; `Command<T>`/WeakEventManager omitted per the port's no-RTTI/RAII doctrine — documented).

Process notes: **U-SL/U-IB were prior-session uncommitted WIP** found in the checkout (salvaged + verified,
not re-implemented). A **mid-batch macOS TCC revocation on `~/Documents`** blocked all repo I/O for a
stretch — U-TN/U-AFS/U-USE finished + self-verified but couldn't commit until access was restored, then
were committed-in-worktree + cherry-picked. Per-integration clean clang-tidy again caught what the agents'
warm-build tidy missed (6 wave-2 nits, hand-fixed). **This takes the audited in-scope surface from ~98%
to effectively complete** (remaining: the documented faithful deviations + the deliberately string-only
`Picker.ItemDisplayBinding`, left out by design).

## Final-sweep audit + demo gallery — ✅ DONE (2026-06-18, gate `9a0600ba0d`)

A user-requested "final sweep" to confirm (1) the abstract widget set, (2) macOS, (3) iOS, (4) all
examples, (5) examples run + documented. Two workstreams:

**(A) Fresh deep re-audit (items 1–3).** A 16-subsystem × 3-axis (abstraction / macOS / iOS) read-only
multi-agent audit vs the C# `src/`, with an independent skeptical verifier per claimed gap. Verdict: the
in-scope surface is **~98%** — substantially complete, **not literally 100%**. The verifier correctly
*rejected* false positives (CoerceValue, geolocation-macOS, StringFormat, PropertyPath indexers, iOS
pattern/image fills, Shell FlyoutWidth, Label.Background-macOS all verified present/faithful). Confirmed
genuine, still-open gaps (mostly small; **all closed in the 2026-06-19 gap-closure batch — see the
section above**):
`SafeAreaEdges` on scroll_view/border/frame/content_view (present only on content_page); generic
`StackLayout` control + `StackOrientation` (+ `AndExpandLayoutManager`); iOS `AdjustsFontSizeToFitWidth`
(knob present, mapper absent); `Binding.UpdateSourceEventName`; Command/CommandParameter on gesture
recognizers (no developer-facing `ICommand`); Trigger/Setter `TargetName` NameScope resolution;
`ImagePaint`/`IPattern` graphics abstractions + macOS `FillWithImage` (**✅ CLOSED — unit U-IP, see the
graphics table below**); `ImageButton.IsAnimationPlaying`
re-push; `Picker.ItemDisplayBinding` (deliberately string-only). The audit's coverage critic produced
**false negatives** (claimed MenuBar/MenuFlyout/ContentView/Application handlers "missing" — they exist
under the port's snake_case structure).

**(B) Runnable demo gallery + docs (items 4–5).** The 11 `src/samples/pages/*.hpp` demo pages were
**orphaned** (no host ran them). Built `gallery_host.hpp` + `macos_gallery.mm` + `ios_gallery.mm`
(page selected by `MAUI_SAMPLE_PAGE`), gave each page an `attach_handlers`, and captured every page on
macOS + iOS with screenshots + GIF recordings + an index & per-example README under
[`port/cpp/docs/examples/`](cpp/docs/examples/README.md) (macOS/iOS = demos; Windows/Linux/Android =
TODO; a full 1:1 port of MAUI's ~282 sample pages is the roadmap). iOS render: 7/11 full,
web_view/swipe_refresh/chrome partial (documented reasons), tabbed_flyout blank (gallery-host limitation,
not a control bug).

**🔴 Framework bug found + fixed (`44bf221e61`).** Making the gallery runnable exposed that
`add_maui_controls_handlers` (the table `maui_app_builder` seeds its registry from) registered only **21
of the 49** built-in control→handler pairs — the other **28** (slider, stepper, switch, check_box,
activity_indicator, the 3 pickers, editor, search_bar, radio_button, image_button, scroll_view, border,
frame, content_view, refresh_view, swipe_view/swipe_item_view, web_view, hybrid_web_view, indicator_view,
carousel_view, table_view, shell, absolute/flex layouts) **threw "no handler registered" when resolved
through the normal hosting path**. The controls + handlers all exist and pass their seam tests (which
attach handlers directly), but were unreachable via `maui_app`. Completed the table to full 49-pair
registrar parity.

**Gate (unchanged by the fixes): headless 2944 / AppKit 2400 / iOS-on-simulator 2170 — 0 failures.**

## iOS/macOS-completion batch — ✅ DONE (2026-06-15, pushed `b27ca67e19`)

Closed the cross-platform + iOS/macOS gaps a fresh disk-verified audit surfaced (8 units). **Gate (all
backends): headless 2867 / AppKit 2366 / iOS-on-simulator 2068 / ASan 2867 / TSan 2867 / clang-tidy 0
(clean build).** Each unit was adversarially reviewed (multi-agent) and its findings fixed before push.

- **X1 — Brush family** (`W7-49`): Brush/SolidColor/Gradient/Linear/Radial/Image brushes + GradientStop(Collection) over `graphics::paint`; Background/Fill/Stroke bridge; XAML `convert_brush`. *(row below)*
- **X2 — Shell `SearchHandler`** (`W7-50`) + **X3 — Shell appearance/styling** (`W7-51`). *(rows below)*
- **X4 — XAML markup-extension + converter tail** (`W7-52`): the `{x:Array}`/`{x:Reference}`/`{FontImage}`/standalone `{TemplateBinding}`/`{RelativeSource}`/`{DataTemplate}` extensions + the `Keyboard`/Flex-enums/`SafeAreaEdges` converters + `ISafeAreaView`/`ICrossPlatformLayout` (line ~365). Headless + compile-through.
- **Y1 — Keyboard + focus subsystem** (`W8-53`) and **Y2 — label/editor text-layout polish** (`W8-54`). *(rows below)*
- **Y3 — image production tail + FontManager** (`W8-55`, folded into the **Image** row).
- **Y4 — control-polish sweep** (`W8-56`): slider `ThumbImageSource`+tint+UpdateOnTap+iOS-26 layout, switch `NeedsContainer` (→ `native_view()` returns the container, the C# `ToPlatform()` seam), progress_bar FlowDirection, graphics_view touch plumbing (apple), table_view rich cells (switch/entry/image) + section group rows — closing the documented apple+ios `// deferred` markers on those controls.

**Android backend — ⏸ DEFERRED (explicit).** This batch was iOS/macOS-only. Android still has only the
M6-equivalent core handlers (button + navigation + window + the W4-34d/e view-mapper/visual/semantics
ops). DEFERRED for a later batch: the in-flight trio **W4-34a/b/c** (label+entry / image / layouts+page),
the broader android control fan-out (~25 more handlers), all **43 Essentials JNI** (currently headless
fakes on android), the **AndroidSpecific** platform-config wiring (stored-inert), and the semantics
**Hint** delegate. Windows/Tizen/GTK + BlazorWebView + Map + source-gen compiled bindings remain
out-of-scope.

## iOS/macOS gap-closure batch — ✅ DONE (2026-06-16, gate tip `393d2ef803`)

The 34 genuine iOS/macOS gaps from the 40-agent audit (`eee3c14e7a`) are **all closed** — implemented
agent-per-gap across **22 collision-aware units** in 5 throttled waves (each agent verified only its own
targeted tests; the coordinator ran the full multi-preset gate per wave + once at the end). **Final gate:
headless 2944 / AppKit 2400 / iOS-on-simulator 2170 / ASan 2944 / TSan 2944 / clang-tidy 0 (clean build).**

An independent **adversarial multi-agent review** of all 22 units (separate from each agent's own
code-review) surfaced **10 confirmed fidelity bugs** (13 candidates, 3 refuted) — all fixed + re-gated.
The notable one: `ContentPage.HideSoftInputOnTapped` never armed in production because InputView focus
changes weren't routed to the soft-input manager (only the tests called it manually) — now `set_is_focused`
fires an `is_focused` mapper (C# `InputView.MapIsFocused`) that reaches the page's manager.

| Unit | Gaps closed (C# oracle) |
|------|-------------------------|
| U01 | Picker/DatePicker/TimePicker `IsOpen`+`Opened`/`Closed`+focus dance |
| U02 | iOS base `FlowDirection`→`SemanticContentAttribute` native push (+ stepper) |
| U03 | `KeyboardAutoManager` scroll-avoidance + next-responder walk |
| U04 | `ContentPage.HideSoftInputOnTapped` iOS mapper + manager + focus routing |
| U05 | Editor `VerticalTextAlignment` native ContentOffset centering |
| U06 | Label `LineBreakMode` + `MaxLines` (iOS UILabel + AppKit cell) |
| U07 | Button `ImageSource` + `ContentLayout` + ImageButtonMapper (+ IsLoading) |
| U08 | CarouselView `Position`/`CurrentItem` writeback + IsSwipe/Peek/Bounce |
| U09 | SwipeItemMenuItem/SwipeItemView handlers + interactive drag-to-reveal pan |
| U10 | Shell FlyoutHeader/Footer(+templates) + FlyoutWidth/HeaderBehavior + search-results stub |
| U11 | WebView `UserAgent` bidirectional + `WKUIDelegate` JS alert/confirm/prompt |
| U12 | `ICanvas.DrawImage` + `i_graphics_image` (Microsoft.Maui.Graphics.IImage) |
| U13 | FilePicker essential (interface + iOS UIDocumentPicker + macOS NSOpenPanel seams) |
| U14 | SecureStorage `DefaultAccessible` + `SetAsync(key,value,accessible)` |
| U15 | macOS `Permissions.LocationWhenInUse` real CoreLocation status |
| U16 | `Compass.ShouldDisplayHeadingCalibration` / IPlatformCompass |
| U17 | Launcher `WebUtils.GetNativeUrl` AbsoluteUri (percent-encode) fallback |
| U18 | TabbedPage `BarBackground` (Brush/gradient tab bars via CALayer) |
| U19 | Switch foreground / trait-change color re-application observers |
| U20 | per-control `SafeAreaEdges` bindable + per-edge native inset application |
| U21 | Gesture `UIGestureRecognizerDelegate` (ShouldReceiveTouch / ShouldRecognizeSimultaneously) |
| U22 | SearchBar `OnMovedToWindow` cancel-color re-fire (+ cancel-button-lookup fix) |

**Documented within-unit deferrals** (faithful scope decisions — NOT gaps; the bulk of each unit landed):
- **U09 SwipeView**: Reveal drag-to-reveal landed; Drag-mode action-view translation + tap-outside-to-close
  + parent-UIScrollView coordination deferred.
- **U10 Shell**: header/footer/width + `FlyoutHeaderBehavior` enum/Default/Fixed + a search-results stub
  landed; `Scroll`/`CollapseOnScroll` scroll-offset tracking + the full ItemsSource-bound results renderer
  deferred. Footer reposition uses a KVO-on-content-bounds equivalent (the cross-platform `i_view` has no
  `MeasureInvalidated` signal yet — adding it would be a view-base contract change).
- **U07 Button**: `ContentLayout` is stored+pushed; the text+image composition awaits the container subsystem.
- **U21 Gesture**: Pan `ShouldRecognizeSimultaneously` is hardcoded false (the C# `Application`-config knob
  isn't wired yet).
- **U03 Keyboard**: the deprecated pre-iOS-13 `statusBarFrame` fallback is intentionally omitted (deployment floor is iOS 26).

**Tooling notes for a future cleanup (not gaps):**
- **2 `// NOLINTNEXTLINE` suppressions** were introduced — `ios_keyboard_auto_manager.mm` and the
  hide-soft-input cleanup `catch(...)` — both `bugprone-empty-catch` on a noexcept teardown swallow, matching
  the verbatim existing precedent at `path_builder.cpp:254` (the codebase carries ~7 such).
- **`.mm` clang-tidy coverage gap (pre-existing):** the `tidy` CMake preset is `MAUI_BACKEND=headless`, so it
  never lints Obj-C++ `.mm` files; native-`.mm` tidy cleanliness relies on per-agent linting + the `-Werror`
  native builds. A standalone clang-tidy over the `.mm` layer shows latent style findings across **every**
  handler (e.g. `create_platform_view can be made static`) — pre-existing debt, not batch-specific. A
  dedicated `.mm`-tidy preset + cleanup is a worthwhile separate task.

These were DISTINCT from the FAITHFUL "not supported" set (macOS sensors/screenshot/app_actions
`feature_not_supported`, AppKit no-soft-keyboard / hand-drawn page-control dots / no nav-bar tint, etc.)
which remains correctly absent (completing it would diverge from MAUI).

## Build & test — run from `port/cpp/`

**Performance model (read this before "the gate is slow").** "ctest for all presets" is not one build —
it is ~6 INDEPENDENT builds of the same ~860 TUs, each in its own `build/<preset>/` tree that shares no
objects (headless / tidy / asan-ubsan / tsan / apple / ios), two of them deliberately expensive (`tidy`
runs clang-tidy on every TU; the sanitizer lanes recompile instrumented). Three things keep it sane —
all wired in:
- **ccache** (`brew install ccache`; auto-detected in `CMakeLists.txt`, disable with `-DMAUI_USE_CCACHE=OFF`)
  makes the 2nd..Nth build of an unchanged TU a cache hit — across runs (clean rebuilds, branch switches,
  worktree-agent builds) and within a gate (`headless`→`tidy` share object cache; only the clang-tidy
  pass re-runs). This is the single biggest win for the full gate.
- **Incremental by default.** Ninja recompiles only changed TUs — never wipe a `build/<preset>` dir to
  "be safe". Only clean-build for a true from-scratch gate.
- **`ctest -j`** — huge for the device lanes (ios on a simulator: ~8 min serial → ~2 min `-j 8`).

**Two scripts encode the tiers** (don't run all presets for routine iteration):
```bash
tools/dev.sh [regex]        # INNER LOOP (seconds): incremental headless build + ctest -R <regex> -j
tools/dev.sh -p apple Btn   #   …on another backend; -t <target> to build just one test target
tools/gate.sh               # FULL GATE: headless tidy asan-ubsan tsan apple ios — configure+build+ctest -j
tools/gate.sh --fast        #   quick pre-commit subset (headless tidy apple); --clean for from-scratch
```

Raw commands (what the scripts run; `-j` = cores):
```bash
export VCPKG_ROOT="$HOME/vcpkg"          # registry clone; brew's vcpkg binary alone lacks the toolchain file
cmake --preset headless && cmake --build --preset headless
ctest --preset headless -j               # all ported tests (graphics + core + controls + xaml + hosting: 775 cases, green)
./build/headless/maui_graphics_benchmarks --benchmark_min_time=0.02s   # Google Benchmark (not a ctest test)
```

**macOS / AppKit backend** (real NSViews; Obj-C++ `.mm` + ARC):

```bash
cmake --preset apple && cmake --build --preset apple && ctest --preset apple -j   # 702 cases incl. real NSButton tap + NSWindow host + GCD dispatcher
./build/apple/maui_button_sample                                                # button sample window (Ctrl-C / close to quit) — builder boot (M-L)
./build/apple/maui_app_sample                                                   # full app→window→page→button sample — builder boot (M-L)
```

**iOS / UIKit backend** (M6 scaffold; real UIViews ON the iOS **simulator**; Obj-C++ `.mm` + ARC):

```bash
cmake --preset ios && cmake --build --preset ios   # cross-compile for the arm64 iphonesimulator (vcpkg overlay triplet arm64-ios-simulator)
ctest --preset ios -j 8                             # 549 cases run ON a booted simulator via tools/ios-sim-run.sh (-j 8 ≈ 2 min; serial ≈ 8 min — ALWAYS pass -j)
```

Needs Xcode with an iOS-simulator SDK + at least one available iPhone simulator (the runner boots the
first one; override with `MAUI_IOS_SIM_UDID=<udid>`). See the M6 row in the table below for the
toolchain decisions (deployment floor 26.0, the TCC staging detour, PRE_TEST gtest discovery).

**Android / NDK backend** (M-android scaffold; the pure-C++ HEADLESS partials cross-compiled for
arm64-v8a and run ON a headless emulator — the JNI per-control fan-out swaps them later):

```bash
cmake --preset android && cmake --build --preset android   # in-SDK NDK 27.2 LTS via vcpkg chainload (triplet arm64-android, ANDROID_STL=c++_static)
ctest --preset android -j 8                                # 1845 cases run ON the dedicated maui-test AVD via tools/android-emu-run.sh (-j 8 ≈ 45 s; incl. the app_process widget host suite)
```

Needs the command-line SDK at `/opt/homebrew/share/android-commandlinetools` (override:
`MAUI_ANDROID_SDK_ROOT`) with platform-tools, emulator, and the android-34 google_apis arm64-v8a
system image; the runner resolves every tool by ABSOLUTE path, creates + boots the dedicated
`maui-test` AVD on demand (override: `MAUI_ANDROID_AVD`), stages binaries keyed by mtime+size under
`/data/local/tmp/maui`, and leaves the emulator running. Portability notes: NDK r27 ships libc++ 18,
which lacks floating-point `std::from_chars` — the four FP parse sites route through
`maui/detail/charconv_compat.hpp` (a strtod/strtof fallback reproducing `chars_format::general`
semantics there; a one-line `std::from_chars` forward on every other backend); Android has no `/tmp`,
so the runner exports `TMPDIR`/`HOME` into the staging dir for `std::filesystem::temp_directory_path`.
Known cosmetic noise: NDK clang 18 emits `-Wmissing-field-initializers` on 6 intentional designated
initializers (`view.cpp` + bindable/binding tests) — clang 19+ removed that false positive, so the
warnings vanish when the NDK rolls forward; left un-touched deliberately.

**Resume:** continue to the next ⬜ milestone below, following `CLAUDE.md`. **M0–M5 are COMPLETE — including
the M5d backlog sweep AND the M5e tail batch** — on **both** the headless backend (**616 tests**) and the
**macOS AppKit backend** (**531 tests**, real `NSButton`/`NSTextField`/`NSWindow`), clang-tidy 0 +
ASan/UBSan + TSan clean. The virtual-view ⇄ handler ⇄ native seam (M1/M2), layout (M3), the v1 control set +
shared ViewMapper (M4), and binding/styles/lifecycle (M5: typed-accessor data binding,
styles/setters/triggers/VSM, Application/Window/lifecycle + typed inherited BindingContext) are all done.
**M5d** (7 parallel worktree agents + a coordinator) closed: per-corner clip radii + System.Numerics
interop, gradient paints (+ apple `CAGradientLayer`), AppKit text polish + full entry editing, image
disk-cache/CacheValidity + `font_image_source` + a typed DI service-provider, the native NSWindow
`window_handler` + window/app extras (geometry/themes/resume-sleep) + `maui_app_sample`, navigation chrome +
animations + modal stack, the styles/resources subsystem (ResourceDictionary + implicit styles +
DynamicResource + advanced triggers + the VSM #18103/#34363 nuance), and Semantics/InputTransparent
ViewMapper props + the VSM `is_enabled`→Disabled auto-drive. **M5e** (4 parallel worktree agents) closed the
remaining tail: the layout-engine refinements (size requests on `view<>` honored by the managers, z-index
native subview ordering, ClipsToBounds), the NATIVE apple accessibility + hit-test push
(`apple_semantics_ops.hpp`), image production hardening (on-disk uri cache + CRC-64, a real async
`NSURLSession` fetch through the dispatcher, native GIF playback), and the `i_application` Core contract +
navigation bar styling (`bar_background_color`/`bar_text_color`/`title_view`) + the
`pushed`/`popped`/`popped_to_root`/`navigating`/`navigated` events. What remains is intentionally-deferred
markup-era / out-of-scope work (see the Deferred backlog): Shell, XAML (M7), and small documented
deviations. **M6 (second platform, iOS) is UNDERWAY behind the same handlers: the scaffold is DONE** —
the `ios` preset cross-compiles the whole library + test tree for the arm64 iOS simulator
(`MAUI_BACKEND=ios`, vcpkg overlay triplet, `MAUI_PLATFORM_IOS`), `ctest --preset ios` runs all
**558** tests ON a booted simulator through `tools/ios-sim-run.sh`, and the button's UIKit partial
(`src/platform/ios/button_handler.mm`, ported 1:1 from `ButtonHandler.iOS.cs` + `ButtonExtensions`)
proves the seam both directions on a real `UIButton` (15 on-simulator seam cases). **M6 fan-out —
image sources (DONE):** `src/platform/ios/image_source_services.mm` is the UIImage twin of the apple
services; every decode runs through ImageIO/`CGImageSource` (UIKit has no auto-animating multi-frame
image): file loads probe the `@2x`/`@3x` sibling for the screen scale (`GetScaledFile` →
`ios_image_ops.hpp`, pure + unit-tested) and decode at the loaded scale; stream/uri bytes decode via
`decode_image_bytes` (single frame keeps the EXIF orientation; an animated GIF expands per the
delay-GCD into `animatedImageWithImages:duration:` — the `ImageAnimationHelper.cs` port); the font
glyph rasterizes through `UIGraphicsImageRenderer` (resolution-dependent, `AlwaysOriginal`); the
SHARED loader keeps the async/cache plumbing (disk round-trip proven on-simulator). The headless
decode-mirror test files (`image_tests.cpp`, `image_source_tests.cpp`, `font_image_source_tests.cpp`)
are swapped for `tests/controls/image_sources_ios_tests.mm` (13 cases, real PNG/GIF fixtures generated
in-process); the ios `image_handler.mm` (UIImageView + start/stopAnimating + the NSURLSession
`configure_loader` wiring) stays with its own fan-out unit — the handoff is documented in the services
.mm header. Every other
control still compiles its HEADLESS partial on ios — **next: the rest of the M6 per-control fan-out**, swapping
each headless partial (+ its tests) for the UIKit twin behind the unchanged cross-platform handlers
(see the M6 row in the table for what each unit inherits / still owes).
The `PROFILE.md §11` decisions are **locked** (view owns handler; `property<T>` member object;
per-type `concept`-vs-`i_*` rule; headers not modules). M1 build order — all done: `event`,
`dispatcher`, `setter_specificity`(+list), `bindable_property<T>` / `bindable_object` / `property<T>`
(fully typed, no `std::any`), primitives (`thickness`/`font`/enums), contracts (`i_element` /
`i_transform` / `i_view` / `i_text_style` / `i_text`), and the handler seam (`type_tag`,
`property_mapper`/`command_mapper`, `i_element_handler`/`i_view_handler`, CRTP `view_handler`,
`handler_registry`/`service_registry`). **M2a (done):** the Controls layer's first slice — `i_button` /
`i_padding` / `i_button_stroke` / `i_text_button` contracts, the cross-platform `button_handler`
(+ headless platform partial), a minimal CRTP `view<ViewInterface>` base, and the `button` control —
proving the seam both directions on headless (Text virtual→native via the mapper; native tap→`clicked`
via `send_*`). **M2 API decision (locked):** virtual-view interfaces stay bare-noun getters and concrete
controls expose **method accessors** (`button.text()`/`set_text()`), each backed by a private
`property<T>` value engine (a property change → `view::on_property_changed` → `handler->update_value`).
**M2b (done):** the macOS AppKit backend — `src/platform/apple/button_handler.mm` (Obj-C++/ARC) drives a
real `NSButton` (Text→`title`; a target-action trampoline routes the click to `send_clicked()`), the
`button_platform` gained a backend-defined destructor (RAII for the retained NSButton), CMake grew an
`apple` preset (OBJCXX + `-fobjc-arc` + `-framework Cocoa`), the seam is verified by `button_apple_tests.mm`
(real `NSButton performClick:` → `clicked`), and `maui_button_sample` is a runnable macOS window hosting
the control. **M2 backfill (done):** opt-in self-registration — `default_handler_registry()`,
`handler_registrar<view,handler>`, and the `MAUI_REGISTER_HANDLER` sugar (button self-registers in
button.cpp); and the **full button surface** — text_color / font / character_spacing / padding / stroke
are now bindable + mapped (headless mirrors every property; AppKit maps font / text_color (contentTintColor)
/ stroke (layer border), with character_spacing + padding documented as AppKit TODOs needing an attributed
title / custom cell). **Deferred to M3/M4:** the shared **ViewMapper** for the generic IView properties
(needs a common platform-view base + the visual-element property set) and image source.
**M3a (done):** the layout foundation — `dimension`, `i_container` / `i_layout` / `i_stack_layout`,
`i_layout_manager` contracts — and a new `maui_layouts` library: `layout_manager` (ResolveConstraints)
+ `stack_layout_manager` (MeasureSpacing) bases + `vertical_stack_layout_manager` /
`horizontal_stack_layout_manager`. Pure cross-platform measure/arrange (no native); 28 GTest cases
ported from C#'s Stack*LayoutManagerTests (spacing / padding / min-max / collapsed-vs-hidden / fill).
**M3b (done): Grid** — `grid_length` / `grid_unit_type`, the row/column definition contracts, `i_grid_layout`
(definitions + spacing + Row/Column/Span attached-property queries), and `grid_layout_manager`: Absolute /
Auto / Star sizing, row+column spans, spacing, padding, min/max, two-pass measure, and arrange-time star
expansion (pimpl grid_structure cached between measure/arrange, exactly as C#'s GridStructure). 19 GTest
cases ported from GridLayoutManagerTests (a representative subset of a very large suite).
**M4a (done): `label`** — the second concrete control, proving the handler recipe generalizes
(display-only: properties flow virtual→native, no events). New contracts `text_alignment` /
`text_decorations` / `i_text_alignment` / `i_label`; the `label` control + `label_handler` (single mapper
keyed on i_label — no chaining) with a headless mirror and a real macOS NSTextField (label style). The
Apple `.mm`s now share `apple_conversions.hpp` (maui color/font → NSColor/NSFont). Also disabled clang-tidy
`portability-template-virtual-member-function` — it flagged the deliberate inline virtual overrides on the
`view<ViewInterface>` template base (latent since M2a; the vtable odr-uses them all on our toolchain).
**M4b — shared ViewMapper (Unit 2, done):** the generic-IView property mapper every handler chains.
New `view_platform_base` (a non-template base for the platform-view structs: headless mirrors
`hidden`/`alpha`/`enabled`/`automation_id` + virtual `update_visibility`/`update_opacity`/
`update_is_enabled`/`update_automation_id`, backend-overridable) and `view_mapper()` — a
`property_mapper<i_view, i_view_handler>` with keys `"visibility"`/`"opacity"`/`"is_enabled"`/
`"automation_id"`. `i_view_handler` gained `view_platform_base* platform_base()`; the CRTP `view_handler`
implements it NON-BREAKINGLY (`if constexpr (is_base_of<view_platform_base, Platform>)` → ptr else null,
so handlers whose platform view doesn't derive the base still compile and the maps no-op). The four
`view<ViewInterface>` properties are now BINDABLE (private `property<T>` bound to NON-template free-function
descriptors `is_enabled_property()`/`opacity_property()`/`visibility_property()`/`automation_id_property()`
in the new `src/controls/view.cpp` — one descriptor per property across all instantiations; opacity clamps
to [0,1] like VisualElement). `button_platform`/`label_platform` now derive `view_platform_base` and the
handlers chain `view_mapper()` (button: `set_chained({&text_mapper(), &view_mapper()})` so generic-IView
keys run first; label: the `property_mapper(view_mapper(), {...})` ctor). AppKit `.mm`s override the four
`update_*` on the real `NSButton`/`NSTextField` (`hidden`/`alphaValue`/`setEnabled:`/
`accessibilityIdentifier`). 9 headless + 2 apple GTest cases (`view_mapper_tests.cpp` /
`view_mapper_apple_tests.mm`). **Deferred (note for coordinator):** the wider ViewMapper set —
Width/Height/Background/transforms/Clip/Shadow/FlowDirection/Semantics/InputTransparent — and C#'s
`IsConnectingHandler()` default-skip optimization. The `MAUI_PLATFORM_APPLE` compile definition (PUBLIC,
apple build only) guards the platform structs' `update_*` override declarations so the headless build keeps
the base mirrors (one backend per build → no ODR mismatch). **Coordinator: to retrofit Units 1 & 3's new
handlers, chain `maui::core::view_mapper()` into each handler's `mapper()` (single-sub-mapper handlers use
the `property_mapper(view_mapper(), {entries})` ctor like `label_handler`; multi-sub-mapper handlers use
`set_chained({&other_mapper(), &view_mapper()})` like `button_handler`), and make each platform-view struct
derive `maui::core::view_platform_base`.**
**M4b — layout controls (Unit 1, done):** `i_layout_handler` (the add / remove / clear / insert / update /
update_z_index child seam), a templated `layout<LayoutInterface>` control base (owns the non-owning child
list + i_container / i_padding, lazily builds its M3 `i_layout_manager` from `create_layout_manager()`, and
overrides measure/arrange to delegate to the manager — a layout computes its own geometry — while arrange
also sizes the host panel), and `vertical_stack_layout` / `horizontal_stack_layout` (+ bindable Spacing).
The `layout_handler` hosts a native container (a plain `NSView` panel on macOS; a headless child-count
mirror) and syncs subviews via the command seam. `view_handler` now derives `i_view_handler` *virtually*
(so `layout_handler` is both a `view_handler` and an `i_layout_handler` with a single `i_view_handler`
subobject), and `i_view_handler` gained `native_view()` (the real native view a panel hosts). `maui_layouts`
is linked into `maui_controls`. The grid *control* is deferred (the grid *manager* already exists).
**M4b — entry + minimal image (Unit 3, done):** `entry` — the first inbound-text + first editable native
control: `i_text_input` / `i_entry`, bindable text / placeholder / is_password / is_read_only / max_length
+ appearance / alignment, `completed` + `text_changed(old, new)` events, over an editable `NSTextField`
(an `NSSecureTextField` cell swap for password) with an `NSTextFieldDelegate` trampoline
(controlTextDidChange → send_text_changed, controlTextDidEndEditing → send_completed). `image` (minimal) —
an `aspect` enum + `i_image` (aspect only) mapped to `NSImageView.imageScaling`. The full async image
*source* subsystem (loaders / caching) is deferred.
**M4b — coordinator integration (done):** the three units merged onto cpp-port-kit; Units 1 & 3's platform
structs retrofitted onto `view_platform_base` + chained `view_mapper()` (layout overrides 3 `update_*` — a
plain NSView panel has no enabled state, so is_enabled keeps the base mirror; entry / image, being
NSControls, override all 4). Full gate green: **282 headless / 246 apple** GTest, clang-tidy 0 findings
(including the Obj-C++ `.mm` files), ASan/UBSan + TSan clean.
**M4c — ViewMapper widening: transforms + FlowDirection (Unit A, done; headless-complete):** the shared
`view_mapper()` grew the render transform and FlowDirection. `view_platform_base` gained a POD
`transform_spec` (the ten ITransform scalars — translation_x/y, scale, scale_x/y, rotation, rotation_x/y,
anchor_x/y; identity defaults) + a `transform` mirror and a `flow_direction` mirror, plus backend-
overridable `update_transform(const transform_spec&)` / `update_flow_direction(flow_direction)` (headless
bodies store the mirrors). `view_mapper()` added ten transform keys — all routing to ONE shared
`map_transform` that reads all ten scalars off the i_view and pushes the whole `transform_spec` (so any
single change rebuilds the entire transform, matching TransformationExtensions.UpdateTransformation) —
plus `"flow_direction"` → `map_flow_direction`. The `view<>` transform getters + `flow_direction()` are
now BINDABLE (private `property<double>` / `property<flow_direction>` over NEW non-template descriptor
free-fns in `view.cpp`: translation/rotation default 0, scale 1, anchor 0.5, flow_direction MatchParent)
with public setters (`set_translation_x` … `set_flow_direction`); property names match the mapper keys
exactly. Shared AppKit helpers in the new header `src/platform/apple/apple_view_ops.hpp`:
`maui::platform::apple::apply_transform(void* native, const maui::core::transform_spec&)` (faithful port
of TransformationExtensions — wantsLayer, anchorPoint, the CATransform3D build with the m34 perspective
for out-of-plane rotation; the anchor-offset term reads the NSView layer bounds, the AppKit analog of
view.Frame) and `apply_flow_direction(void* native, maui::core::flow_direction)`
(NSView.userInterfaceLayoutDirection; MatchParent → the app-wide default). These are provided for the
coordinator's per-control retrofit — NOT wired into any control here. CMake gained `-framework QuartzCore`
on the apple `maui_core` link (CATransform3D C symbols; Cocoa doesn't pull them). 5 new headless
(`view_mapper_tests.cpp`: identity defaults, single-setter-rebuilds-whole-spec, flow_direction, initial-
values-on-attach, label-generalization) + 5 new apple (`view_mapper_apple_tests.mm`: apply_transform
scale/anchor/perspective/identity + apply_flow_direction) GTest cases. **Out of scope (deferred):**
Background (needs paint), Shadow (needs i_shadow), Clip (needs i_shape), Width/Height (layout-driven),
Semantics, InputTransparent; and C#'s `IsConnectingHandler()` default-skip.
**M4c — grid control (Unit B, done):** `grid : layout<i_grid_layout>` over the existing `grid_layout_manager`
— `row_definition`/`column_definition` (concrete `i_grid_*_definition`, default Star), RowDefinitions /
ColumnDefinitions, bindable row/column spacing, and the Grid.Row/Column/RowSpan/ColumnSpan attached
properties (per-child `cell_info` in a pointer-keyed map; C# validation: row/col ≥ 0, span ≥ 1). Reuses
`layout_handler` (a grid is an `i_layout`; `MAUI_REGISTER_HANDLER(grid, layout_handler)`), so it inherits the
native panel + the chained `view_mapper()`. The grid *manager* already existed (M3b).
**M4c — minimal content_page (Unit C, done):** `i_content_view` (`content()` + i_padding) + `content_page :
view<i_content_view>` (a single non-owning Content + bindable Title/Padding; measure/arrange port C#'s
LayoutExtensions.MeasureContent/ArrangeContent — content sized within padding) + a `content_page_handler`
hosting the content's native view as a subview of a plain `NSView` (headless mirrors the hosted content).
Navigation / toolbar / safe-area / dialogs / templates deferred.
**M4c — image file source (Unit D, done):** `i_image_source` / `i_file_image_source` + a concrete
`file_image_source` + an `image_source::from_file(path)` factory; `i_image` / `image` gained a bindable
`source` (`property<shared_ptr<i_image_source>>`); `image_handler::map_source` loads the file SYNCHRONOUSLY
(`[[NSImage alloc] initWithContentsOfFile:]`; headless mirrors the path + a loaded flag). Async loading +
cancellation, the IImageSourceService / service-provider seam, uri / stream / font sources, and caching deferred.
**M4c — coordinator integration (done):** the four units merged onto cpp-port-kit; **Unit A's apple
transform / flow_direction wiring was retrofitted onto all six platform structs** (button / label / entry /
image / layout / content_page each override `update_transform` / `update_flow_direction` to call the
apple_view_ops helpers; grid reuses layout_handler). Full gate green: **326 headless / 264 apple** GTest,
clang-tidy 0 findings (including the Obj-C++ `.mm` files), ASan/UBSan + TSan clean.
**M4d — ViewMapper visuals: Background + Shadow + Clip (Unit V, done):** stood up the visual-layer value
types that `i_view` only forward-declared, and mapped them. New `maui::graphics::paint` + `solid_paint`
(one color), `maui::core::i_shadow` + `shadow` (radius / opacity / paint / offset), `maui::graphics::i_shape`
+ `rectangle` / `round_rectangle` / `ellipse` (`path_for_bounds` built from the existing `path_f`
append_* helpers). `view_platform_base` gained non-owning background / shadow / clip mirrors +
`update_background` / `update_shadow` / `update_clip`; `view_mapper()` added the three keys; `view<>` makes
`background()` / `shadow()` / `clip()` bindable (`property<shared_ptr<>>` + non-template descriptors). Shared
AppKit helpers in `src/platform/apple/apple_visual_ops.hpp`: `apply_background` (solid → `layer.backgroundColor`),
`apply_shadow` (`layer.shadow{Color,Opacity,Radius/2,Offset}`), `apply_clip` (`shape->path_for_bounds` → a
`path_f`→`CGPath` walk → a `CAShapeLayer` mask). Self-contained + headless-complete; the apple per-control
overrides are the coordinator's retrofit. 8 headless + 8 apple GTest cases. Gradients / per-corner clip /
Width/Height/Semantics/InputTransparent deferred.
**M4d — async image sources (Unit I, done):** the image-source subsystem grew uri + stream sources, an
async loader with cancellation, an in-memory cache, and the service-registry seam. New contracts
`i_uri_image_source` (uri + caching_enabled) / `i_stream_image_source` (a bytes provider —
`image_bytes get_bytes(token)`, simplifying C#'s `Task<Stream>`); concrete `uri_image_source` /
`stream_image_source` + `image_source::from_uri` / `from_stream` factories. The service seam:
`i_image_source_service` (async `load(source, token, on_result)`), an `image_source_service_registry`
(mirrors handler_registry — `register_service<Source, Service>()` keyed by the source interface, `resolve`
via a dynamic_cast probe walk) with a process-wide default + `register_default_image_source_services`, an
`image_source_result` (the native image handle + RAII disposer + a headless kind/detail mirror), and per-
source `file/uri/stream_image_source_service` (cross-platform decl + `{headless,apple}` partials sharing one
`decode_image_bytes` primitive). The `cancellation_token` is a `shared_ptr<atomic<bool>>` flag
(is_cancelled/cancel). The `image_source_loader` (ports ImageSourcePartLoader + ImageSourceServiceResultManager):
`begin_load()` disposes the prior result + cancels the prior token + mints a fresh one; `update_source()`
resolves the service / handles the uri cached fast-path (`std::map<uri,bytes>`, no expiry), loads, marshals
the apply onto the dispatcher (headless `manual_dispatcher` pumped in tests; apple inline when no dispatcher),
does the **source-identity recheck** (`!token.is_cancelled() && source == current_source_`), then
`complete_load`. `image_handler` now OWNS a loader (`source_loader()`): `map_source` keeps the SYNC file fast-
path and routes uri/stream through the loader async; the cross-platform routing lives once in
image_handler.cpp, the three per-backend primitives (`load_file_source_sync` / `apply_loaded_result` /
`clear_source_native`) touch the native NSImageView / headless mirror. `image_platform` gained a
`source_kind` mirror; still derives `view_platform_base`, mapper still chains `view_mapper()`. THREADING: the
loader's mutable state is touched only on the dispatcher (UI) thread; this cut's services load synchronously
(no worker), so the only cross-thread element is the atomic cancellation flag → TSan-clean. UAF-safe teardown
via a loader liveness weak_ptr the queued apply checks; ABA-safe because token cancellation gates the apply
independent of pointer identity. CMake: +4 cross-platform core `.cpp` (loader / registry / services-glue /
uri_bytes) + a per-backend `image_source_services.{cpp,mm}` (apple `.mm` `-fobjc-arc`). 6 new headless +
3 new apple GTest cases (extend image_tests.cpp / image_apple_tests.mm — no test-list churn). DEFERRED: disk
caching + CacheValidity expiry, font image sources, the full DI service-provider, resolution-dependent reload,
a production HTTP stack (apple uri does `file://` + a synchronous `http(s)` NSData fetch; headless never hits
the network — stream sources + `file://` only). **326 → 331 headless / 264 → 267 apple**, clang-tidy 0 findings
(incl. the `.mm` files + the gated tests, no suppressions).
**M4d — navigation pages (Unit N, done):** `navigation_page` — a push/pop stack of content_pages hosting
the current page's native view in a container (swapped on push/pop, no animation). `content_page` gained
the **page lifecycle** (Page.cs SendAppearing/SendDisappearing + `appearing`/`disappearing` events,
idempotent via a `has_appeared_` flag). New contracts: `navigation_request` (the new stack + animated) +
`i_stack_navigation` (request_navigation / navigation_finished, IStackNavigation.cs). `navigation_page :
view<i_view> + i_stack_navigation` owns a NON-owning `vector<content_page*>` with push/pop/pop_to_root +
current_page/root_page/navigation_stack; push/pop/pop_to_root mutate the stack then fire Disappearing(prev)
+ Appearing(new) — BOTH before notifying the handler — per NavigationPage.cs's MauiNavigationImpl ORDER
(C#'s async semaphore/TaskCompletionSource/overlap queue collapsed to a synchronous single transition).
The drive is a `"request_navigation"` COMMAND (payload = a `navigation_request`); `navigation_page_handler`
hosts a plain NSView container and swaps the current page's `native_view()` subview (headless mirrors the
hosted page). `navigation_page_platform` derives `view_platform_base`; the handler chains `view_mapper()`.
`navigation_page::set_handler` re-issues the request after wiring (C# OnHandlerChangedCore) so a freshly-
attached container hosts the current page; the navigation_page also fires the ROOT's initial
send_appearing() itself (standing in for the absent window lifecycle). **Deferred:** the navigation bar /
back button / title bar, push/pop animations, the modal stack, InsertPageBefore/RemovePage, and Shell.
15 headless + 5 apple GTest cases; self-registers (MAUI_REGISTER_HANDLER). *characterization.
**M4d — coordinator integration (done):** the three units merged onto cpp-port-kit; **Unit V's apple
Background / Shadow / Clip wiring was retrofitted onto all six platform structs** (button / label / entry /
image / layout / content_page each override `update_background` / `update_shadow` / `update_clip` → the
apple_visual_ops helpers; the clip mask is sized to the NSView bounds; grid reuses layout_handler). Full
gate green: **354 headless / 281 apple** GTest, clang-tidy 0 (incl. the `.mm` files + the gated tests via
the `tidy` preset), ASan/UBSan + TSan clean (Unit I's async loader runs single-threaded on the dispatcher,
so TSan stays clean). **M4 (control set v1) is COMPLETE.**
**Still next: M6** (second platform — iOS, behind the same handlers). Every M4/M5 deferral and tail has been
closed by the M5d sweep + the M5e tail batch (see the resume header + the Deferred backlog for the few
intentionally-deferred markup-era items).

## Tooling — format, lint, sanitizers (run from `port/cpp/`)

- **clang-format** (v22, `port/cpp/.clang-format`, Microsoft base / 120 col / Allman): applied
  automatically after every Write/Edit by the `.claude/settings.json` PostToolUse hook
  (`port/cpp/tools/format-hook.sh`, scoped to C/C++ under `port/cpp/`). Manual: `clang-format -i <file>`.
- **clang-tidy** (`port/cpp/.clang-tidy`, a curated ruleset; uses the llvm keg at
  `/opt/homebrew/opt/llvm/bin`). Off by default; lint the library sources with the `tidy` preset —
  advisory, findings don't fail the build: `cmake --preset tidy && cmake --build --preset tidy`.
  **The M0 sources (`color`, `path_builder`, `path_f`) are clang-tidy-clean — 0 findings.** When
  The M1 core sources **and headers** (events/dispatcher/bindable/primitives/contracts + the handler
  seam) are likewise clang-tidy-clean — verified with a full-header pass (`--header-filter`). When
  invoking the keg's clang-tidy directly it needs the AppleClang sysroot, e.g.
  `clang-tidy --extra-arg=-isysroot --extra-arg="$(xcrun --show-sdk-path)" -p build/headless <file>`.
- **Sanitizers** (`Sanitizers.md`) — target-level via the `maui_sanitizers` interface lib, one lane each:
  - `cmake --preset asan-ubsan && cmake --build --preset asan-ubsan && ctest --preset asan-ubsan` — ASan+UBSan (default checked build; **354/354 green**)
  - `cmake --preset tsan && cmake --build --preset tsan && ctest --preset tsan` — ThreadSanitizer (**354/354 green**)
  - `msan` preset is for **Linux/Clang CI only** — `-fsanitize=memory` is unsupported on AppleClang/macOS.

## Milestones (see `PROJECT.md §5`)

| Milestone | Description | Status |
|---|---|---|
| M0 | Graphics primitives compile + pass ported tests | ✅ |
| M1 | Core contracts + property/handler infra + dispatcher, unit-tested | ✅ |
| M2 | `button` end-to-end (headless → macOS), tap works in sample app | ✅ |
| M3 | Layout measure/arrange (`stack_layout`, `grid`) pass layout tests | ✅ |
| M4 | Control set v1 (label, entry, image, layouts, content page) on macOS | ✅ |
| M5 | `bindable_object`/`bindable_property`, binding, style, lifecycle | ✅ (M5a binding, M5b styles/triggers/VSM, M5c Application/Window/lifecycle + inherited BindingContext) |
| M5d | Deferred-backlog sweep (gradients, text, image+DI, NSWindow host, navigation chrome/modal, styles/resources, ViewMapper a11y props) | ✅ (7 parallel worktree agents + coordinator) |
| M5e | Tail batch (layout-engine size-requests/z-index/ClipsToBounds, native apple a11y + hit-test, image disk-cache + async HTTP + GIF, i_application + nav bar styling/events) | ✅ (4 parallel worktree agents) |
| M6 | Second platform (iOS) behind the same handlers | ✅ (scaffold + 4 fan-out units: ALL 9 controls have real UIKit partials; the full suite — 538 cases — runs ON the iOS simulator via the `ios` preset's `simctl` ctest runner; `ios_app_sample` UIApplicationMain target) |
| M7 | XAML and/or Essentials (as prioritized) | 🚧 (wave 1 ✅: `maui_xaml` registries + pugixml parser/node tree + converters + the `i_markup_extension` seam, 131 headless tests; wave 2 — visitors/loader (U3) + markup extensions (U5) — was cut off by a session limit MID-WORK: partial uncommitted code preserved in `.claude/worktrees/agent-a9c91013d1bcdc446` (U3: hydration_context/name_scope/visitors) + `agent-a4377f797717614d6` (U5: markup_extensions/runtime_environment/static_registry); U6 (end-to-end integration) not started) |
| M7 W1-17 | Essentials app-model + storage: preferences (NSUserDefaults/typed), secure_storage (Keychain), version_tracking (pure managed), app_info, file_system, main_thread (gcd facade), launcher, browser, app_actions (macOS = not supported per suffix oracle), permissions (desktop auto-grant + ios location status-query) - contracts + facades + per-backend partials + headless fakes + tests | ✅ headless/apple/ios |
| M7 W2-26 | Essentials networking + media: connectivity (NetworkAccess/ConnectionProfiles/ConnectivityChanged + dedupe — real on apple/ios via Network.framework NWPathMonitor, shared `connectivity_base`), text_to_speech (SpeakAsync/locales/SplitSpeak + range validation — NSSpeechSynthesizer on macOS, AVSpeechSynthesizer on ios, headless records utterances), screenshot (IsCaptureSupported/CaptureAsync→`screenshot_result` w/ width/height/stream — ios UIGraphicsImageRenderer, **macOS NOT supported** per suffix oracle, headless fake), media_picker (PickPhoto/Photos/Video/Videos/Capture* + IsCaptureSupported + `file_result` — **service seam** w/ documented sim deviation: apple `IsCaptureSupported=false`, ios camera-availability, picker UI not drivable headlessly; headless canned result), unit_converters (pure-managed temp/distance/angle/pressure/weight/angular-velocity + haversine, exhaustive value tables from the C# oracle) — contracts + facades + per-backend partials + headless fakes + tests | ✅ headless/apple (ios partials present; sim smoke) |
| M7 W1-11 | Chrome/menus: toolbar_item + page ToolbarItems/MenuBarItems + trackers + window toolbar/menu_bar/title_bar maps (NSToolbar / NSMenu main menu / titlebar accessory on AppKit; iOS = nav-bar buttons via the navigation chrome, menus/title bar stored-inert per C#), MenuBar/MenuFlyout family, attached ContextFlyout (NSView.menu / UIContextMenuInteraction) + ToolTipProperties (NSView.toolTip / iOS no-op) via the shared view_mapper, title_bar basics, chrome_page sample | ✅ headless/apple/ios |
| W2-20 swipe + refresh | `swipe_view` ⇐ SwipeView (Content + Left/Right/Top/BottomItems `swipe_items` collections w/ Mode Reveal/Execute + SwipeBehaviorOnInvoked; `swipe_item`/`swipe_item_view`; IsOpen + the Idle/Swiping/Open state machine w/ 15%/60% thresholds; SwipeStarted/Changing/Ended + Open/Close request events) and `refresh_view` ⇐ RefreshView (Content + IsRefreshing/IsRefreshEnabled coercion + RefreshColor + the command-as-callback refresh channel + Refreshing event). The swipe machine is the shared cross-platform `swipe_machine` (the MauiSwipeView.cs port) driven by SYNTHETIC offsets headless + a documented pan-driven-reveal cut on the native NSView/UIView hosts; the refresh host is a real UIRefreshControl on a UIScrollView content (iOS), a documented spinner-flag deviation on AppKit (no native pull). New core contracts (i_swipe_view/i_swipe_items/i_swipe_item[+menu_item/view]/i_refresh_view) + 5 enums + the request records; swipe_refresh demo page. Deviations: no ControlTemplate (templated_view incompatible w/ the i_swipe_view:i_content_view diamond), no parent-scroll auto-close (no ListView/CollectionView in the port), no ICommand (collapsed to callback+predicate per W1-11), headless/AppKit swipe threshold uses item-width/default (no native content frame). | ✅ headless/apple/ios |
| M-android | Third platform (Android) behind the same handlers | 🚧 (U7a scaffold: the `android` preset — in-SDK NDK 27.2 LTS, arm64-v8a, `c++_static`, vcpkg `arm64-android` — cross-compiles the FULL library + test tree with the headless partials, and `ctest --preset android` runs all **747** cases green ON the dedicated `maui-test` emulator via `tools/android-emu-run.sh` (adb twin of the simctl runner); next: the JNI seam headers + the widget test-host decision, then the per-control JNI fan-out) |
| M-android m3 | JNI seam + widget test host (W1-18-finish) | ✅ (the JNI utility layer `src/platform/android/jni/` — `jni_env`/`jni_ref`/`jni_cache`/`jni_string`, transcoders unit-tested on-emulator — and the PROVEN test-host lane: strategy (i) `app_process` + `ActivityThread.systemMain()` WON (no APK/gradle; the fallback (ii) runner-APK was not needed) — `tools/android-testhost-run.sh` dexes `testhost/Bootstrap.java`, which mints a themed Context and runs the gtest suite embedded in `libmaui_android_widget_tests.so`; real `android.widget.TextView`/`Button` text round trips green ON the emulator (`android_testhost_widget_suite`), full preset 993/993) |
| M-android u28 | button — the first REAL android control handler (the iOS Rosetta replayed over JNI) | ✅ (`src/platform/android/button_handler.cpp` REPLACES the headless partial in the android CMake branch, ported 1:1 from `ButtonHandler.Android.cs` + the Android extensions: a real `android.widget.Button` over the new `app_context()` seam (test host pins its themed Context), text/text_color/font(size+typeface)/character_spacing(ToEm)/padding(ToPixels)/enabled/visibility/alpha/automation_id(content description) pushes, stroke+corner_radius+background color in ONE lazily-installed `GradientDrawable`, click via `dev.mauicpp.NativeOnClickListener` (`src/platform/android/java/`, RegisterNatives — the AndroidNative-Java-lib analog; the testhost runner dexes it). Mirrors stay live for the VM-less cross-platform suite; `jni_cache` gained field ids. 14 both-direction seam cases in `tests/platform/android_button_tests.cpp` (read-backs over JNI getters + `performClick`→`clicked` + disconnect inertness) — full android preset **1845/1845** on-emulator; headless 1837 / apple 1487 / ios 1226 / tidy 0 unbroken. Documented deviations: plain `Button` not `MauiMaterialButton` (no Material AAR); `GradientDrawable` not `MauiRippleDrawable` (installing it drops the default theme bg); null-color branches collapse (non-nullable colors); no font registrar; `OnTouchListener` Pressed/Released + focus/layout-change wiring deferred (callbacks stay C++-drivable) |
| W3-29 | collection_view iOS native virtualization (Items2 compositional path) | ✅ headless/apple-compile-through/ios (the cross-platform items model + fake-viewport simulator from W2-19 stays the in-memory state mirror on every backend; W3-29 adds the REAL native UICollectionView stack on iOS, wired through the shared `collection_view_handler.hpp` `#ifdef MAUI_PLATFORM_IOS` block. `src/platform/ios/collection_view_handler.mm` now hosts a UNIFIED controller/cell/source architecture ported from the Items2 family (`ItemsViewController2` + the Structured/Selectable/Groupable/Reorderable controllers collapsed into ONE `MauiItemsCollectionViewController` — UICollectionViewDataSource+Delegate; `MauiCollectionViewCell` ⇐ TemplatedCell2/DefaultCell2 unified; `MauiCollectionReusableView` ⇐ the *SupplementaryView2 family) over a compositional `UICollectionViewCompositionalLayout` built by a `layout_factory` ⇐ LayoutFactory2 (item→group→section + boundary supplementary items; linear 1-col / grid Span-col over both orientations). The shared `.cpp` calls a small iOS bridge (`native_reload`/`native_rebuild_layout`/`native_update_selection_mode`/`native_update_platform_selection`/`native_update_empty_view`/`native_update_can_reorder`/`native_scroll_to`) at the same moments C# does (ReloadData/UpdateItemsSource/UpdateSelectionMode/UpdatePlatformSelection/UpdateLayout/UpdateEmptyView/ScrollTo). NEW shared test helper `tests/support/run_loop_pump.hpp` (pump_run_loop/pump_until — turns the CFRunLoop so the compositional layout + cell realization happen deterministically; reusable by other on-device units). `tests/controls/collection_view_ios_tests.mm` — 13 on-simulator cases: data→realized cells, **cell REUSE asserted under programmatic scroll** (distinct cell instances bounded, not one-per-item), selection single/multi via the native delegate + selected set, grouping supplementary header/footer present, reorder moves the bound model + re-renders gated on CanReorderItems, scroll-to moves the native content offset, empty-view host (tag 333). Counts: ios **+13** native cases; the cross-platform `collection_view_sim`/items suites (97 on-sim) stay green; headless 113 items/sim cases green; apple compile-through clean. DOCUMENTED DEVIATIONS: the classic-Items iOS UICollectionViewFlowLayout path is NOT ported (Items2 compositional only); estimated self-sizing cells reduced to a fixed per-cell estimate for deterministic test layout; cells show the item text mirror (the realized data_template content record lives in the cross-platform simulator); native interactive-drag mutator omitted because the erased i_item_collection is read-only (reorder mutates the bound typed collection, as a real drag's IList mutation does); macOS NSCollectionView host landed in W3-30.) |
| W3-30 | collection_view AppKit native backend (NSCollectionView FlowLayout) | ✅ apple/headless/ios-compile-through (the W2-19 placeholder `src/platform/apple/collection_view_handler.mm` is REPLACED with a REAL NSCollectionView stack, the AppKit twin of W3-29's iOS controller/cell/source architecture, wired through the shared `collection_view_handler.hpp` `// --- appkit (W3-30) ---` `#ifdef MAUI_PLATFORM_APPLE` block. A genuine `NSCollectionView` inside an `NSScrollView`, driven by an `NSCollectionViewFlowLayout` + a unified `MauiCollectionDataSource` (NSCollectionViewDataSource + NSCollectionViewDelegateFlowLayout — the ItemsViewController + Structured/Selectable/Groupable/Reorderable controllers collapsed into one, mirroring the iOS `MauiItemsCollectionViewController`), `MauiCollectionViewItem` ⇐ TemplatedCell2/DefaultCell2 unified (NSCollectionViewItem; default NSTextField label + recycled via `makeItemWithIdentifier:`/`prepareForReuse`), `MauiCollectionSupplementaryView` ⇐ the *SupplementaryView2 family (section header/footer). The shared `.cpp` calls a parallel apple bridge (`on_connect_handler` wires datasource/delegate; `native_reload`/`native_rebuild_layout`/`native_update_selection_mode`/`native_update_platform_selection`/`native_update_empty_view`/`native_update_can_reorder`/`native_scroll_to` + inspection seams) at the same C# moments, guarded by `#ifdef MAUI_PLATFORM_APPLE` next to the existing iOS guards. Selection→model via the delegate `didSelect/didDeselectItemsAtIndexPaths` (single/multi); programmatic selection synced via `selectItemsAtIndexPaths:`/`deselectItemsAtIndexPaths:`; empty-view a marker `MauiCollectionEmptyHostView` subview (NSView.tag is read-only unlike UIView.tag → `native_empty_view_shown()` seam). `tests/controls/collection_view_appkit_tests.mm` — **13** apple seam cases (run-loop pumped via the shared `run_loop_pump.hpp`): data→realized items, **item REUSE asserted under programmatic scroll** (distinct instances bounded, not one-per-item), selection single/multi + programmatic sync, grouping section header/footer supplementaries, reorder moves the bound model + re-renders gated on CanReorderItems, empty-view, grid (multi-column) + horizontal orientation. Counts: apple **+13** (full apple ctest 2199 green); headless 2671 green unchanged (apple `#ifdef` blocks inert); ios compile-through clean; tidy 0; ASan clean. DOCUMENTED DEVIATIONS: the iOS COMPOSITIONAL-LAYOUT path (UICollectionViewCompositionalLayout + LayoutFactory) and SNAP POINTS are iOS-ONLY — NSCollectionView has no compositional layout, so AppKit uses `NSCollectionViewFlowLayout` (linear=1 col, grid=`span` cols, both orientations via scrollDirection; header/footer via the flow layout's reference sizes) and snap points have no NSCollectionViewFlowLayout analog (the cross-platform simulator still mirrors them as state); estimated self-sizing cells reduced to a fixed per-item estimate for deterministic test layout; items show the item text mirror (the realized data_template content record lives in the cross-platform simulator); native interactive-drag mutator omitted (read-only erased i_item_collection — reorder mutates the bound typed collection, as on iOS).) |
| G1 | formatted_string + span — Label's rich per-span text (a gap the original audit MISSED) | ✅ headless/apple/ios (`maui::controls::span` ⇐ Span.cs: Text + FontFamily/FontSize/FontAttributes (new `maui::core::font_attributes` flags + with_attributes/attributes_of, FontExtensions) + TextColor/BackgroundColor/CharacterSpacing + TextDecorations + LineHeight + GestureRecognizers (tap-only via the new collection `validate` hook — Span.ValidateGesture) + `get_effective_font` (FontExtensions.GetEffectiveFont, IsSet-gated); `maui::controls::formatted_string` ⇐ FormattedString.cs: an observable spans collection that parents each span as a logical child (flows BindingContext), re-raises ONE `changed` signal on any collection/span change (SpansCollectionChanged + OnItemPropertyChanged), to_string concat + from_string factory, §8 per-span tokens dropped before the span dies. Label integration: `label.set_formatted_text` (Label.FormattedText) clears Text + subscribes `changed`; the label resolves each span into a `maui::core::label_run` (Span.ToNSAttributedString resolution: effective font / TextColor-or-default / BackgroundColor / Math.Max(0,kerning) / decorations / line-height) exposed via `i_label::formatted_text_runs` (the IImageSource-style controls→core boundary; FormattedText is NOT on C# ILabel — the reflection-free port hands the handler resolved runs). `map_formatted_text` builds the native NSAttributedString (apple/ios — shared `attributed_from_runs` + trait-aware `to_ns_run_font`/`to_ui_run_font` in apple_text_ops/ios_text_ops) and mirrors the runs headless; clearing reverts to plain Text. 16 backend-agnostic + 3 apple + 3 ios seam cases + the formatted_text_page demo (+3). Documented deviations: span TextTransform / the per-span Style object property / UpdateFormsText not ported (markup-era; the merged implicit/class Style still resolves through element); span gesture recognizers are stored+parented but not pushed to a native composite (the label hosts those — deferred); FontSize "unset" is 0 not NaN (observationally identical on the effective-font path). Counts: headless **1853** / apple **+9** / ios **+6**, tidy 0.) |

## Deferred backlog (revisit later — each was a documented "first cut", never a silent gap)

Consolidated from the per-component Notes above. Pick these off opportunistically or alongside the
milestone that needs them; none blocks M5.

Most of the M4/M5 backlog was CLOSED in the **M5d sweep** (gradients, per-corner clip + System.Numerics,
AppKit text + entry editing, image cache/font-source/DI, the NSWindow host + window/app extras, navigation
chrome/animation/modal, the styles/resources subsystem, Semantics/InputTransparent + the VSM auto-drive).
What remains:

**ViewMapper / visuals:**
- ✅ **DONE (M5d):** gradient paints (+ apple `CAGradientLayer`); per-corner `corner_radius` clip; `Semantics` + `InputTransparent` (bindable + mapped to the headless mirror — see the a11y note below). The `ImagePaint`/`PatternPaint` *drawing-canvas* (ICanvas Fill*) kinds are now DONE (unit U-IP — graphics table); the `ImagePaint`/`PatternPaint`-as-view-Background brush path stays out of scope (C# iOS faithfully throws there).
- ✅ **DONE (W1-11):** `ToolTip` + `ContextFlyout` (shared view_mapper keys over i_tool_tip_element / i_context_flyout_element + per-backend view_chrome_ops; real NSView.toolTip / NSView.menu on AppKit, UIContextMenuInteraction attach on iOS, tooltip = documented iOS no-op per C#). Still deferred: C#'s `IsConnectingHandler()` default-skip optimization.

**Layout-engine refinements (M5d unit H):**
- ✅ **DONE:** Size requests on `view<>` — the bindable `WidthRequest`/`HeightRequest`/`MinimumWidthRequest`/`MinimumHeightRequest`/`MaximumWidthRequest`/`MaximumHeightRequest` (NON-template shared descriptors in `view.cpp`; defaults -1/-1/-1/-1/+inf/+inf, matching VisualElement.cs). `i_view::width()`/`height()`/`minimum_*`/`maximum_*` now derive from the REQUEST (not the arranged frame, which stays in `frame_`) via VisualElement's IView mapping (unset OR explicit -1 → Unset(NaN) for Width/Height; unset → Unset for Minimum*; `EnsurePositive` otherwise; Maximum* always `EnsurePositive`, default +inf). `property<T>` gained `is_set()` (the C# `IsSet(BindableProperty)` analog). `layout<>` dropped its hardcoded width/height/min/max overrides — the base now supplies request semantics, so a layout honors an explicit size request too. The leaf-control `view<>::measure` resolves the handler's content size against the view's OWN requests (`ViewHandlerExtensions.ResolveConstraints` → `layout_manager::resolve_size_request`), the per-child clamp the managers rely on.
- ✅ **DONE:** z-index ordering — `view<>` gained a bindable `z_index` (VisualElement.ZIndex); `order_by_z_index` + `get_layout_handler_index` (`Microsoft.Maui.Handlers.LayoutExtensions`, stable sort + the GetLayoutHandlerIndex algorithm) ported in `core/layout_z_order.{hpp,cpp}`. `layout_handler` add/insert place each new subview at its z-ordered index, and `update_z_index` re-stacks on a runtime change (headless mirror + Apple `NSView` subviews). A child's z-index change routes to the parent layout's handler `update_z_index` (the `ViewHandler.MapZIndex` → `LayoutHandler.UpdateZIndex` chain, inlined via the child's `logical_parent`). **Deviation:** the C# layout *managers* never sort by z-index (z-index is paint/subview order only — confirmed: `grep ZIndex src/Core/src/Layouts` is empty), so the managers keep arranging in logical order and z-index drives the native subview stacking in the handler (where MAUI puts it); the literal "managers arrange in z-order" wording is not followed because it would be invented behavior. `order_by_z_index` is the faithful C# helper (used by C#'s bulk `SetVirtualView`); the port adds children incrementally via `get_layout_handler_index`, so `order_by_z_index` is currently test-exercised only.
- ✅ **DONE:** `ClipsToBounds` — `i_layout.clips_to_bounds()` + bindable `Layout.IsClippedToBounds` on `layout<>` (shared `clips_to_bounds_property()` descriptor, default false) + a `layout_handler` mapper entry (`map_clips_to_bounds`, chained after `view_mapper`) → `layout_platform::update_clips_to_bounds` (headless mirror; Apple `layer.masksToBounds`).
- ✅ **DONE (X4):** `ICrossPlatformLayout` / `ISafeAreaView` on the layout seam — the `i_cross_platform_layout` contract (`core/i_cross_platform_layout.hpp`) + the existing `i_safe_area_view` are now standalone mixins on `layout<>`: `cross_platform_measure`/`cross_platform_arrange` forward to the layout manager (C# `Layout.CrossPlatformMeasure`/`CrossPlatformArrange` == `LayoutManager.Measure`/`ArrangeChildren`), and `ignore_safe_area()` is the obsolete `Layout.IgnoreSafeArea` auto-property (default false, with `set_ignore_safe_area`). Kept OFF `i_layout` (it stays the minimal manager/handler surface). **Deviation:** the actual native safe-area inset application is an Apple-host deferral (headless has no safe area; the per-edge `SafeArea.Ignore` attached property is a loader-side concern). The `SafeAreaEdgesTypeConverter` + `safe_area_edges`/`safe_area_regions` value types are ported (X4 converter tail).

**Accessibility / hit-testing — ✅ DONE (tail unit Y):** the native apple push is in — `apple_semantics_ops.hpp`'s `apply_semantics` (Description → `accessibilityLabel`, Hint → `accessibilityHelp`, `is_heading` → the heading role; ports the iOS `SemanticExtensions.UpdateSemantics` intent) + `apply_input_transparent` (a per-class `-hitTest:` gate — the AppKit analog of `UserInteractionEnabled = false` / MAUI's `LayoutView.HitTest`, since AppKit containers don't recurse subview hit-tests), overridden on all 6 apple platform structs.

**Image — ✅ mostly DONE (tail unit IM; production tail + FontManager unit Y3):** on-disk uri cache (`uri_image_disk_cache` + a faithful CRC-64-Jones `crc64`, layered under the in-memory TTL cache, both honoring `CacheValidity` via the injected clock seam — ports `UriImageSourceService.DownloadAndCacheImageAsync`/`GetCachedFileName`); a real **async `NSURLSession` dataTask** fetch marshalled back through the dispatcher (weak-liveness + cancellation + source-identity recheck preserved; TSan-clean); native GIF multi-frame playback via `NSImageView.animates` driven by `IsAnimationPlaying`. **Y3 closed the remaining gaps:** a real **`font_manager`** (`i_font_manager` + `font_registrar`/`i_font_registrar` + `font_file`) — ports `FontManager.iOS`/`FontManager.Standard` (the installed-family descriptor / `.SFUI-*` system-font-by-weight / cleansed-name `fontWithName` chain + weight/slant traits + Dynamic Type scaling on iOS; `CleanseFontName` → registrar alias / file-name variants / PostScript name; an owned per-font cache) on apple (`NSFont`) + ios (`UIFont`) + a headless sentinel fake; the **font image service resolves its typeface through it** (`default_font_manager().get_font`, the process-wide no-DI seam). Resolution-dependent **reload is wired end-to-end**: `image_handler::on_window_changed()` re-issues a resolution-dependent source when the per-backend `query_display_scale()` (apple window backing scale / ios trait `displayScale`) differs from the load-time density (`RequiresReload`). The **apple file service now probes `@2x`/`@3x`** for parity with iOS (the shared `apple_shared/scaled_file.hpp` `get_scaled_file`). The **`ImageSourcePaint` background branch** renders on apple+ios: `image_source_paint` (a `Paint` carrying an `i_image_source`) installs the source's image as a named backing `CALayer` via `apply_background` (apple/ios visual_ops), clearing the solid fill — reusable by the X1 ImageBrush unit. Still deferred: the typed DI service-provider resolves by source instance (a `dynamic_cast`), not `System.Type` (a no-reflection consequence — PROFILE §6); disk TTL uses the per-process steady-clock seam (cross-process stamps detected via negative-age → miss) rather than wall-clock mtime; embedded-font *loading* (the `IEmbeddedFontLoader` resource-copy step) — the registrar models the alias→filename map a host populates, no managed assemblies.

**Navigation (M4d) — bar / animation / modal / stack-edits DONE (M4d follow-up):**
- ✅ Navigation bar (custom AppKit NSView + NSTextField title + back NSButton chevron above the content),
  back button → `pop()`; bar title tracks `current_page()->title()`, back shown when depth > 1.
- ✅ Push/pop animation (`push/pop/pop_to_root(animated=true)` thread `NavigationRequest.Animated`; apple
  CoreAnimation `CATransition` cross-fade on the content swap; headless synchronous; completion stays sync).
- ✅ Modal stack (`push_modal` / `pop_modal` / `modal_stack()` — a SEPARATE overlay stack, lifecycle order
  ported from `NavigationModel.PushModal/PopModal`; apple overlays a modal NSView on the container).
- ✅ `insert_page_before` / `remove_page` (INavigation; ported from `MauiNavigationImpl.OnInsertPageBefore` /
  `OnRemovePage` incl. removing the current page → `pop()`, inner removal fires no lifecycle).
- Still deferred: Shell; the window-hierarchy guard in `SendAppearing` (Window/Application lifecycle — M5);
  `BarBackground`/`BarBackgroundColor`/`BarTextColor`/`TitleView`/`TitleIconImageSource` styling;
  `Pushed`/`Popped`/`PoppedToRoot`/Navigated* events; overlapping-navigation queueing (collapsed to
  synchronous); modal-as-child-NSWindow (we use an in-container overlay — documented deviation);
  `Page.SendBackButtonPressed` hardware-back hook (only the StackDepth>1→pop branch is ported).

**Text controls (M2–M4):** **DONE (AppKit text polish + full entry editing — see the component table row).**
The earlier deferrals are now implemented: `character_spacing` on button/label/entry (shared
`apple_text_ops.hpp` attributed-string builder over `NSKernAttributeName`), label/entry
`vertical_text_alignment` (custom vertical-aligning cells) + `placeholder_color` (attributed placeholder
foreground), `button` padding (custom `MauiButtonCell` content insets), and the entry editing surface —
`ReturnType` / `ClearButtonVisibility` / `IsTextPredictionEnabled` / `IsSpellCheckEnabled` /
`CursorPosition` / `SelectionLength`. **Keyboard + focus subsystem — ✅ DONE (W8-53):** the `Keyboard`
value type (`maui::core::keyboard` ⇐ Keyboard.cs + `keyboard_flags` ⇐ KeyboardFlags.cs — a `kind`
discriminator + flags value type reproducing C#'s singleton/RTTI identity by value equality, with
`default_keyboard()/plain/chat/email/numeric/telephone/text/url/date/time/password` + `create(flags)`),
the `keyboard` bindable property on entry/editor/search_bar (ITextInput.Keyboard, default Keyboard.Default)
wired through each handler's mapper key `keyboard` (mapped AFTER prediction/spellcheck so a custom
keyboard's flags win — the C# mapper order); and the **focus subsystem** — `i_view`'s Focus/Unfocus/
IsFocused/Focused/Unfocused, `set_is_focused` as the OnIsFocusedPropertyChanged funnel (fires
Focused/Unfocused + ChangeVisualState), and the shared `view_command_mapper` (Focus/Unfocus) chained onto
every text handler's command mapper, driving the per-backend `view_focus_ops` (becomeFirstResponder /
resignFirstResponder on iOS, window makeFirstResponder: on AppKit, always-succeed no-op headless). iOS
also adds `MapKeyboard` (UIKeyboardType + autocapitalization/spellcheck/autocorrection traits via
`ios_keyboard_ops.hpp` = KeyboardExtensions.ApplyKeyboard) + the **Done input-accessory toolbar**
(AddMauiDoneAccessoryView, entry+editor only — not search bar, matching C#) + IsFocused reflection from
the editing-begin/end callbacks. Still deferred (genuine gaps): `ReturnCommand`; the full
`KeyboardAutoManager` scroll-avoidance (ShouldReturn's next-responder walk collapses to its resign arm);
on AppKit, soft-keyboard concepts (KeyboardType / the Done accessory bar) have **no equivalent** so
`map_keyboard` is mirror-only there (documented deviation — AppKit has no soft keyboard); `ReturnType` /
`ClearButtonVisibility` likewise have no AppKit visual analog (mirror-only, headless mirrors them too), and
prediction/spellcheck apply only while a field editor is attached (first-responder).

**Styles / triggers / VSM (M5b → mostly resolved in M5d):**
- ✅ **DONE (M5d):** `resource_dictionary` + merged dictionaries + `DynamicResource`, implicit (TargetType-keyed) styles, style classes, `BasedOn`-by-resource-key; the VSM #18103/#34363 implicit-style **downgrade** + system-state `with_full_vsm_priority` promotion (+ the `visual_state_setter_system` constant); `Setter.TargetName`, `EnterActions`/`ExitActions`, `MultiTrigger`, `data_trigger` (typed BindingContext condition). See the M5d component row.
- ✅ **DONE (W1-15 styles tail):** `Style.ApplyToDerivedTypes` (over a DECLARED base-type chain — `set_style_target_type<TControl, TBases...>`), `Behaviors` (+ `view<>::behaviors()`), `EventTrigger` (+ the `trigger_action` bases, over the named-event registrar on `element`), `StateTriggers` (StateTrigger/CompareStateTrigger<T>/DeviceStateTrigger/OrientationStateTrigger/AdaptiveTrigger), the attached-property `VisualStateGroupsProperty` storage face (`view<>::set_visual_state_groups`); the `is_enabled → Disabled/Normal` auto-drive landed in M5d-H. See the W1-15 component row.
- Still deferred: `CanCascade`; Behaviors/Triggers attached to a `Style`; VSM Clone + duplicate-name validation; per-trigger index ordering (all triggers share `setter_specificity::trigger`). Implicit styles match only controls that declare a style target type via `set_style_target_type<T>()` (button/label/entry/image/content_page/the layouts/grid/navigation_page do; a user subtype declares its chain for the derived-type match).

**Application / Window / lifecycle:**
- ✅ **DONE (M5d):** the native **`window_handler` over `NSWindow`** (a lightweight `i_element_handler`: creates the NSWindow, sets its contentView to the root page's `native_view()`, maps `NSWindowDidBecomeMain`/`WillClose` → `send_activated`/`send_destroying`) + a headless mirror + a runnable `maui_app_sample`; window geometry (X/Y/Width/Height + `frame_changed`), application themes (UserAppTheme/PlatformAppTheme/RequestedTheme), window resume/sleep (`Resumed`/`Stopped`/`Backgrounding`); the `i_window` Core contract.
- ✅ **DONE (tail unit MISC):** the `i_application` Core contract — `application` now implements it (like `window : i_window`); plus the navigation bar styling (`bar_background_color`/`bar_text_color` bindable + `title_view`, applied on the AppKit bar via the `i_stack_navigation` chrome getters) and the `pushed`/`popped`/`popped_to_root`/`navigating`/`navigated` events in C#'s order.
- Still deferred: multi-window orchestration subtleties + `IPersistedState` (`Backgrounding` is a bare notification); `windowDidResignMain`→`send_deactivated` is intentionally omitted (it would wrongly Disappear the page — the port only Disappears on Destroying); window chrome / TitleBar / display-density; `BarBackground` (Brush). The `Page.SendAppearing` hard window-guard stays soft (the window DRIVES appearing; the navigation_page self-drives the no-window case). `navigating`/`navigated` are navigation_page events carrying the page (content_page has no NavigatingFrom/NavigatedTo plumbing at this layer).

**Data binding (M5a) → M7 (XAML):** ✅ landed — the W1-02 runtime engine (nested string paths, `StringFormat`, `FallbackValue`/`TargetNullValue`, `MultiBinding`, relative/ancestor sources) is now BRIDGED into XAML `{Binding}` via `register_runtime_bindings()` (see the M7 binding-bridge component row). Still deferred: compiled-binding source-gen, the `bind_to_context` convenience, `{Binding Source=…}` for non-walkable (value-only) sources, UpdateSourceEventName.

## Components

| Component | Layer | Contract✓ | Tests ported | Impl | Platform(s) | Notes |
|---|---|---|---|---|---|---|
| project scaffold (CMake/presets/vcpkg/GoogleTest+GMock+Google Benchmark) | infra | — | — | ✅ | headless | green smoke `ctest` (Step 1a done); AppleClang 21, cmake 4.3, ninja, vcpkg toolchain |
| `color` + `colors` | graphics | ✅ | ✅ | ✅ | headless | 36 GTest cases green (incl. exact HSL/HSV→hex, 147 named colors round-trip). System.Numerics (Vector4) interop omitted — TODO; byte/int ctors exposed as from_rgb/from_rgba factories |
| `point`/`point_f`/`size`/`size_f` | graphics | ✅ | ✅ | ✅ | headless | 124 parse cases + cross-cast/operator characterization green; double+float families; Vector2/Matrix3x2 interop omitted (TODO) |
| `rect`/`rect_f` | graphics | ✅ | ✅ | ✅ | headless | 66 parse cases + contains/union/intersect/inflate/edges/casts characterization; `Union`→`union_with` (C++ keyword) |
| `path_f` + `path_operation` | graphics | ✅ | ✅* | ✅ | headless | build/shapes/flatten/bounds/reverse/rotate/separate/arc + SVG arc (ArcFlattener, SVGArcTo). *characterization tests (no C# oracle). Matrix3x2 `Transform` omitted (TODO) |
| `path_builder` (SVG parser) | graphics | ✅ | ✅* | ✅ | headless | M/L/H/V/C/S/Q/T/A/Z abs+rel; *characterization tests. Faithful to PathBuilder.cs (incl. its dead implicit-after-M branch) |
| **canvas core (F1)**: `i_canvas`/`i_drawable`/`canvas_state`/`abstract_canvas<TState>`/`recording_canvas` + the canvas enums (`line_cap`/`line_join`/`winding_mode`/`blend_mode`/`horizontal_alignment`/`vertical_alignment`/`text_flow`) + `font` (graphics) + `canvas_defaults` + matrix3x2 factories/extensions | graphics | ✅ | ✅* | ✅ | headless + macOS + iOS | The full ICanvas member set 1:1 (C# set-only properties → `set_*`; DrawImage documented-deferred with IImage). `abstract_canvas<TState>` ports AbstractCanvas (state stack, transform tracking, stroke-limit scaling, lazy dash flush incl. the offset-doesn't-dirty quirk, rounded-rect draw clamp); `canvas_state` ports CanvasState (dash/stroke/transform + DeconstructScales-derived scales); ICanvasStateService/IStringSizeService collapsed (default/copy ctor; per-backend measurement). `recording_canvas` is the HEADLESS canvas: derives abstract_canvas (so the C# state semantics run headlessly) and records typed op structs (a `std::variant` of 40 ops) for golden-op assertions — PictureCanvas-inspired, NOT a port of it (deviations recorded in the header: antialias/reset recorded, fill-paint stored as the background_color projection, dash surfaces as the platform flush). `maui::graphics::font` collapses C# IFont into the Font value struct (only first-party impl). matrix3x2 gains CreateTranslation/Scale/Rotation + GetDeterminant/DeconstructScales/GetLengthScale (real call sites arrived). 27 GTest cases (canvas_tests.cpp). *characterization (no GPU-free C# unit tests cover AbstractCanvas) |
| **Graphics.Text**: `text_attributes` (+ `text_attribute`/`marker_type`), `attributed_text_run` (+ comparer/`optimize_runs`), `i_attributed_text`, `attributed_text` (+ `attributed_text_block`, `optimize`/`create_paragraphs`/`create_paragraph_run`/`create_blocks`) | graphics | ✅ | ✅* | ✅ | headless + macOS + iOS | TextAttributes' Dictionary<TextAttribute,string> as a value map with the typed extension accessors folded in (bools stored as C# "True"/"False"; the GetMarker/SetMarker stored-under-UnorderedList quirk preserved); IAttributedTextRun collapsed into the `attributed_text_run` value type; AbstractAttributedText.Optimal folded into `i_attributed_text::optimal()`. CreateParagraphs reproduces StringReader.ReadLine + C#'s start+=length+1 cursor quirk. **Documented-deferred:** the XML wire format (XmlAttributedTextReader/Writer), MutableAttributedText. 30 GTest cases (attributed_text_tests.cpp). *characterization |
| `coregraphics_canvas` (src/platform/apple_shared/ — ONE .mm for BOTH Apple backends) | platform/apple_shared | ✅ | ✅* | ✅ | macOS + iOS | Port of Platforms/MaciOS/PlatformCanvas.cs over `abstract_canvas<coregraphics_canvas_state>` (PlatformCanvasState's Shadowed flag): CGContext stroke/fill/alpha/blend/dash (size-scaled), draw/fill line/arc(incl. the elliptical transform path)/rect/rounded-rect(arc-to-point walk)/ellipse/path, clip rect/path/subtract (EOClip), CTM transforms, save/restore/reset, SetShadow, solid + linear/radial CGGradient fills (FillWithGradient incl. the shadow pre-fill), **image + pattern fills (unit U-IP): FillWithImage (CGPattern NoDistortion tiling + the Y-flip tile matrix, blits the image CGImage) + FillWithPattern (CGPattern ConstantSpacing, the tile callback re-enters a nested canvas and runs the pattern draw), SetFillPaint detects image_paint/pattern_paint, every Fill* (rect/rounded-rect/ellipse/arc/path — path uses the bounding-box origin as the phase) dispatches gradient→pattern→image→solid, and FillColor/Restore/Reset clear the staged pattern+image like C#**, CoreText draw_string (CTLine + framesetter w/ alignment) + draw_text (per-run bold/italic/underline/fore/background) + get_string_size (typographic bounds / the ×10-factor framesetter measure). **Header deferrals:** getColorspace knob → DeviceRGB; CT font resolution subset; strikethrough/sub/superscript run attrs. (PatternPaint/ImagePaint *drawing-canvas* fills + DrawImage are now DONE; the ImageBrush-as-view-Background path stays deferred — C# iOS throws there.) Contains a flagged COPY of the `path_to_cg_path` walk (apple_visual_ops.hpp/ios_visual_ops.hpp — **DEDUP for the coordinator**). 11 base + 5 U-IP (image/pattern fill, upright, clear-staged-image) CGBitmapContext pixel-readback GTest cases run on macOS AND on the iOS simulator. *characterization |
| benchmarks (color, path) | graphics | — | — | ✅ | headless | BenchmarkDotNet → Google Benchmark; `maui_graphics_benchmarks` builds + runs |
| `event<>` + `scoped_connection` | core | ✅ | ✅* | ✅ | headless | 16 GTest cases; multicast with snapshot-on-raise (mirrors .NET `X?.Invoke`) + token/RAII teardown (the WeakEventManager role per §8). Uses port-provided `move_only_function` (libc++ lacks `std`'s). *characterization — infra, no C# oracle |
| `i_dispatcher` + `manual_dispatcher` | core | ✅ | ✅* | ✅ | headless | 11 GTest cases; mirrors `IDispatcher`/`IDispatcherTimer` (dispatch / dispatch_delayed / timer + is_dispatch_required). Headless impl is a deterministic **virtual-clock** pump (`run_pending`/`advance`) — no wall-clock/threads. *characterization (C# Standard impl just throws) |
| `setter_specificity` (+ `setter_specificity_list`) | core | ✅ | ✅* | ✅ | headless | packed-uint64 precedence key + per-property value store; 14 GTest cases incl. a port of `SetterSpecificityListTests`. *characterization |
| `bindable_property<T>` + `bindable_object` + `property<T>` | core | ✅ | ✅* | ✅ | headless | **fully-typed** value layer, **no `std::any`** (per §7): each `property<T>` member owns a `setter_specificity_list<T>` and the value precedence; `bindable_object` is just the notification base; `bindable_property<T>` is the typed shared descriptor. Change-notification (changing→changed, real-change-only), coerce/validate, lazy+cached default-creator, handler override, zero-copy `get()→const T&`, per-instance `.changed`. 15 cases derived from `Core.UnitTests`. *characterization |
| primitives: `visibility`/`flow_direction`/`layout_alignment`/`thickness`/`font` | core | ✅ | ✅* | ✅ | headless | Core value types/enums the view contracts depend on; 26 GTest cases. *characterization |
| `i_element`/`i_transform`/`i_view`/`i_text_style`/`i_text` | core | ✅ | ✅* | ✅ | headless | virtual-view contracts (abstract `i_*` classes, §11 per-type rule). Full `IView` surface; heavy sub-objects (paint/semantics/shadow/clip) + the typed view-handler accessor forward-declared/deferred to M3/M4/#22. 5 GTest cases (mock conformance). *characterization |
| handler seam: `i_element_handler`/`i_view_handler`, `property_mapper`/`command_mapper`, CRTP `view_handler`, `type_tag`, `handler_registry`/`service_registry`, `i_maui_context` | core | ✅ | ✅* | ✅ | headless | 17 GTest cases (mock handler over a fake platform view: connect creates+connects+maps, update_value/invoke, disconnect; mapper chaining/override; type_tag identity; registries). No-reflection **explicit registration** (PROFILE §6); platform view = `void*` and command args = `std::any` (boundary-confined erasure only). M1 simplifications noted in headers: no merged-mapper cache, no CanInvokeMappers gate, reciprocal `view.Handler=this` deferred to M2 hosting. *characterization |
| `i_button`/`i_padding`/`i_button_stroke`/`i_text_button` | core | ✅ | ✅* | ✅ | headless | Button virtual-view contracts (IButton : IView, IPadding, IButtonStroke; ITextButton adds IText). `IButton.Pressed/Released/Clicked()` → `send_pressed/send_released/send_clicked()` (renamed: C++ can't share a name between the method and the control's event). *characterization |
| `view<ViewInterface>` (minimal control base) | controls | ✅ | ✅* | ✅ | headless | bindable_object + i_view impl + handler ownership/wiring + measure/arrange seam. Templated on the control's view-interface to avoid the i_view diamond (no virtual inheritance). **M2 subset** — full VisualElement property set / real layout deferred to M3/M4. *characterization |
| `button_handler` (+ headless partial) | core/handlers | ✅ | ✅* | ✅ | headless | CRTP `view_handler<button_handler, i_button, button_platform>`; cross-platform mapper tables (Text live; text_color/font/stroke/padding deferred) + per-backend platform recipe (create/connect/disconnect/map_text). `button_platform` is a single cross-platform struct (native slot + headless mirror + event callbacks). *characterization |
| `button` control | controls | ✅ | ✅* | ✅ | headless + macOS | The Rosetta Stone's virtual view. **Full own surface bindable + mapped**: Text + the i_text_style appearance (text_color/font/character_spacing) + Padding + the i_button_stroke border (stroke_color/thickness/corner_radius); clicked/pressed/released events + optional command; IsEnabled gating (ButtonElement order: command→event; release always clears IsPressed). Self-registers its handler (MAUI_REGISTER_HANDLER). 14 headless GTest cases (seam both directions + every property). *characterization |
| `button_handler` AppKit backend (`.mm`) + `maui_button_sample` | platform/apple | ✅ | ✅* | ✅ | macOS | Real `NSButton` via Obj-C++/ARC: Text→`title`; font→`NSFont`; text_color→`contentTintColor`; stroke→layer border; target-action trampoline → `send_clicked`; RAII NSButton release. character_spacing → an attributed `NSButton.attributedTitle` (kerning, + the text color in the kerned title) via `apple_text_ops.hpp`; padding → a custom `MauiButtonCell`'s content insets (the AppKit analog of UIButton's ContentEdgeInsets). 10 GTest cases drive a real NSButton (`performClick:` → `clicked`; appearance; disabled suppressed; disconnect; registry-resolved; kerning; kerning+color; padding insets). Sample app = a live NSWindow round-tripping a tap. Translated from ButtonHandler.iOS.cs (no AppKit oracle in mainline MAUI; macOS there is Mac Catalyst/UIKit). *characterization |
| handler self-registration (`default_handler_registry`/`handler_registrar`/`MAUI_REGISTER_HANDLER`) | core | ✅ | ✅* | ✅ | headless + macOS | Opt-in §6 self-registration over the explicit primitive; macro-free registrar + macro sugar; noexcept registrar (load-time). OBJECT-library tree-shaking caveat documented. clang-tidy `^MAUI_` allow-listed. *characterization |
| `label` control (`i_label`/`i_text_alignment`/`text_alignment`/`text_decorations`, `label_handler`) | controls + core/handlers | ✅ | ✅* | ✅ | headless + macOS | Second control (display-only) — proves the recipe generalizes. Bindable text/text_color/font/char_spacing/padding/alignments/decorations/line_height; mapper keyed on i_label, now chained onto `view_mapper()` (M4b). Headless mirror + real macOS NSTextField (text/textColor/font/alignment). The handler now also maps character_spacing (kerned `attributedStringValue` via `apple_text_ops.hpp`) + vertical_text_alignment (a custom `MauiLabelCell` offsetting the text rect Start/Center/End); decorations + line_height + padding + the explicit-Width multi-line wrap are now mapped too (see **W8-54** below). Shared `apple_conversions.hpp` (color/font→AppKit). 6 headless + 5 apple GTest cases; self-registers. *characterization |
| shared ViewMapper (`view_platform_base`, `view_mapper`; `view<>` IsEnabled/Opacity/Visibility/AutomationId bindable) | core/handlers + controls | ✅ | ✅* | ✅ | headless + macOS | M4b Unit 2 — the generic-IView property mapper every handler chains. `view_platform_base` (non-template platform-view base: headless mirrors hidden/alpha/enabled/automation_id + backend-overridable update_*); `view_mapper()` = `property_mapper<i_view, i_view_handler>` keyed visibility/opacity/is_enabled/automation_id. `i_view_handler::platform_base()` + CRTP `view_handler` non-breaking `if constexpr` impl. The four `view<>` props bindable via NON-template descriptor free-fns in `src/controls/view.cpp` (opacity clamped [0,1]). button/label platform structs derive the base + chain `view_mapper()`; AppKit pushes to NSButton/NSTextField (hidden/alphaValue/setEnabled:/accessibilityIdentifier). Wider ViewMapper set (Width/Height/Background/transforms/Clip/Shadow/FlowDirection) + IsConnectingHandler skip deferred. 9 headless + 2 apple GTest cases. *characterization |
| ViewMapper transforms + FlowDirection (`transform_spec`, `map_transform`/`map_flow_direction`; `view<>` transform + flow_direction bindable; `apple_view_ops.hpp`) | core/handlers + controls + platform/apple | ✅ | ✅* | ✅ | headless + macOS | M4c Unit A — widens `view_mapper()` with the render transform + FlowDirection. `view_platform_base` gained a POD `transform_spec` (ten ITransform scalars, identity defaults) + `transform`/`flow_direction` mirrors + `update_transform`/`update_flow_direction`. Ten transform keys all route to ONE `map_transform` (reads all ten off i_view → pushes the whole spec, so any change rebuilds the full transform, per TransformationExtensions) + `flow_direction`→`map_flow_direction`. The ten `view<>` transform getters + `flow_direction()` now bindable (NON-template descriptor free-fns in `view.cpp`) with public setters; names match the mapper keys. Shared AppKit helpers `apply_transform`/`apply_flow_direction` in the new `src/platform/apple/apple_view_ops.hpp` (faithful TransformationExtensions CATransform3D build incl. m34 perspective; NSView.userInterfaceLayoutDirection). **Retrofitted (M4c integration) onto all six platform structs** (button/label/entry/image/layout/content_page override `update_transform`/`update_flow_direction` → the helpers; grid reuses layout_handler), so transforms/flow_direction reach the native NSView. CMake +`-framework QuartzCore` (apple `maui_core`). Background/Shadow/Clip/Width/Height/Semantics/InputTransparent deferred (need new value types or are layout-driven). 5 headless + 6 apple GTest cases. *characterization |
| `grid` control (`row_definition`/`column_definition`, Grid.Row/Column/Span attached props) | controls | ✅ | ✅* | ✅ | headless + macOS | M4c Unit B — `grid : layout<i_grid_layout>` over the existing `grid_layout_manager`. Concrete row/column definitions (default Star) + RowDefinitions/ColumnDefinitions; bindable row/column spacing; the Grid.Row/Column/RowSpan/ColumnSpan attached props stored per-child in a pointer-keyed `cell_info` map (C# validation: row/col ≥ 0, span ≥ 1). Reuses `layout_handler` (a grid is an i_layout) → inherits the native NSView panel + chained `view_mapper()`. 22 headless + 4 apple GTest cases; self-registers. *characterization |
| `content_page` (`i_content_view`, `content_page`, `content_page_handler`) | controls + core/handlers | ✅ | ✅* | ✅ | headless + macOS | M4c Unit C — a minimal page hosting a single Content. `i_content_view` (content + i_padding); `content_page : view<i_content_view>` (non-owning Content + bindable Title/Padding; measure/arrange port C# MeasureContent/ArrangeContent — content sized within padding). `content_page_handler` hosts the content's native view as a subview of a plain NSView (headless mirror). Chains `view_mapper()`; derives `view_platform_base`. Navigation/toolbar/safe-area/dialogs/templates deferred. 11 headless + 4 apple GTest cases; self-registers. *characterization |
| image file source (`i_image_source`/`i_file_image_source`, `file_image_source`, `image.source`) | core + controls + handlers | ✅ | ✅* | ✅ | headless + macOS | M4c Unit D — `image` gained a real bindable `source` (`property<shared_ptr<i_image_source>>`). `i_image_source`/`i_file_image_source` contracts + concrete `file_image_source` + `image_source::from_file(path)`. `image_handler::map_source` loads the file **synchronously** (`[[NSImage alloc] initWithContentsOfFile:]`; headless mirrors path + loaded flag; empty/null/failed → clears). Async loading + cancellation, the IImageSourceService/service-provider seam, uri/stream/font sources + caching deferred. 6 headless + 4 apple GTest cases. *characterization |
| async image sources (`i_uri_image_source`/`i_stream_image_source`, `uri/stream_image_source`, `i_image_source_service`/`image_source_service_registry`, `image_source_loader`, `cancellation_token`) | core + controls + handlers | ✅ | ✅* | ✅ | headless + macOS | M4d Unit I — uri + stream sources, an async loader with cancellation, an in-memory uri cache, and the service-registry seam. `i_uri_image_source` (uri + caching_enabled) / `i_stream_image_source` (a bytes provider `image_bytes get_bytes(token)`, simplifying C#'s `Task<Stream>`); concrete `uri_image_source`/`stream_image_source` + `from_uri`/`from_stream`. `i_image_source_service` (async `load(source, token, on_result)`); `image_source_service_registry` (mirrors handler_registry; `register_service<Source,Service>()` keyed by source interface, `resolve` via dynamic_cast probe) + a default registry; `image_source_result` (native handle + RAII disposer + headless kind/detail mirror); per-source `file/uri/stream_image_source_service` (cross-platform decl + `{headless,apple}` partials sharing one `decode_image_bytes`). `image_source_loader` (ports ImageSourcePartLoader + ImageSourceServiceResultManager): `begin_load` cancels the prior token + disposes the prior result; `update_source` resolves the service / uri cached fast-path (`map<uri,bytes>`, no expiry), marshals the apply onto the dispatcher (headless `manual_dispatcher`; apple inline), does the source-identity recheck (`!token.is_cancelled() && source==current_source_`), `complete_load`. `image_handler` OWNS the loader (`source_loader()`); `map_source` keeps the SYNC file fast-path + routes uri/stream async (routing cross-platform; 3 per-backend primitives touch NSImageView / mirror). Still chains `view_mapper()`; `image_platform` derives `view_platform_base` (+ `source_kind` mirror). THREADING: loader state touched only on the dispatcher thread; services load synchronously (no worker) so the sole cross-thread element is the atomic cancellation flag → TSan-clean; UAF-safe teardown via a loader liveness weak_ptr; ABA-safe via token cancellation. Disk caching/CacheValidity, font sources, the full DI provider, resolution reload, and a production HTTP stack deferred (apple uri = `file://` + sync `http(s)` NSData; headless = stream + `file://`, no network). 6 headless + 3 apple GTest cases (extend the existing image test files). *characterization |
| page lifecycle + `navigation_page` (`navigation_request`/`i_stack_navigation`, `navigation_page`, `navigation_page_handler`; `content_page` Appearing/Disappearing) | core + controls + core/handlers | ✅ | ✅* | ✅ | headless + macOS | M4d Unit N — a push/pop page stack. `content_page` gained the page lifecycle (Page.cs SendAppearing/SendDisappearing + `appearing`/`disappearing` events, idempotent via `has_appeared_`). `navigation_request` (new stack + animated) + `i_stack_navigation` (request_navigation/navigation_finished, IStackNavigation.cs). `navigation_page : view<i_view> + i_stack_navigation` owns a NON-owning `vector<content_page*>` (caller owns the pages) with push/pop/pop_to_root + current_page/root_page/navigation_stack; push/pop/pop_to_root mutate the stack then fire Disappearing(prev)+Appearing(new) BOTH before notifying the handler, per NavigationPage.cs MauiNavigationImpl ORDER (C#'s async semaphore/TaskCompletionSource/overlap queue collapsed to a synchronous single transition). Drive is a `"request_navigation"` COMMAND (payload = `navigation_request`); `navigation_page_handler` hosts a plain NSView container and swaps the current page's `native_view()` subview (headless mirrors the hosted page); `navigation_page_platform` derives `view_platform_base`, handler chains `view_mapper()`. `set_handler` re-issues the request after wiring (C# OnHandlerChangedCore) so the attached container hosts the current page; the navigation_page fires the ROOT's initial send_appearing() itself (absent window lifecycle). Nav bar/back button/title bar, push/pop animation, modal stack, InsertPageBefore/RemovePage, Shell deferred. 15 headless + 5 apple GTest cases; self-registers. *characterization |
| navigation: bar + animation + modal + stack-edits (`navigation_page` chrome/`push_modal`/`pop_modal`/`modal_stack`/`insert_page_before`/`remove_page`; `i_stack_navigation` bar getters + `send_back_button_pressed`; `navigation_page_handler` custom bar + `request_modal_navigation`) | core + controls + core/handlers + platform/apple | ✅ | ✅* | ✅ | headless + macOS | M4d follow-up — completes the navigation deferrals. **Chrome:** `bar_title()` (current page Title) + `back_button_visible()` (depth > 1) + `send_back_button_pressed()` (→ pop, C# OnBackButtonPressed StackDepth>1 branch); folded onto `i_stack_navigation` so the core handler reads them. AppKit builds a CUSTOM bar (NSView + centered NSTextField title + a back NSButton using the system go-back chevron template image) above a content area; `host_current` populates it; the back button's target-action trampoline (`MauiNavBackTarget`, retained like button_handler) calls `send_back_button_pressed()`. **Animation:** `push/pop/pop_to_root(animated=true)` thread `NavigationRequest.Animated`; apple cross-fades the content swap via a CoreAnimation `CATransition`; headless synchronous; `navigation_finished` stays inline. **Modal:** `push_modal`/`pop_modal`/`modal_stack()` — a SEPARATE overlay stack; lifecycle ORDER ported from `NavigationModel.PushModal`/`PopModal` (previousVisible.Disappearing → new.Appearing; visible = top modal else nav current); a `"request_modal_navigation"` COMMAND drives the handler, which overlays the top modal's NSView on the whole container (an in-container overlay, NOT a child NSWindow — documented deviation) and removes it when the stack empties (the underlying content is never removed; `host_current` preserves a live overlay across content swaps). **Stack edits:** `insert_page_before`/`remove_page` (INavigation) port `MauiNavigationImpl.OnInsertPageBefore`/`OnRemovePage` — insert before a stack page (RootPage follows front()), remove the current page → `pop()`, inner removal fires no lifecycle; C#'s throws become no-ops (no exceptions in the seam). All commands reuse `navigation_request`; the platform mirrors (`bar_title`/`back_button_visible`/`last_animated`/`hosted_modal`) make the seam observable headless. 21 new headless + 6 new apple GTest cases (now 36 headless / 11 apple total for navigation). Self-registers. Deferred: Shell, bar styling/TitleView, Pushed/Popped/Navigated* events, overlapping-navigation queueing, hardware-back Page hook. *characterization |
| ViewMapper visuals: Background/Shadow/Clip (`paint`/`solid_paint`, `i_shadow`/`shadow`, `i_shape`/`rectangle`/`round_rectangle`/`ellipse`; `apple_visual_ops.hpp`) | graphics + core + controls + platform/apple | ✅ | ✅* | ✅ | headless + macOS | M4d Unit V — stands up the visual-layer value types `i_view` only forward-declared. `paint` + `solid_paint` (one color); `i_shadow` + `shadow` (radius/opacity/paint/offset; defaults 10/1/black/0); `i_shape` + `rectangle`/`round_rectangle`/`ellipse` (`path_for_bounds` via the existing `path_f` append_* helpers). `view_platform_base` gained background/shadow/clip mirrors + `update_background`/`update_shadow`/`update_clip`; `view_mapper()` keys background/shadow/clip; `view<>` makes them bindable (`property<shared_ptr<>>` + non-template descriptors). AppKit helpers `apply_background` (layer.backgroundColor) / `apply_shadow` (layer.shadow*) / `apply_clip` (path_f→CGPath→CAShapeLayer mask) in `apple_visual_ops.hpp`. **Retrofitted (M4d integration) onto all six platform structs** (button/label/entry/image/layout/content_page override `update_*`→the helpers; clip sized to NSView bounds; grid reuses layout_handler) so the visuals reach the native layer. Gradients/per-corner clip/Width/Height/Semantics deferred. 8 headless + 9 apple GTest cases. *characterization |
| gradient paints (`gradient_stop`, `gradient_paint` abstract, `linear_gradient_paint`, `radial_gradient_paint`; AppKit `CAGradientLayer`) | graphics + platform/apple | ✅ | ✅* | ✅ | headless + macOS | M4 backlog — the gradient brush kinds the M4d Background cut deferred. `gradient_stop` (offset/color value type, ordered by offset = PaintGradientStop.CompareTo); `gradient_paint : paint` (abstract) holds the stops + ports `GetColorAt` linear interpolation, StartColor/EndColor + StartColorIndex/EndColorIndex, GetSortedStops, IsTransparent (any stop alpha < 1), SetGradientStops/AddOffset/RemoveOffset/BlendStartAndEndColors; default stops white→white (empty restores it). `linear_gradient_paint` (start/end `point`, default (0,0)/(1,1)); `radial_gradient_paint` (center `point` + radius, default (0.5,0.5)/0.5). The GradientPaint-copy ctors leave start/end (or center/radius) at the C# struct default (Point.Zero/0), not the parameterless-ctor values — faithful to C#. `background_color()` returns the start/end blend (port decision: C#'s GradientPaint doesn't override BackgroundColor; this satisfies the abstract paint contract with a representative color). AppKit: `apply_background` (apple_visual_ops.hpp) now dynamic_casts to `linear`/`radial_gradient_paint` and installs a tagged `CAGradientLayer` sublayer (axial/radial `type`, `startPoint`/`endPoint` (radial endPoint = PaintExtensions GetRadialGradientPaintEndPoint, cornerRadius = radius), `colors[]`/`locations[]` from the sorted stops — ports GetCAGradientLayerColors incl. the transparent-stop neighbor-borrow + GetCAGradientLayerLocations incl. the even-spread fallback), sized to the backing-layer bounds; switching to solid/null removes it. The six platform structs are UNCHANGED (their `update_background` already routes through `apply_background`). The `ImagePaint`/`PatternPaint` *drawing-canvas* kinds are ported in unit U-IP (next row); the gradient view-Background path here is unaffected. 36 headless (gradient_paint_tests.cpp) + 8 apple (view_mapper_apple_tests.mm) GTest cases. *characterization (no C# unit-test oracle for these types). |
| **U-IP drawing-canvas image + pattern paints** (`image_paint`, `i_pattern`/`abstract_pattern`/`paint_pattern`/`pattern_paint`/`picture_pattern`/`i_picture`; coregraphics `FillWithImage`/`FillWithPattern`) | graphics + platform/apple_shared | ✅ | ✅* | ✅ | headless + macOS + iOS | The drawing-canvas (ICanvas Fill*) image/pattern paint family the gap audit flagged. **Abstractions** (`include/maui/graphics/`): `image_paint : paint` (non-owning `i_graphics_image*`; IsTransparent=false; BackgroundColor base default — `ImagePaint.cs`); `i_pattern` (width/height/step_x/step_y + draw(canvas) — `IPattern.cs`); `abstract_pattern : i_pattern` (the 3 ctors: full dims+steps / dims=steps / single step — `AbstractPattern.cs`); `paint_pattern : i_pattern` (forwards dims to Wrapped, 0 if null; draw primes canvas stroke/fill from the paint foreground, black if no paint, then draws Wrapped — the dead `BackgroundColor.Alpha>1` pre-fill is ported but inert — `PaintPattern.cs`); `pattern_paint : paint` (the `Pattern` setter wraps a non-paint_pattern value in an OWNED paint_pattern{Paint=this}, leaves an existing paint_pattern as-is; IsTransparent: nullable BackgroundColor (`std::optional<color>`) null OR alpha<1 → true, else ForegroundColor alpha<1 — `PatternPaint.cs`); `picture_pattern : abstract_pattern` (wraps `i_picture`, dims from the picture, draw replays it — `PicturePattern.cs`); `i_picture` (minimal draw + x/y/w/h — `IPicture.cs`). Base `paint` gained a virtual `foreground_color()` (default value-type color; only pattern_paint overrides) so PatternPaint's reads stay faithful. **coregraphics** (`coregraphics_canvas.mm`): `SetFillPaint` dynamic_casts to image_paint/pattern_paint and stages `fill_image_`/`fill_pattern_` (cleared by FillColor/Restore/Reset like C#); `FillWithImage` (CGPattern NoDistortion + Y-flip tile matrix, image tile callback blits the CGImage) + `FillWithPattern` (CGPattern ConstantSpacing, pattern tile callback re-enters a lazily-created nested coregraphics_canvas bound to the tile context and runs `pattern->draw`); every Fill* (rect/rounded-rect/ellipse/arc/path) dispatches gradient→pattern→image→solid (path uses the bounding-box origin as the phase, mirroring C#). **Ownership:** the image and the user's wrapped pattern are non-owning borrows (caller owns); pattern_paint owns the wrapper (unique_ptr, out-of-line ctor/dtor); the wrapper's Paint and the picture are non-owning back-/refs. **Scope boundary:** the ImageBrush-as-view-Background path is untouched (still deferred — C# iOS throws there). Tests: 17 headless (image_pattern_paint_tests.cpp — accessors, dims, wrapping, IsTransparent, draw golden ops, recorder integration) + 5 apple/ios CGBitmapContext pixel-readback (image fill tiles/upright, pattern fill tiles, set_fill_color clears a staged image). clang-tidy 0 from a clean build. *characterization (no C# unit-test oracle for these graphics types). |

| layout foundation (`dimension`, `i_container`/`i_layout`/`i_stack_layout`, `i_layout_manager`) | core/layouts | ✅ | — | ✅ | headless + macOS | `ILayout : IView + IContainer + IPadding + ClipsToBounds` (ISafeAreaView / ICrossPlatformLayout still deferred). `dimension` = Unset(NaN)/Minimum(0)/Maximum(inf) + is_explicit_set. M5d unit H: size requests on `view<>` (WidthRequest/HeightRequest/Min*/Max* → IView getters), bindable `z_index` + `order_by_z_index`/`get_layout_handler_index` (LayoutExtensions) + handler subview restacking, ClipsToBounds (→ Apple `masksToBounds`). |
| stack layout managers (`layout_manager`/`stack_layout_manager` + vertical/horizontal) | layouts | ✅ | ✅* | ✅ | headless + macOS | New `maui_layouts` lib — pure cross-platform measure/arrange (ResolveConstraints, MeasureSpacing, stacking). 28 GTest cases via mock view/stack (spacing / padding / min-max / collapsed-vs-hidden / fill / child-constraint). The layout *controls* + native panel are M4. *ported from C# Stack*LayoutManagerTests (the oracle) |
| Grid (`grid_length`/`grid_unit_type`, `i_grid_*` contracts, `grid_layout_manager`) | core + layouts | ✅ | ✅* | ✅ | headless + macOS | The full row/column algorithm: Absolute/Auto/Star sizing, row+column spans, spacing, padding, min/max, two-pass measure + arrange-time star expansion (pimpl grid_structure cached measure→arrange, like C# GridStructure). 19 GTest cases (representative subset of the large GridLayoutManagerTests). *ported from the C# oracle |
| layout controls (`i_layout_handler`, `layout<>` base, `vertical_stack_layout`/`horizontal_stack_layout`, `layout_handler`) | controls + core/handlers | ✅ | ✅* | ✅ | headless + macOS | M4b Unit 1 — wrap the M3 stack managers in controls + a native host panel. `layout<LayoutInterface>` owns the non-owning child list (i_container) + bindable padding, lazily builds its manager via `create_layout_manager()`, and overrides measure/arrange to delegate to the manager (a layout computes its own geometry); arrange also sizes the panel. `layout_handler` (i_layout_handler add/remove/clear/insert/update/update_z_index) hosts a plain NSView panel (headless: child-count mirror). `view_handler` derives `i_view_handler` *virtually* (layout_handler = view_handler + i_layout_handler); `i_view_handler::native_view()` exposes the hosted native view. Retrofitted onto `view_mapper()` (is_enabled keeps the base mirror — NSView has no enabled state). maui_layouts linked into maui_controls. Grid *control* deferred. 14 headless + 4 apple GTest cases; self-registers. *characterization |
| `entry` control (`i_text_input`/`i_entry`, `entry_handler`) | controls + core/handlers | ✅ | ✅* | ✅ | headless + macOS | M4b Unit 3 — first inbound-text + first editable native control. Bindable text/placeholder/is_password/is_read_only/max_length (UTF-8-byte truncation in set_text) + i_text_style appearance + alignment; `completed` + `text_changed(old,new)` events. Editable NSTextField (NSSecureTextField cell swap for password, preserving font/color) + an NSTextFieldDelegate trampoline (controlTextDidChange→send_text_changed, controlTextDidEndEditing→send_completed). Chains `view_mapper()`. **AppKit text polish + full editing surface (done):** character_spacing (kerned text + placeholder via `apple_text_ops.hpp`), vertical_text_alignment (custom vertical-aligning cells, plain + secure), placeholder_color (attributed-placeholder foreground). Entry editing widened — contract `i_text_input`/`i_entry` gained `ReturnType`/`ClearButtonVisibility` (`return_type`/`clear_button_visibility` enums) + `IsTextPredictionEnabled`/`IsSpellCheckEnabled` + the mutable `CursorPosition`/`SelectionLength` pair (inbound `set_cursor_position`/`set_selection_length`, clamped ≥ 0); headless mirrors all; AppKit applies prediction/spellcheck/cursor+selection on the field editor (when editing) and routes `NSTextViewDidChangeSelectionNotification` → the virtual view, with return_type/clear_button mirror-only on desktop. 19 headless + 13 apple GTest cases; self-registers. *characterization |
| `image` control (`aspect`/`i_image`, `image_handler`; aspect only) | controls + core/handlers | ✅ | ✅* | ✅ | headless + macOS | M4b Unit 3 (minimal) — maps ONLY the scaling mode: `aspect` (aspect_fit/aspect_fill/fill/center) → NSImageView.imageScaling (+ centered). No image bytes loaded — the async image *source* subsystem (IImageSource/loaders/caching) is deferred. Chains `view_mapper()`. 5 headless + 2 apple GTest cases; self-registers. *characterization |

| data binding (`binding_mode`, `bind()` + `binding_handle`; `bindable_property` default_binding_mode) | core | ✅ | ✅* | ✅ | headless | M5a — **typed accessor bindings** (reflection-free, the PROFILE §6 / code-first substitute for C#'s string-path + INotifyPropertyChanged). `bind(property<T>& target, property<U>& source, mode, convert, convert_back)` observes the source via the existing `property<T>.changed` and pushes at `setter_specificity::from_binding` (so a manual set still wins + clears restore the bound value); modes one_way/two_way (re-entrancy-guarded, no feedback loop)/one_time/one_way_to_source; `default_mode` resolves to the target's `default_binding_mode` (+ two_way→one_way_to_source on a read-only target); RAII `binding_handle` tears the subscriptions down. 8 GTest cases (every mode + converter + the precedence interaction + teardown). **BindingContext + tree inheritance folded into M5c** (shares the element-tree propagation). String paths / registered-accessor table / StringFormat / FallbackValue / MultiBinding / compiled bindings deferred to M7. *characterization |

| styles / setters / triggers / VSM (`setter`, `style`, `property_trigger<T>` + `trigger_handle`, `visual_state`/`visual_state_group`/`visual_state_manager`) | controls | ✅ | ✅* | ✅ | headless | M5b — apply/un-apply bundles of property values through the **existing** value-precedence ladder (`property<T>::set/clear(value, specificity)`). Foundation: each `property<T>` **self-registers** an erased `{apply(any, specificity), clear(specificity)}` handle with its `bindable_object` (keyed by descriptor name) so `setter::of(descriptor, value)` reaches a typed slot via `apply_setter`/`clear_setter` — value boxed at the boundary, **storage stays typed**. `style` = target type-tag + `vector<setter>` (+ a directly-assigned `based_on` applied at `setter_specificity::as_base_style`); `view<>` gains `set_style`/`style()` (apply at `style_local`, un-apply the previous). `property_trigger<T>` watches a `property<T>` via `.changed`, applies setters at `setter_specificity::trigger` (above a manual value) while it equals the target, RAII `trigger_handle` tears down + un-applies. `visual_state_manager::go_to_state` swaps the outgoing/incoming state setters at `visual_state_setter`. 15 GTest cases (style precedence ladder + replace + based_on; trigger apply/override/restore/teardown; VSM swap/idempotent/unknown/manual-interaction). Ported `SetterSpecificity.AsBaseStyle`. Implicit styles / ResourceDictionary / DynamicResource / the VSM implicit-style downgrade + system-state promotion / EventTrigger / MultiTrigger / StateTriggers deferred (see backlog). *characterization |

| Application / Window / element lifecycle + inherited BindingContext (`controls::element`, `window`, `application`; `bindable_object` BindingContext) | controls | ✅ | ✅* | ✅ | headless | M5c — a non-template `controls::element` re-roots `view<>` (mirroring C#'s BindableObject → Element → VisualElement), carrying the shared tree lifecycle: `for_each_logical_child` (containers — `content_page`/`layout<>`/`navigation_page` — override it), **typed inherited BindingContext** (held on `bindable_object` as `shared_ptr<void>` + a `type_tag` so `binding_context<X>()` is checked; an explicit set blocks inheritance; `on_binding_context_changed` propagates down), and the **Window back-ref + Loaded/Unloaded** (`set_containing_window` flows the window down the subtree, firing Loaded top-down / Unloaded bottom-up). `window` hosts one root page + the IWindow lifecycle (`send_created`/`activated`/`deactivated`/`destroying`); activating it Loads the page subtree + drives `content_page` Appearing (the windowed-appearing the M4d deviation lacked). `application` owns the windows + the one-time `started` hook (`open_window` starts+creates+activates). 13 GTest cases (BindingContext inherit/override/through-layout/typed-null; window activate/deactivate Loaded-Unloaded + subtree propagation; application open/close/start + context inheritance app→window→page). Native NSWindow `window_handler`, modal/multi-window, and string-path bindings deferred (see backlog). *characterization |

| styles / resources subsystem — resource_dictionary, implicit styles, DynamicResource, VSM downgrade, advanced triggers (`resource_dictionary`, `merged_style`; `element` resources/dynamic-resource; `setter_specificity::with_full_vsm_priority`/`visual_state_setter_system`/`style_class`; `setter::of_for`, `multi_trigger`, `data_trigger`) | controls + core | ✅ | ✅* | ✅ | headless | **M5d — the deferred styles/resources backlog.** `resource_dictionary` = a string-key → `std::any` store (typed `get<T>()`; the boundary-confined erasure `setter` already uses) + merged dictionaries (last-to-first lookup, **destruction-order-safe** bidirectional back-links — ASan-verified) + a `values_changed` event; implicit styles keyed by `implicit_style_key(type_tag)`, class styles under a class prefix. `controls::element` gains a lazy `resources()`, a `logical_parent_` (Element.Parent, set by attach/detach_logical_child), `try_get_resource` (walks self→ancestors), `set/remove_dynamic_resource` (re-applied at `dynamic_resource_setter` on a resource change or reparent — change propagates DOWN to children, a child's own key shadows a parent's), and a `merged_style` (MergedStyle) resolving the implicit (TargetType) + class styles layered implicit < class < local (`style_implicit` < `style_class` < `style_local`). `style` gains `base_resource_key` (BasedOn-by-key, resolved via a `resource_resolver` from the element's chain), `style_class`, and a resolver-aware apply/unapply; `view<>` re-applies a key-based local style when the chain changes (`on_resource_chain_changed`). **VSM restore:** re-added `setter_specificity::with_full_vsm_priority` + `visual_state_setter_system` (faithful `WithFullVsmPriority` + the StyleImplicit-sourced VSM specificity) + `style_class`; `visual_state_manager::mark_from_implicit_style()` downgrades a custom state below manual but `is_system_driven_state` promotes Disabled/Focused/… back above it (#18103/#34363). **Advanced triggers:** `setter::of_for` (Setter.TargetName retarget by pointer — no namescope), `trigger_action` + EnterActions/ExitActions on every trigger (shared `trigger_body`), `multi_trigger` (type-erased `i_trigger_condition` aggregate), `data_trigger<Context,Value>` (typed BindingContext condition, re-evaluated on `binding_context_changed`). Controls declare their implicit-style TargetType via `set_style_target_type<T>()` (button/label/entry/image/content_page/v+h stack layouts/grid/navigation_page). 40 new GTest cases (resource_dictionary / dynamic_resource / implicit_style / advanced_trigger + 3 VSM-downgrade cases); ASan/UBSan + TSan clean; clang-tidy 0. ApplyToDerivedTypes / CanCascade / Behaviors / StateTriggers / per-trigger index / VisualStateGroupsProperty storage deferred (see backlog). *characterization |

| **M6 iOS backend scaffold** + `button_handler` UIKit partial (`ios` preset/triplet/runner; `src/platform/ios/button_handler.mm`, `ios_conversions.hpp`, `ios_text_ops.hpp`) | platform/ios + build | ✅ | ✅* | ✅ | iOS simulator | **The iOS toolchain + seam proof, end-to-end, so per-control units can fan out.** **Build:** `MAUI_BACKEND=ios` compiles the button's real UIKit partial + every OTHER control's HEADLESS partial (pure C++ — the library links and the full controls suite stays meaningful; each fan-out unit swaps one partial+tests pair); `MAUI_PLATFORM_IOS` (PUBLIC, sibling of `MAUI_PLATFORM_APPLE`) selects the ios `update_*` override block on `button_platform`; links UIKit/QuartzCore + Foundation/CoreFoundation/libobjc explicitly (iOS's UIKit does not re-export them, unlike macOS's Cocoa umbrella); plain console executables (`CMAKE_MACOSX_BUNDLE OFF`). **Preset/triplet:** `CMAKE_SYSTEM_NAME=iOS` + `iphonesimulator` sysroot + arm64 + **deployment floor 26.0** (this SDK's libc++ stamps floating-point `std::from_chars`/`to_chars` — used by `std::format` + the graphics parsers — "introduced in iOS 26.0"; the installed 26.4 sim runtimes satisfy it); vcpkg `arm64-ios-simulator` via an **overlay triplet** (`cmake/triplets/`, wired by `VCPKG_OVERLAY_TRIPLETS`) pinning `VCPKG_OSX_DEPLOYMENT_TARGET` to match (gtest 1.17.0 + benchmark 1.9.5 build clean). **Runner (`tools/ios-sim-run.sh` = `CMAKE_CROSSCOMPILING_EMULATOR`):** boots the first available iPhone (`MAUI_IOS_SIM_UDID` overrides) and `simctl spawn`s the binary; gtest discovery is `PRE_TEST` w/ 180s timeout (a cross-compiled test can't run at build time; the 5s POST_BUILD default timed out); **TCC detour** — a simulator process touching a privacy-protected host folder (~/Documents…) hangs forever inside open(2) in headless sessions (no Files-and-Folders prompt), so the runner stages the binary in the per-user temp dir and redirects gtest discovery's `--gtest_output=json:` there, moving results back host-side. **Handler (ported 1:1 from ButtonHandler.iOS.cs + ButtonExtensions.cs):** `UIButton(UIButtonType.System)` + `SetControlPropertiesFromProxy` appearance seeding; ButtonEventProxy's four touch wirings (TouchDown→Pressed; TouchUpInside→Released+Clicked; TouchUpOutside/Cancel→Released) via an associated-object-retained trampoline; UpdateText (Normal title) + the MapText→MapFormatting refresh; UpdateTextColor (Normal/Highlighted/Disabled + tintColor; the TextColor==null branch collapsed — non-nullable color, as on AppKit); UpdateCharacterSpacing (kerned attributed Normal title via `ios_text_ops.hpp`, carrying the text color; spacing 0 → `setAttributedTitle:nil` restores the plain path); UpdateFont (titleLabel, `UIFont.buttonFontSize` default); UpdatePadding (NaN→DefaultPadding(12,7) → +borderWidth int-truncated → 0→AlmostZero → `contentEdgeInsets`, deprecated-but-functional exactly as C# uses it, under the same suppression); UpdateStrokeThickness/CornerRadius (`>= 0` guards) + UpdateStrokeColor on the layer; `sizeThatFits:` measure (inf→`CGFLOAT_MAX`); `setFrame:` arrange; ios `update_visibility/opacity/is_enabled/automation_id` push to the UIButton. **Tests:** 15 on-simulator seam cases (`button_ios_tests.mm`) — titles, all four touch flows (+`is_pressed`), disabled gating (native `enabled` + the control's gate), disconnect, appearance, 4 kerning cases (incl. the re-kern path), contentEdgeInsets padding (AlmostZero + border add), the generic-IView pushes, registry resolution; the ios `maui_controls_tests` swaps ONLY `button_tests.cpp`/`view_mapper_tests.cpp` out for it (their assertions read the headless button mirrors). **Event-injection caveat** (documented in the test header): UIControl action dispatch relays through `UIApplication.sharedApplication`, which cannot exist in a spawned bundle-less process on iOS 26 (`[[UIApplication alloc] init]` throws, bundle-less `UIApplicationMain` traps — both probed on-simulator), so the test replicates UIControl's documented dispatch-table walk (`allTargets` × `actionsForTarget:forControlEvent:` → `NSInvocation`); the sole unexercised hop is UIApplication's one-line relay. **Deferred to the M6 fan-out:** the other controls' UIKit partials (label/entry/image/layout/content_page/navigation_page/window) + their `*_ios_tests.mm` + a `view_mapper_ios_tests.mm`; the shared ios view/visual/semantics op helpers (button's transform/flow_direction/background/shadow/clip/semantics/input_transparent keep the base mirrors for now); button MapImageSource; NeedsContainer/WrapperView; the Mac-Catalyst `UIButtonConfiguration` branches; an app-bundle test-host lane (true UIApplication dispatch + UIWindow tests); a UIKit sample app. *characterization |
| **M7 wave 1 — XAML node tree + parser** (`maui_xaml` lib: `xaml_node.hpp/.cpp`, `xaml_parser.hpp/.cpp`, `tests/xaml/parser_tests.cpp`; new `pugixml` vcpkg dep) | xaml | ✅ | ✅* | ✅ | headless (+ builds on apple) | Ported from `XamlNode.cs`/`XmlName.cs`/`XmlType.cs`/`XamlNodeVisitor.cs` + `XamlParser.cs`'s parse half (incl. the `XamlLoader` root loop, `TypeArgumentsParser`, the `XmlnsHelper.targetPlatform` prune, and `MarkupExpressionParser`'s string TOKENIZATION — `match_markup`/`get_next_piece`/`parse_markup_name`; extension RESOLUTION is wave 2). Node family: `i_xaml_node` (INode+BaseNode merged; raw non-owning parent backref), `value_node` (+IsEscaped), `markup_node` (unparsed `{…}`), `element_node` (insertion-ordered `xml_name_map` properties + collection_items + skip_properties), concrete `root_node` (+warnings), `list_node`; the visitor seam (`tree_visiting_mode` TopDown/BottomUp, StopOnDataTemplate/VisitNodeOnDataTemplate/StopOnResourceDictionary knobs, `xaml_node_visitor` delegate adapter, `clone()`, `skip_prefix`). Parser walks a pugixml DOM mirroring the XmlReader loop decision-for-decision: xmlns scoping as per-node immutable resolver snapshots (`xml_namespace_resolver`); property-element vs attached-BP naming; x:Name/x:Key/x:Class/x:TypeArguments/x:Arguments/`_CreateContent` directives; `{}` escape + `{…}` markup detection (GetValueNode, incl. the trim/substring quirk); text/CDATA trim+merge; duplicate-property / duplicate-x:Arguments / multiple-template-children errors as `xaml_parse_exception` ("Position L:P. msg" like C#); 1-based line info from pugixml `offset_debug`. **60 tests** (tree shapes from FindByName/StringLiterals/DataTemplate/GenericsTests xaml + visitor ordering + tokenizer + error paths); headless 676 green, apple preset builds + xaml tests green. **Documented deviations:** value_node is string-typed in wave 1 (x:TypeArguments keeps the raw string in the node; the parsed list lives on the element's `xml_type`); attribute / list_node line info = owning-element / start-tag position (pugixml exposes no attribute or end-tag offsets); invalid type names throw eagerly (C# stores a null XmlType and NREs later); NameScopeRef + IsOnPlatformDefaultValue deferred to wave 2; `parse_options.target_platform` is the DeviceInfo.Platform seam. *parse-shape characterization |

| **M6 iOS containers** — `layout_handler` / `content_page_handler` / `navigation_page_handler` UIKit partials + the shared ios op headers (`src/platform/ios/{layout,content_page,navigation_page}_handler.mm`, `ios_visual_ops.hpp`, `ios_semantics_ops.hpp`) | platform/ios | ✅ | ✅* | ✅ | iOS simulator | **The container trio on real UIViews + the FOUNDATIONAL shared iOS op headers for the per-control retrofit.** **`ios_visual_ops.hpp`** ports the iOS C# originals DIRECTLY (the apple twin had adapted them): `apply_background` = PaintExtensions.iOS.cs (solid → layer backgroundColor; linear/radial → tagged CAGradientLayer sublayer with GetCAGradientLayerColors' transparent-stop borrow + GetCAGradientLayerLocations' even-spread + GetRadialGradientPaintEndPoint clamp; null clears both), `apply_shadow` = ShadowExtensions (radius/2, clear-on-null), `apply_clip` = WrapperView.SetClip (CAShapeLayer mask via the same faithful AsCGPath walk as apple), all simpler than AppKit (UIView is always layer-backed — no wantsLayer). **`ios_semantics_ops.hpp`** ports SemanticExtensions.UpdateSemantics(UIView,…) directly — accessibilityLabel/Hint (port's empty string = C# null → nil, preserving UIKit fallbacks), the IsNullOrWhiteSpace element marking with the UIControl carve-out (UIStepper/UIPageControl re-marked), UIAccessibilityTraitHeader keyed on hasHeader — and ViewExtensions.UpdateInputTransparent's general branch (`userInteractionEnabled = !value`; UIKit has it natively, unlike the AppKit -hitTest: gate). **Handlers** (LayoutHandler.iOS.cs / ContentViewHandler.iOS.cs): plain-UIView panel/host (LayoutView/ContentView's CrossPlatformLayout collapses — the port's manager drives layout); add/insert at GetLayoutHandlerIndex via the NATIVE insertSubview:atIndex: (clamped), EnsureZIndexOrder restack, snapshot ClearSubviews, ClipsToBounds = the REAL UIView property; set_content = ClearSubviews + re-parent. **navigation_page**: the apple twin's custom bar translated to UIKit (UILabel title + system-chevron UIButton back wired by a retained TouchUpInside proxy, bar at y=0 top-left origin, CATransition cross-fade, full-container modal overlay, Bar*Color/TitleView styling) — UINavigationController integration deliberately NOT attempted (documented deviation; the handler seam stays ours). All three platform structs override visibility/opacity/automation_id + background/shadow/clip/semantics/input_transparent (transform/flow_direction keep base mirrors until the shared ios view-ops arrive). CMake: the three partials + grid/layout/content_page/navigation_page tests swapped to `*_ios_tests.mm` (grid reuses layout_handler, so its headless-mirror tests had to swap too; children in ios tests are buttons — label/entry are still headless); CoreGraphics linked explicitly (UIKit does not re-export the CGPath/CGColor C functions). **Tests:** 33 on-simulator cases — subview hosting/z-restack/clipsToBounds/arrange for the panel + grid, content re-host/swap/clear, nav push/pop/bar title/back-tap-pops (UIControl dispatch-walk injection)/modal overlay (incl. navigate-under-modal)/arrange/bar styling/TitleView, plus the ops-header grammar (gradients install/replace/borrow/spread, shadow set+clear, clip mask set+remove, semantics traits/no-op/null-safety, input-transparent). Full ios suite 537 green; headless 616 / apple 531 unaffected. *characterization |

| **M-L hosting / app builder** — `maui_hosting` lib: `maui_app_builder`/`maui_app`, `i_maui_handlers_collection` + `maui_handlers_collection`, lifecycle events (`i_lifecycle_builder`/`i_lifecycle_event_service`/`lifecycle_event_service` + the window bridge + apple/ios shells), `maui_context`, **GCD dispatcher** (`gcd_dispatcher` + `src/platform/apple_shared/gcd_dispatcher.mm`) | hosting + core/platform | ✅ | ✅* | ✅ | headless + macOS + iOS simulator | **The builder boot (Layer 8).** `maui_app_builder` (<= MauiAppBuilder + UseMauiApp/ConfigureMauiHandlers/ConfigureLifecycleEvents/ConfigureDispatching): ctor seeds the default controls handler table (`add_maui_controls_handlers` — the explicit AddControlsHandlers list, per-build registry isolation), `use_maui_app<TApp>()` mints the `controls::application` subclass (default-ctor + factory overloads), `configure_handlers` callbacks replay in order against a fresh `handler_registry` through the `i_maui_handlers_collection` facade (erased add/try_add/is_registered primitives + typed AddHandler/TryAddHandler sugar; `handler_registry` gained the `register_factory` primitive — layered, not forked), `configure_lifecycle_events` collects `lifecycle_event_registration`s, `build()` (one-shot, throws on reuse) assembles `maui_app` (<= MauiApp): owns the service/handler registries, the dispatcher, the `lifecycle_event_service`, the application (registered as the `i_application` singleton), and a `maui_context` (<= MauiContext). Extras: `open_window` bridges the EXISTING window events (created/activated/deactivated/destroying/resumed/stopped/backgrounding) into the lifecycle registry under `window_lifecycle_events::*` names BEFORE Application.OpenWindow drives them (delegates receive the `controls::window&`; `close_window` unbridges), and `attach_handler` (<= ElementExtensions.ToHandler) resolves from the built registry + threads the context (SetMauiContext before SetVirtualView) + view-owns-handler. Lifecycle registry mirrors LifecycleEventService: `std::any`-erased std::function delegates (boundary-confined erasure), `get_event_delegates<TDelegate>` = the OfType filter, `invoke_events` helpers; per-platform REGISTRATION shells `add_apple`/`add_ios` + fluent builders (invoke side = the platform app delegate; the ios sample drives `finished_launching`). **GCD dispatcher:** `gcd_dispatcher` (<= Dispatcher.iOS.cs) — ONE ARC .mm shared by the apple+ios source lists; dispatch/dispatch_delayed onto `dispatch_get_main_queue` (move-only action boxed for the block), `is_dispatch_required` via `pthread_main_np`, timer = ONE repeating `dispatch_source` mirroring C# OnTimerTick at delivery (tick→Stop when !IsRepeating; set_interval re-arms a running timer — documented deviation vs C#'s next-reschedule pickup); `build()` mints it on apple/ios, `manual_dispatcher` on headless (`use_dispatcher` overrides). Samples REWIRED to builder boot (button_sample/maui_app_sample/ios_app_sample: use_maui_app<sample_app> owning the tree + attach_handler + open_window + lifecycle logging; os_log + exception-guarded main, lint-clean). **Out of scope (documented in maui_app_builder.hpp):** the .NET host machinery — IHostApplicationBuilder/Configuration/Logging/Metrics/Properties/ConfigureContainer/IMauiInitializeService. Tests: 16 `app_builder_tests.cpp` + 13 `lifecycle_events_tests.cpp` (maui_hosting_tests, headless+apple, wired like maui_xaml_tests) + 11 `gcd_dispatcher_tests.mm` (ONE file in maui_core_tests on apple AND on-simulator ios — pumped CFRunLoop). Headless 775 / apple 702 / ios 549 green; tidy 0; new .mm lint-clean vs both compile DBs. *characterization |

| **M7 wave 2 — XAML visitors + loader** (`xaml_visitors.hpp/.cpp`, `xaml_loader.hpp/.cpp`, `xaml_binding_applier.hpp/.cpp`; `tests/xaml/{visitor,loader}_tests.cpp`) | xaml | ✅ | ✅* | ✅ | headless (+ builds on apple/ios) | W1-01-finish: the seven-pass pipeline (ExpandMarkups/PruneIgnoredNodes/Namescoping/CreateValues/RegisterXNames/FillResourceDictionaries/ApplyProperties) + `xaml_loader::load`/`load_into` → `xaml_load_result` (graph owner + root name_scope + keep-alive + theme subscriptions) + the replaceable `{Binding}` applier hook (default = loud reject; the runtime-binding unit registers the real one). 54 new tests (LoaderTests/NameScopeTests/MarkupExtensionTests ports). Deviations (loud failures): attached BPs, events, x:Arguments/FactoryMethod, templates/styles-in-XAML, {x:Null}, custom collection types; details in the headers. |

| **M7 — binding bridge stitch** (`xaml_runtime_bindings.hpp/.cpp` — `register_runtime_bindings()`; `markup_extensions` {Binding} factory now BUILDS a `maui::controls::binding`) | xaml | ✅ | ✅* | ✅ | headless (+ builds on apple/ios) | The W1-02 runtime engine wired into the XAML applier hook: `binding_request` now carries the built `binding_base` `instance` (BindingExtension.ProvideValue = `new Binding(Path, Mode, Converter, ConverterParameter, StringFormat, Source)`), and `register_runtime_bindings()` (explicit, PROFILE §6) installs the TrySetBinding port — resolve the attribute via `xaml_property_registry`, route through `element::set_binding(bindable_name, instance)`; non-bindable/non-element targets fail with TrySetPropertyValue's "Cannot assign property …" catch-all. Converter/Source must arrive PRE-RESOLVED (nested {StaticResource}/{x:Static}; raw strings reject loudly — no string→instance lookup); Source additionally requires the walkable `shared_ptr<bindable_object>` box (C# takes any object — documented deviation); UpdateSourceEventName stays a loud deferral. The formerly-deferred LoaderTests go LIVE: TestSetBindingToBindableProperty / TestBindingPath / TestBindingModeAndConverter (code-seeded ReverseConverter resource — custom non-element types are not XAML-creatable) / TestSetBindingToNonBindablePropertyShouldThrow (Window.Title). 10 new/updated GTest cases (5 loader e2e + extension StringFormat/Converter/ConverterParameter/Source/TargetNullValue/FallbackValue). *characterization |

| **M7 — converter parity** (`xaml_converters` + `convert_easing`/`convert_text_decorations`/`convert_clear_button_visibility`; `register_standard_xaml_converters` now seeds the FULL table) | xaml | ✅ | ✅* | ✅ | headless (+ builds on apple/ios) | Closing the C# TypeConverter audit for PORTED types: `convert_easing` (EasingTypeConverter.cs — eleven named easings, case-insensitive, "Easing." qualifier; **deviation:** C# returns a NULL Easing for empty/whitespace, the port's value-typed easing throws), `convert_text_decorations` (TextDecorationConverter — ','/' ' split keeping empties, case-insensitive [Flags] names + the UNtrimmed "line-through" alias), `convert_clear_button_visibility` (the generic Enum.Parse path). `register_standard_xaml_converters` registers the WHOLE xaml_converters.hpp table (color/point/rect/size/size_f/thickness/corner_radius/grid_length/row+column definitions/layout_alignment/easing/float + all enum tables) through a `registry_converter` adapter (xaml_convert_error → xaml_parse_exception, C# ConvertTo's single error), replacing the duplicated local built-ins — every registered enum/struct attribute is now markup-text settable (loader e2e test). **Deferral rows:** KeyboardTypeConverter / FlexEnumsConverters / SafeAreaEdgesTypeConverter / ShadowTypeConverter — their types (Keyboard, the flex enums, safe_area_regions, Shadow) are unported; DateTime/TimeSpan/Uri/ListString/Type/Reference converters wait on their consumers; per-PROPERTY converters (FontSizeConverter named sizes, VisualElement.VisibilityConverter's "visible"/"hidden" bool aliases) need a per-property converter override channel the type-keyed registry lacks — `convert_font_size`/`convert_is_visible` stay free functions. Oracles: EasingTests.cs (Core.UnitTests) + TextDecorationUnitTests.cs. 7 new GTest cases + registry/loader coverage. *characterization |

| **W1-15 styles tail** — `behavior.hpp/.cpp` (Behavior/Behavior<T>/AttachedCollection → `behavior`/`typed_behavior<T>`/`behavior_collection`, `view<>::behaviors()`), `event_trigger.hpp` (+`trigger_action_base`/`typed_trigger_action<T>`) over the NEW named-event registrar on `element` (`register_named_event`/`connect_named_event` — the reflection-free GetRuntimeEvent substitute; button registers clicked/pressed/released), `state_trigger.hpp/.cpp` (StateTriggerBase + StateTrigger/CompareStateTrigger<T>/DeviceStateTrigger/OrientationStateTrigger/AdaptiveTrigger over the essentials facades), VSM attached-storage face (`view<>::set_visual_state_groups` → `visual_state_manager::replace_from` with the old-current-state unapply, trigger wiring, loaded/unloaded-driven SendAttached/SendDetached, `VisualStateGroup.GetActiveTrigger` + conflict scoring), Style.ApplyToDerivedTypes (`style::apply_to_derived_types`/`can_be_applied_to` + the DECLARED base-type chain `set_style_target_type<TControl, TBases...>` walked by merged_style for implicit AND class styles) | controls | ✅ | ✅* | ✅ | headless (+ apple/ios) | Ported from Behavior.cs/AttachedCollection.cs/EventTrigger.cs/TriggerAction.cs, StateTriggerBase.cs + the four concrete triggers, VisualStateManager.cs (VisualStateGroupsPropertyChanged/UpdateStateTriggers/ResolveStateTriggersConflict), Style.CanBeAppliedTo/MergedStyle.OnImplicitStyleChanged. Oracle tests: BehaviorTest/EventTriggerTest/StateTriggerTests/AdaptiveTriggerTests/OrientationStateTriggerTests/DeviceStateTriggerTests/StyleTests(derived-types). 32 new GTest cases. `maui_controls` now links `maui_essentials` (C# Controls→Essentials direction). Documented substitutes: type guards/casts → dynamic_cast + refuse-not-throw; base-type chain is DECLARED (no reflection); compare trigger typed (no Convert.ChangeType); trigger attach strictly window-driven (like C#); replace-groups detaches old triggers deterministically (§8). Still deferred: Behaviors/Triggers ON a Style, CanCascade, VSM Clone + duplicate-name validation, per-trigger index ordering. *characterization |

| **W1-07 containers — native half** — apple/ios `border_handler.mm` + `scroll_view_handler.mm` (+ `apple_border_ops.hpp`/`ios_border_ops.hpp`), `containers_page` demo | core/handlers + samples | ✅ | ✅* | ✅ | macOS + iOS (headless landed with the W1-07 verticals) | Border: the MauiCALayer recipe over STOCK layers — shape mask (apply_clip) + tagged CAShapeLayer stroke at 2× thickness (the mask cuts the outer half), thickness-scaled dash/phase/miter; the frame facade rides it. ScrollView: NSScrollView documentView + clip-origin scroll_to with the bounds-change write-back (AppKit collapse: animated completes synchronously) / UIScrollView ContentTag content + ContentSize pushed from the unbounded-arranged content + delegate write-back (ScrollViewHandler.iOS.cs / MauiScrollView.cs verbatim). content_view rides the content_page partials. 10 apple + 10 ios seam cases + 4 containers_page cases (every preset). Also: two iOS 26.5-environment fixes — `essentials_app_actions.mm` userInfo `description` via NSObject, and link-embedded keychain entitlements (`tools/ios-sim-entitlements.plist`) for SecItem* on the new sim runtime. *characterization |

| **W2-22 drag & drop** — `data_package.hpp/.cpp` (DataPackage + DataPackageView + DataPackagePropertySet[View] + `data_package_operation`), `drag_gesture_recognizer.hpp` (+ `drag_starting_event_args` / `drop_completed_event_args`), `drop_gesture_recognizer.hpp` (+ `drag_event_args` / `drop_event_args`), `drag_drop_data.hpp/.cpp` (the cross-control text/image seam), `drag_drop_recognizers.cpp` (send_* bodies), native attach in the apple/ios `gesture_platform_manager.mm` | controls (gestures) | ✅ | ✅* | ✅ | headless + macOS + iOS simulator | Ported from `DragGestureRecognizer.cs` (CanDrag; SendDragStarting — the DragSource stamp, the Cancel/Handled short-circuit, the GetStringValue text fallback; SendDropCompleted's _isDragActive once-per-drag latch) + `DropGestureRecognizer.cs` (AllowDrop; SendDragOver/SendDragLeave; SendDrop — the AllowDrop guard, the TrySetValue text + Image.Source injection onto the target, the Handled short-circuit, the DragSource text fallback) + the DataPackage cluster (Text/Image[i_image_source]/Properties[std::any bag] + the View snapshot independence via Clone) + the event-arg family. Recognizers extend the existing gesture family (added to `view<>.gesture_recognizers()`, diff-synced by the gesture_platform_manager). Native attach is ATTACHMENT-ONLY: ios installs/removes UIDragInteraction/UIDropInteraction (MauiDragDropDelegate); apple registers/unregisters NSDraggingDestination types + a drag-source flag — a synthetic dragging SESSION is not drivable headlessly or in the spawned sim lane, so the native tests assert install/remove via `native_registered_drag_source/drop_target` (the documented no-synthetic-session deviation; the full session→Send* bridge is device-test territory, pinned headlessly by the recognizers). Oracle tests: DataPackageTests / DragGestureRecognizerTests / DropGestureRecognizerTests. 29 headless (`drag_drop_tests.cpp`) + 4 apple (`gesture_apple_tests.mm`) + 3 ios (`gesture_ios_tests.mm`) cases. **Documented deviations:** Command/CommandParameter not ported (no ICommand port yet — like the other recognizers), so the command-fired oracle assertions are dropped (the event-raise + data-package plumbing they share is pinned); GetPosition(relativeTo)/PlatformArgs dropped (no headless coordinate seam, no native session); the drag-side image AUTO-extraction (Data.Image ??= source.Source) is omitted (the image controls expose only a borrowed i_image_source*, not the shared_ptr the package needs; a handler-set image is unaffected); the TimePicker/DatePicker text seam is omitted (it needs .NET culture-default DateTime/TimeSpan ToString/TryParse, a .NET-specific facility the port does not reproduce — the Label/Entry/Editor + Switch/RadioButton/CheckBox rows ARE covered), and the Button.ImageSource drop target (button has no image source in the port). *characterization |

| **W2-23 shapes + graphics_view — native half** — apple/ios `graphics_host.{hpp,mm}` (the shared `PlatformGraphicsView` recipe: a flipped NSView / transparent UIView whose `drawRect` builds the W1-13 `coregraphics_canvas` over the current `CGContext` and replays the borrowed drawable) + `graphics_view_handler.mm` + `shape_view_handler.mm` per backend | core/handlers + platform/apple,ios | ✅ | ✅ | ✅ | macOS + iOS simulator (headless landed with the merged W2-23 core, da6797742d) | The two handlers share ONE drawing host (C#'s `PlatformTouchGraphicsView`/`MauiShapeView` are both `PlatformGraphicsView` subclasses): `graphics_view` points the host at `VirtualView.Drawable`; `shape_view` points it at the platform's own `shape_drawable` (the `MauiShapeView.Drawable = new ShapeDrawable(...)` recipe). `update_drawable`/`invalidate_drawable` + `update_shape`/`invalidate_shape` keep the portable mirrors (drawable borrow + invalidation count) current beside the native `setNeedsDisplay` push, so the seam tests read the same surface as headless; the generic-IView `update_*` route through the existing `apple_view_ops`/`apple_visual_ops`/`apple_semantics_ops` (+ ios twins), is_enabled keeping the base mirror (a plain NSView/UIView host). Ownership: `create_drawable_host` returns a `__bridge_retained` host; the platform dtor `CFRelease`s it. Native tests do a `CGBitmapContext` render + pixel readback (the coregraphics_canvas precedent): apple via `displayRectIgnoringOpacity:inContext:`, ios via `layer renderInContext:` — a red drawable fill and a blue-filled/red-stroked rectangle put the right ink through the CG stack; an empty host stays transparent. 5 apple + 4 ios native cases (`{graphics_view,shape_view}_{apple,ios}_tests.mm`); apple 1981 / ios 1702 green, clang-tidy 0. **Deviation:** the native gesture/touch channel (C#'s `PlatformTouchGraphicsView` Start/Drag/EndInteraction) is deferred with the shared gesture seam — the hosts draw only (the interaction channel is portable and unchanged). |

| **W2-24 platform-configuration (full)** — the `IElementConfiguration`/`Configuration` MECHANISM + ALL SIX `*Specific` knob sets, stored, with a WIRED-REAL subset driving native UIKit/AppKit. Mechanism: `element::on<TPlatform>()` (deducing-this → `config<platform_tag, TElement>`), the six `i_config_platform` marker tags (iOS/macOS/Android/Windows/Tizen/GTK), and an element-side attached `platform_specs_` store (`set/has/platform_spec`, namespaced keys `"<platform>.<DeclaringClass>.<Prop>"`, SetValue equality short-circuit + property_changed) in ONE additive `// --- platform configuration (W2-24) ---` block in `element.hpp`. Each C# `*Specific` static class → a free-function knob set (`get_x`/`set_x` + config-chaining overloads) under `include/maui/controls/platform_configuration/` over the matching PORTED control. Wired-real (mapper-driven, real native): iOSSpecific `NavigationPage.IsNavigationBarTranslucent` (UINavigationBar blur backdrop + content-under-bar), `Page.PrefersStatusBarHidden`/`PreferredStatusBarUpdateAnimation`/`UseSafeArea`(+SafeAreaInsets write-back via `MauiIosPageView.safeAreaInsetsDidChange`)/`PrefersHomeIndicatorAutoHidden` (root `UIViewController` overrides + `setNeeds*` nudges), `Entry.CursorColor` (`UITextField.tintColor`). New Core side-contracts (the reflection-free remap seam, Core mapper tables stay Core-owned): `i_ios_entry_specifics`, `i_ios_page_specifics`, `i_safe_area_view`(+2). macOSSpecific window/page/tabbed knobs stored; AndroidSpecific stored-inert (until JNI fan-out); Windows/Tizen/GTK inert. | controls + core (+ platform/apple,ios,headless) | ✅ | ✅ | ✅ | headless + macOS + iOS simulator | Ported from `src/Controls/src/Core/PlatformConfiguration/**` (Configuration.cs + IElementConfiguration + all `*Specific` namespaces) and `PlatformSpecificsTests.cs` (the 5 mechanism tests, incl. the open-vendor knob). Oracle quirks ported: the Page parent-redirect (all three `IiOSPageSpecifics` getters consult a parent page that has the home-indicator key set), the verbatim status-bar-animation switch (0→Fade/1→Slide/else None, disagrees w/ the enum order), Tizen one-shot NextFocusDirection, AndroidSpecific TabbedPage `OffscreenPageLimit` validateValue throw + bottom-placement MaxItemCount cap. The `MauiView.AdjustForSafeArea` analog folds the realized insets into content_page measure/arrange. 44 platform_configuration GTest cases (`platform_configuration_tests.cpp`, backend-agnostic — the `platform_configuration_wired` suite's 7 cross-platform-seam mirrors run on every backend; the iOS twin drives real UIKit, pinned by the on-sim full suite). **Deviations:** C#'s `PlatformConfigurationRegistry` per-platform cache dropped (config<> is a stateless value accessor — observably identical, reference-identity only); knobs over UNPORTED controls are deferred with this row; the per-control native `*_ios_tests.mm` on-sim unit referenced in a comment was not split out (the wired seam is covered by the cross-platform `platform_configuration_wired` suite running on the iOS backend + the existing per-control on-sim tests). |

| **W2-21 shell model + routing (headless)** — the Shell item tree (`shell`/`shell_item`/`shell_section`/`shell_content`/`base_shell_item`/`shell_group_item`) with implicit-wrapping factories (`adopt`/`create_from_shell_section`/`create_from_shell_content` — no implicit conversions, PROFILE §6), Title/Icon/Route/IsEnabled/FlyoutItemIsVisible, the CurrentItem/CurrentSection/CurrentContent chain, and `ContentTemplate` lazy page creation; routing (`routing` + `shell_uri`/`shell_uri_handler` + `route_request_builder` + `request_definition`): route registration/validation (implicit IMPL_ routes), the absolute/relative/`../`/query URI parser, the `i_query_attributable` + `apply_query_attributes` hook, and `GetNavigationRequest` ported 1:1 (NodeLocation walk, GenerateRoutePaths/SearchPath/ExpandOutGlobalRoutes/GetBestMatches, CollapsePath); navigation (`shell_navigation_manager` + `shell_navigating`/`shell_navigated_event_args` + `shell_navigating_deferral` + `shell_navigation_state`): go_to_async(state, animate), Navigating with GetDeferral/Complete/cancel, Navigated (accumulate + OnAppearing gate), per-kind stack mutation, CurrentState composition; `shell` control wires 1-3 + FlyoutBehavior/FlyoutIsPresented model state. | controls (shell) | ✅ | ✅* | ✅ | headless | **HEADLESS-ONLY** (native shell chrome is a later wave; library still compiles through on apple/ios). Behavior derived from `ShellUriHandlerTests.cs` + `ShellNavigatingTests.cs` (the gold oracle), ported extensively — **81 GTest cases** (`shell_tests.cpp` / `shell_uri_handler_tests.cpp` / `shell_navigating_tests.cpp` + `shell_test_base.hpp`); ASan/UBSan clean; clang-tidy 0. **No native handler this unit** (wave-4 chrome). **Deviations:** `Navigated` fires synchronously through the content's OnAppearing (no Window-attach gate — the headless shell has no window; `BaseShellItem.OnAppearing`'s Navigation.NavigationStack delegation IS ported so a pop's Navigated still fires when the content has disappeared); no modal stack (the modal branches of GoToAsync collapse away); §8 — `ShellSection.OnRemovePage` disconnects the handler + holds a keep-alive strong ref before dropping the section's page retention (C# leans on the GC). *characterization |

| **W3-31 table_view + cells** — the obsolete-but-ported Cell family (`cells/cell.hpp/.cpp` base over `element` + `i_cell_container`; `text_cell`/`switch_cell`/`entry_cell`/`image_cell`(over text_cell)/`view_cell`) + the TableView model layer (`table_section_base`/`table_section`/`table_root`/`table_model`) + the `table_view` control (over `i_table_view`) + `table_view_handler` with a REAL UITableView (ios) / NSTableView (apple) datasource+delegate (cell reuse + selection) and a headless row-realization simulator | controls (+ platform/headless,apple,ios) | ✅ | ✅* | ✅ | headless + macOS + iOS simulator | Ported from `Cells/Cell.cs` + `TextCell`/`SwitchCell`/`EntryCell`/`ImageCell`/`ViewCell`.cs, `TableView/TableView.cs` (+ its `TableSectionModel`) + `TableModel`/`TableSectionBase`/`TableRoot`/`TableSection`.cs, and the `TableViewRenderer` behavior. Cell: DefaultCellHeight/Height/IsEnabled(re-raises HasContextActions)/RenderHeight(walks the table parent's HasUnevenRows+RowHeight via `i_cell_container`)/Tapped+OnTapped/Appearing+Disappearing+Send*/ContextActions(owned menu_items, parented+context-flowed)/ForceUpdateSize(rate-limited + uneven-rows gated). text_cell command = the port's callable + `command_can_execute` predicate (button/check_box convention) driving IsEnabled (the ICommand-CanExecute oracle cases). table_view: Root(empty default)/RowHeight/HasUnevenRows/Intent/Model/ModelChanged, the Root setter parenting every cell (cell.Parent=table) + hooking section_collection_changed + section-Title bubbling, OnBindingContextChanged→Root. Handler: a real grouped UITableView with a UITableViewDataSource/Delegate (numberOfSections/Rows/titleForHeader, cellForRow DEQUEUES by reuse id → realize/reuse record, didSelectRow→model.RowSelected taps the cell) and a flattened view-based NSTableView (makeViewWithIdentifier reuse, selection notification → model); the headless simulator mirrors realize/reuse/selection into the same platform record the .mm twins fill, so all three presets assert one oracle. **Tests:** ~43 backend-agnostic (`cells_tests.cpp` + `table_view_tests.cpp`) on every preset + 4 real-native seam cases each (`table_view_apple_tests.mm` / `table_view_ios_tests.mm`). **DEVIATIONS:** (1) **ListView DEFERRED → CollectionView (W3-29)** — its `TemplatedItemsList` (grouping/header-footer templating/caching-strategy/recycle) is the gnarliest single oracle file and CollectionView already covers the modern replacement (pre-authorized descope). (2) TableView's FillAndExpand layout-options (no LayoutOptions surface in the port — the constructor's only port-skipped line); the layout-options assertions in TestConstructor are skipped. (3) ImageCell's BindingContext→ImageSource propagation + Cancel-on-disappear have no analog (the port's image sources are plain interfaces, not bindable_object). (4) entry_cell.Keyboard not ported (no Keyboard type, as `entry`). (5) Cell.Handler?.Invoke + the 16ms ForceUpdateSize debounce: the cell is a plain element (no own handler — the table realizes it), so the `force_update_size_requested` event IS the seam and the queued flag resets immediately (no UI thread). (6) Native rows host the cell's primary text + realize/reuse/selection; the live per-cell native editors (embedded UISwitch/UITextField) + native section group rows are deferred. *characterization |
| **W3-33 carousel_view + indicator_view** — `carousel_view` (`controls/items/carousel_view.hpp/.cpp`, `<= CarouselView`) over the W3-29 items infra: derives `structured_items_view` (so it reuses the existing ItemsLayout face + the `collection_view_handler` virtualization seam — the headless fake-viewport simulator + the iOS Items2 compositional UICollectionView from W3-29 — exactly the reuse C#'s `CarouselViewHandler2 : ItemsViewHandler2<CarouselView>` gets; documented collapse from C#'s bare `ItemsView` base). Carousel knobs: Loop (OneTime, default true) / PeekAreaInsets / IsBounceEnabled / IsSwipeEnabled / IsScrollAnimated / CurrentItem (TwoWay) / Position (TwoWay) + the C# Position/CurrentItem choreography (command → event → On*Changed hook, verbatim order) + AnimatePosition/CurrentItemChanges; the ctor installs the horizontal MandatorySingle/Center snap layout (`CreateCarouselHorizontalDefault`); a Position change funnels a CENTERED `scroll_to` through the reused handler (CarouselViewHandler2.MapPosition → ScrollToPosition). Registers `collection_view_handler`. `indicator_view` (`controls/indicator_view.hpp/.cpp`, `<= IndicatorView`) over `view<i_indicator_view>` (Core `i_indicator_view` + `indicator_view_handler` in `maui::core`, like the activity_indicator pair): Count / Position (TwoWay + FromHandler write-back) / MaximumVisible / HideSingle / IndicatorSize / IndicatorColor (LightGrey) / SelectedIndicatorColor (Black) / IndicatorsShape (Circle) + ItemsSource→Count subscription (ResetItemsSource/OnCollectionChanged). `max_visible_indicators` ports `IndicatorViewExtensions.GetMaximumVisible` (min(MaximumVisible,Count) floored at 0, HideSingle collapses a lone dot). Per-backend partials: **ios** the real `MauiPageControl` (UIPageControl) — numberOfPages/currentPage/tints/size + a ValueChanged native-tap write-back; **apple** an NSStackView-of-dots (hand-drawn, the selected dot tinted); **headless** the dot mirror (dot_count/current_page/shape/colors/size). | controls + core (+ platform/headless,apple,ios) | ✅ | ✅ | ✅ | headless / apple / ios | Behavior derived from `CarouselView.cs` + `CarouselViewTests.cs` (constructor defaults, Position/CurrentItem command+event fire-once, AddRemove→Position 0), `IndicatorView.cs` + `IndicatorViewLayoutTests.cs` + `IndicatorViewExtensions.cs` + `MauiPageControl.cs` (numberOfPages/GetCurrentPage clamp) + `IndicatorViewHandler.{cs,iOS.cs}`. **Tests:** headless **23** (`carousel_view_tests.cpp` 11 + `indicator_view_tests.cpp` 12), apple **+5** native (`indicator_view_apple_tests.mm`), ios **+10** native (`carousel_view_ios_tests.mm` 4 + `indicator_view_ios_tests.mm` 6). **DEVIATIONS:** (1) **AppKit carousel is NON-LOOPING** — C# ships NO AppKit/macOS CarouselView either; the macOS carousel rides the collection_view_handler's apple placeholder (the cross-platform simulator is the state mirror), Loop is inert there. (2) The carousel reuses the W3-29 collection_view_handler whose iOS compositional path carries NO phantom-cell loop wrap (`LoopCount = ItemCount + 2`); Loop is honored as control state + the centered snap scroll, but the native UICollectionView shows ItemCount cells (the infinite-wrap visual is a documented carry-over of W3-29 scope). (3) CarouselView.VisibleViews (an ObservableCollection<View> the renderer fills) is OMITTED (no port virtualization realizes real child views). (4) IndicatorView is a TemplatedView in C#; the port OMITS the IndicatorTemplate / IndicatorStackLayout path (the default-dot rendering — the only path the native UIPageControl/NSStackView drives — is fully modeled). (5) IndicatorsShape is the `indicator_shape` enum (not an IShape); IsCircleShape collapses to `shape == circle`. clang-tidy 0; ASan/UBSan clean. |
| **W3-32 shell chrome (native shell_handler)** — the NATIVE chrome behind the W2-21 shell model: `shell_handler` (`maui::core`, lives in `maui_controls`) over `shell : view<i_view>`, ported from the cross-platform `ShellHandler.cs` mapper KEYS + the iOS compat `ShellRenderer`/`ShellItemRenderer`/`ShellSectionRenderer`. The handler resolves the model into a `shell_render_tree` (the tab host for the current item = one `shell_section_renderer` per visible section, each with a `vc_stack` of root content + pushed pages = the per-section nav stack; plus the flyout rows from `Shell.GetItems()`). The SEAM: navigation (`go_to` over the shell_navigation_manager) mutates the model + raises `on_property_changed("current_item"/"current_state"/...)`; `view<>` routes that to `update_value` → `map_current_item`/`map_current_state`/`map_flyout_*` → `rebuild` → `realize_tree` — i.e. **route navigation actually reconfigures the native container**. Flyout item templates via the existing `data_template` (W1-09; `set_flyout_item_template`). Self-registers (`MAUI_REGISTER_HANDLER(shell, shell_handler)`). Per-backend partials: **headless** records the resolved render-tree mirror (route→render-tree unit-testable); **iOS** a real container `UISplitViewController` (primary column = pan-presented flyout drawer; secondary = the current item's `UITabBarController`, one `UINavigationController` per section whose `viewControllers` ARE the section vc_stack) — the on-simulator route→VC-stack e2e; **apple** an `NSSplitViewController` (sidebar = flyout list, content = an `NSTabView` of the items). | controls (shell) | ✅ | ✅ | ✅ | headless / apple / ios | Behavior derived from `ShellHandler.cs` + the iOS renderer set + `ShellTests.iOS.cs` (the VC-hierarchy oracle: `ShellRenderer.ViewController` child VCs, `ShellItemRenderer.CurrentRenderer`, `ShellSectionRenderer.ViewControllers`). **e2e (the gate):** `go_to("//two/tabfour/")` reconfigures the real tab host (item two, tabfour selected); `go_to("Details?id=3")` pushes onto the active section's `UINavigationController` (stack→2). Tests: headless **7** (`shell_handler_tests.cpp`), apple **4** (`shell_handler_apple_tests.mm`), ios **6** (`shell_handler_ios_tests.mm`). **Deviations:** AppKit has no pan DRAWER → the flyout is a persistent SIDEBAR (`FlyoutIsPresented` collapses/expands it); AppKit has no `NSNavigationController` → a section tab shows the TOP page only (the full stack is recorded in the mirror), a back-swap not an animated push; the native→virtual flyout-presented sync (user dismiss) is one-way virtual→native (the bundle-less test process can't host a live window — the flyout_page_handler precedent); the per-row flyout-template BindingContext = item flow is deferred (consistent with W2-21 leaving Shell.ItemTemplate off the model). clang-tidy 0; ASan/UBSan clean. |

| **W8-54 label/editor text-layout polish** — closes the documented `// deferred` markers on Label text layout + Editor placeholder. **Label LineHeight:** `LabelHandler.Mapper["line_height"] → map_line_height` folds `LabelExtensions.UpdateLineHeight` into the shared MapFormatting refresh in C# order (LineHeight → Decorations → CharacterSpacing → re-assert HorizontalTextAlignment); the attributed-string helper `with_line_height` (apple_text_ops/ios_text_ops, the `AttributedStringExtensions.WithLineHeight` port — an `NSMutableParagraphStyle.lineHeightMultiple`, `-1` = unset bail-out) sets it on the NSTextField `attributedStringValue` / UILabel `attributedText`; headless mirrors `line_height`. **Label Padding (TextInsets):** `Mapper["padding"] → map_padding` → on **iOS** `MauiLabel.TextInsets` (the `MauiIosLabel` now ports `DrawText`'s RTL-flipped `UIEdgeInsetsInsetRect` + `SizeThatFits`' subtract-before/add-after insets) and on **apple** a `MauiLabelCell` that insets `titleRectForBounds` (left/right flipped live from the control view's `userInterfaceLayoutDirection` at draw time, matching MauiLabel reading `EffectiveUserInterfaceLayoutDirection`); headless mirrors `padding`. **Label PreferredMaxLayoutWidth:** `get_desired_size` ports `LabelHandler.iOS.GetDesiredSize` — an explicit virtual `Width` clamps the width constraint and turns on multi-line wrap (UILabel `numberOfLines = 0` + `preferredMaxLayoutWidth`; NSTextField `cell.wraps` + `preferredMaxLayoutWidth`), else single-line; headless models the wrap (ceil) + padding inflation. **Editor placeholder/insets parity:** `MauiTextView.UpdatePlaceholderLabelFrame` (the placeholder is re-pinned to the text container's `lineFragmentPadding` / `textContainerInset` in the view's layout pass) + `UpdatePlaceholderFont` (placeholder font tracks the editor font) + `UpdateHorizontalTextAlignment` (placeholder alignment follows) ported on **iOS** (`MauiIosEditorTextView.layoutSubviews`) and **apple** (`MauiEditorTextView.layout`, the placeholder NSTextField now multi-line). | controls + core (+ platform/headless,apple,ios) | ✅ | ✅ | ✅ | headless / apple / ios | Behavior derived from `LabelExtensions.cs` (UpdateLineHeight/UpdatePadding), `AttributedStringExtensions.WithLineHeight`, `MauiLabel.cs` (TextInsets DrawText/SizeThatFits + RTL flip), `LabelHandler.iOS.cs` (the PreferredMaxLayoutWidth GetDesiredSize branch) + `LabelHandler.cs` (the shared Mapper's LineHeight/Padding entries), and `MauiTextView.cs` (InitPlaceholderLabel / UpdatePlaceholderLabelFrame / UpdatePlaceholderFont) + `TextViewExtensions.cs`. **Tests:** headless **+4** (`label_tests.cpp`: line_height+padding mirror, padding inflates desired size, explicit-width wrap, unconstrained single line), apple **+4** label (`label_apple_tests.mm`: paragraph-style multiple, padding→cell, padding inflation, explicit-width wrap) **+2** editor (`editor_apple_tests.mm`: placeholder font/alignment follow), ios **+4** label (`label_ios_tests.mm`) **+2** editor (`editor_ios_tests.mm`). Full presets green: headless **2733**, apple **2257**, ios on-simulator (label/editor seams all pass; the only failures in the full ios run are the pre-existing flaky WKWebView/hybrid tests, which pass on retry). clang-tidy clean-build **0**. **Deviations:** the editor's vertical-centering content-inset dance stays mirror-only (carried from W8-53); the editor placeholder is a plain UILabel/NSTextField (no per-span attributed kerning of the placeholder). |

| **W7-50 / X2 shell SearchHandler** — the Shell search surface: the `search_handler` model (a `bindable_object`, ported from `SearchHandler.cs` + `ISearchHandlerController`), the `Shell.SearchHandler` attached property, and the chrome's native search-box wiring. **Model:** `query`/`placeholder`/`search_box_visibility` (`hidden`/`collapsible`/`expanded`, default Expanded)/`shows_results`/`is_search_enabled` (default true)/`clear_placeholder_enabled`/`display_member_name`/`automation_id`/colors/`keyboard`/`query_icon`+`clear_icon`+`clear_placeholder_icon` (`shared_ptr<i_image_source>`)/`item_template` (`data_template`)/`selected_item` (read-only, written at `from_handler` specificity). Behavior: `set_query` → `on_query_changed(old,new)` + the `query_changed` event; `query_confirmed()` → `on_query_confirmed()` (runs the `command`, then the `queried` event — command-then-event); `item_selected(obj)` → `on_item_selected` + record SelectedItem + confirm (non-WinUI); `clear_placeholder_clicked()` → `on_clear_placeholder_clicked()` (runs `clear_placeholder_command`). All four are protected virtuals (a derived handler overrides to filter). ItemsSource (a materialized `vector<shared_ptr<bindable_object>>`) → a `list_proxy` (count/index_of/contains/indexer + `collection_changed`) exposed via `results()`; `list_proxy_changed(old,new)` fires on reassign. **Attached property:** `shell::set_search_handler`/`get_search_handler`/`get_search_handler_shared`/`remove_search_handler` over a process-wide side map keyed by the target `bindable_object*` (the routing element-route precedent) with a captured weak-liveness token so a dead/recycled target self-prunes; setting flows the target's binding context into the handler (C# `OnSearchHandlerPropertyChanged` → `SetInheritedBindingContext`). **Chrome:** `shell_handler` gains a `shell_search_box` mirror in the render tree; `rebuild_search_box` resolves `GetSearchHandler(current_page)` (falling back to the shell), fills the mirror (present/collapsible/enabled/shows_results/query/placeholder/result_count), subscribes to the handler's `property_changed` to re-realize, and holds an owning ref so the publisher outlives the subscription. Per backend: **headless** records the mirror; **iOS** installs a real `UISearchController` into the current section's nav item (`searchController` + `hidesSearchBarWhenScrolling` for Collapsible; removed for Hidden; bookmark button = ClearPlaceholderEnabled), reinstalled by `realize_tree` since a tab-host rebuild replaces the host nav item; **apple** adds a real `NSSearchField` above the content (AppKit deviation: no `UISearchController`). Native edits → `send_query_changed`; search action → `query_confirmed`; iOS bookmark → `clear_placeholder_clicked`. | controls (shell) | ✅ | ✅ | ✅ | headless / apple / ios | Behavior derived from `SearchHandler.cs` + `ISearchHandlerController.cs` + `SearchBoxVisibility.cs` + `ListProxy.cs` + the iOS compat `ShellPageRendererTracker` (the `UISearchController` install oracle). Tests: headless **18** (`search_handler_tests.cpp` — model 8, attached 3, chrome 7), apple **5** (`search_handler_apple_tests.mm`), ios **5** (`search_handler_ios_tests.mm`). **Deviations (documented in the headers):** the C# ICommand `Command`/`ClearPlaceholderCommand` collapse to `move_only_function<void()>` action channels (no ICommand subsystem — the search_bar/menu_item precedent), so the CanExecute→IsSearchEnabled/ClearPlaceholderEnabled auto-coupling is NOT ported (those stay plain bindable props); `command_parameter`/`clear_placeholder_command_parameter` kept as bindable `std::any` for surface fidelity (not forwarded). Font/text-styling mixins (FontElement/TextElement/TextAlignmentElement/IPlaceholderElement) + IsFocused/Focus/Unfocus + soft-input requests + automation-properties-on-icon + the ListProxy lazy-windowing over arbitrary IEnumerable are deferred (pure pass-through styling / focus needing a live first-responder / a virtualization optimization). C# has no `SelectedItemTemplate` (the unit brief's mention) — only `ItemTemplate`, which is ported. clang-tidy 0; ASan/UBSan clean. |

| **W7-51 shell appearance / styling** — the resolved Shell chrome appearance: the `shell_appearance` value type (ported from `ShellAppearance.cs`), the `Shell.*` color ATTACHED properties, the `GetAppearanceForPivot` resolution walk, and the native appearance trackers the chrome installs. **Model:** `shell_appearance` (a `final` `i_shell_appearance_element`) carries the ten chrome colors (`background`/`disabled`/`foreground`/`title`/`unselected` + the five `tab_bar_*` counterparts) + `flyout_width`/`flyout_height`, each `std::optional` (nullopt = unset, the C# nullable-Color / -1-double sentinel — the navigation_page bar-styling "set vs unset" precedent); `ingest(element)` fills only still-unset slots from the pivot's attached values (the C# `_colorArray[i]==null && IsSet` guard, lowest-pivot-wins); the five `effective_tab_bar_*` colors apply the `TabBar* ?? base` fallback (`IShellAppearanceElement`); `operator==` compares all colors + both doubles. **Attached properties:** `shell::set_/get_<color>` + `set_/get_flyout_width|height` over a process-wide side map keyed by the target `element*` (the routing/SearchHandler side-map precedent) with a captured weak-liveness token so a dead/recycled element self-prunes (a recycled slot starts a clean bag, never inherits the predecessor's values). **Resolution walk:** `shell::get_appearance_for_pivot(element&)` (static — reads nothing off the shell) ports `WalkToPage` (Shell→CurrentItem→CurrentItem→PresentedPage) then the up-walk via `logical_parent()`, ingesting each level + (until a ShellContent is seen) the current section's root content (the C# "root content honored under a pushed page" deviation); returns nullopt when nothing set. **Chrome:** `shell_handler` gains a `applied_appearance` mirror in the render tree; `rebuild_appearance` resolves the CURRENT page's appearance (recomputed on every navigation rebuild — the `UpdateToolbarAppearanceFeatures` + `IAppearanceObserver` re-fire) and calls `realize_appearance` per backend. **headless** records the mirror; **iOS** pushes onto each section's `UINavigationBar` (`UINavigationBarAppearance` background=BackgroundColor, titleTextAttributes=TitleColor, tintColor=ForegroundColor) + the `UITabBar` (`UITabBarAppearance` from the Effective tab-bar colors, tintColor/unselectedItemTintColor) — re-applied at the end of `realize_tree` since a tab-host rebuild makes fresh bars; **apple** tints the content `NSTabView` layer (BackgroundColor) + the sidebar `NSTableView` (EffectiveTabBarBackgroundColor). | controls (shell) | ✅ | ✅ | ✅ | headless / apple / ios | Behavior derived from `ShellAppearance.cs` + `IShellAppearanceElement.cs` + the `Shell.*` attached-property set + `Shell.GetAppearanceForPivot`/`Ingest`/`WalkToPage`/`UpdateToolbarAppearanceFeatures` + `ShellAppearanceTests.cs` (`ColorSetCorrectly`) + the iOS compat `ShellNavBarAppearanceTracker`/`ShellTabBarAppearanceTracker` (the bar-tint oracle). Tests: headless **14** (`shell_appearance_tests.cpp` 11 — ingest precedence / effective fallback / equality / the walk; `shell_handler_tests.cpp` +3 — connect resolves, empty-when-unset, navigation re-tints), apple **1** (`shell_handler_apple_tests.mm` — NSTabView layer + sidebar tint), ios **1** (`shell_handler_ios_tests.mm` — nav-bar + tab-bar `standardAppearance.backgroundColor`). **Deviations (documented in the headers):** `Shell.FlyoutBackdrop` (a `Microsoft.Maui.Controls.Brush`) is OMITTED from the value type + ingest — the controls `Brush` type is not ported (the single deferred ingest slot; every Color + the two doubles are ingested with full fidelity); AppKit has no `UINavigationBar`/`UITabBar` so the apple tracker tints only the available NSTabView/sidebar surfaces (no nav-bar / per-tab title colors — the navigation_page_handler AppKit precedent); `MakeComplete` is a no-op (the C# body only re-null-assigns already-null slots). clang-tidy 0; ASan/UBSan clean. |

| **X1 brush family + paint bridge + XAML converter** — the controls-level `Brush` family (developer-facing fill described by an Element) + its bridge to the existing `graphics::paint`. **Types** (`include/maui/controls/brushes/`): `brush` (abstract `: element` — `is_empty`/`is_null_or_empty`/`has_transparency` + the 147 named `Brush.AliceBlue…` statics, generated off the SAME `MAUI_GRAPHICS_NAMED_COLORS` macro as `graphics::colors` so they can't drift); `solid_color_brush` (nullable `std::optional<color>`, value `Equals` per dotnet/maui#27281); `gradient_stop` (`: element`, nullable color + offset, parented to its brush so it inherits BindingContext); `gradient_brush` + `gradient_stop_collection` (observable Add/Insert/Count, parents stops + raises `invalidate_gradient_brush_requested`); `linear_gradient_brush` (StartPoint/EndPoint) + `radial_gradient_brush` (Center/Radius); `image_brush` (a `shared_ptr<i_image_source>`); `immutable_brush` (the named statics — read-only Color, refuses a parent via the new `element::on_logical_parent_changing` seam = C# `OnParentChangingCore`). **Bridge** (`brush_paint_bridge.{hpp,cpp}` — the C# implicit operators): `to_paint`/`to_brush` (solid→solid_paint, linear/radial→gradient_paint, image→image_source_paint) + `paint_is_null_or_empty` (PaintExtensions.IsNullOrEmpty) + `brush_is_null_or_empty_as_paint` (the brush-side IsNullOrEmpty over nullable colors). **Wiring:** `view::set_background_brush` + `shape::set_fill_brush`/`set_stroke_brush` own the brush (BindingContext inheritance via `set_inherited_binding_context`, mirroring `VisualElement.OnBindingContextChanged`) and store the bridged paint in the existing `background_`/`fill_`/`stroke_` (NON-breaking — the paint-typed setters/getters + handlers are untouched). **XAML:** `maui::controls::convert_brush` (`brush_type_converter.{hpp,cpp}`, a Controls-level type per C#) ports `BrushTypeConverter` + `GradientBrushParser` (color name/hex/CSS-color + linear-/radial-gradient), registered (appended) in `register_standard_xaml_converters`. | controls (brushes) | ✅ | ✅ | ✅ | headless / apple / ios | Behavior derived from `src/Controls/src/Core/Brush/Brush.cs` + `BrushTypeConverter.cs` + `{SolidColor,Gradient,LinearGradient,RadialGradient,Image}Brush.cs` + `GradientStop(Collection).cs` + `ImmutableBrush.cs` + `src/Core/src/Graphics/PaintExtensions.cs`; tests ported from `SolidColorBrushTests`/`LinearGradientBrushTests`/`RadialGradientBrushTests`/`BrushTypeConverterUnitTests`. Tests: **brush_tests.cpp** (backend-agnostic; runs on headless/apple/ios). **Deviations (documented in the headers):** C#'s nullable `Color` is a value type in the port, so brush colors are `std::optional<color>` and the bridge DROPS null-color gradient stops (the graphics `gradient_stop` color can't be null) — an all-null gradient becomes zero usable stops; the C# `Paint p = brush; p.IsNullOrEmpty()` pattern is ported as `brush_is_null_or_empty_as_paint` (computed over the brush's nullable colors) so the null-color oracle cases match. The brush surfaces are NAMED setters (`set_background_brush`, not a `set_background` overload) to keep the existing `set_background(nullptr)/(paint)` calls unambiguous. `GradientStopCollection` is a purpose-built observable type (no ObservableCollection); `InvalidateGradientBrushRequested` is the re-render signal (the WeakBrushChangedProxy resubscription is not modeled — re-set the brush to retrigger). The named statics use snake_case (`brush::red()`, PROFILE §9). UNBLOCKS `Shell.FlyoutBackdrop` (W7-51's deferred ingest slot). clang-tidy 0 from a clean build. |

_(extend this table as components are added)_
