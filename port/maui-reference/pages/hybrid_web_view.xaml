<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiReference.Pages.HybridWebViewPage" Title="HybridWebView">
    <!-- The builder twin (hybrid_web_view_page.hpp) additionally wires the button column's Clicked
         handlers and HybridWebView.RawMessageReceived to exercise the JS bridge; AUTHORING.md rule 3
         forbids event attributes here, so this page is static markup only (structural, not behavioral,
         parity — tracked in known_diverging()/EQUIVALENCE_FINDINGS.md cluster E). -->
    <Grid Padding="12" RowSpacing="8" ColumnSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Editor Grid.Row="0" Grid.Column="0" Text="HybridWebView here" IsReadOnly="True" MinimumHeightRequest="200" />

        <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="8">
            <Button Text="Send message to JS" />
            <Button Text="Invoke JS" />
            <Button Text="Invoke Async JS" />
            <Button Text="Test JS Exception" />
            <Button Text="Test JS Async Exception" />
        </VerticalStackLayout>

        <HybridWebView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HybridRoot="HybridSamplePage" />
    </Grid>
</ContentPage>
