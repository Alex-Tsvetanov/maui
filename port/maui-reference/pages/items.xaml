<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiReference.Pages.ItemsPage" Title="Items">
    <VerticalStackLayout Spacing="12">
        <CollectionView Header="Today" SelectionMode="Single">
            <CollectionView.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Water the plants</x:String>
                    <x:String>Review the port</x:String>
                    <x:String>Ship wave 2</x:String>
                </x:Array>
            </CollectionView.ItemsSource>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding .}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Label Text="Pick a task" />
    </VerticalStackLayout>
</ContentPage>
