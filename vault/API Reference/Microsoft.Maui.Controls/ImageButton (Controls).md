---
title: "ImageButton (Controls)"
tags:
  - api
  - kind/class
  - ns/Microsoft-Maui-Controls
aliases:
  - "Microsoft.Maui.Controls.ImageButton"
namespace: "Microsoft.Maui.Controls"
kind: class
platforms:
  - All platforms (.NET)
  - Android
  - iOS
  - Mac Catalyst
  - Windows
  - Tizen
  - .NET Standard
assemblies:
  - Controls
---

# ImageButton (Controls)

> [!abstract] Class in `Microsoft.Maui.Controls`
> Full name: `Microsoft.Maui.Controls.ImageButton`

Represents a button that displays an image and reacts to touch events.

## Platforms

| Platform | Available |
|---|---|
| All platforms (.NET) | ✅ |
| Android | ✅ |
| iOS | ✅ |
| Mac Catalyst | ✅ |
| Windows | ✅ |
| Tizen | ✅ |
| .NET Standard | ✅ |


## Constructors

| Name | Summary |
|---|---|
| [[ImageButton (Controls).ImageButton\|ImageButton]] | Initializes a new instance of the `ImageButton` class. |

## Properties

| Name | Summary |
|---|---|
| [[ImageButton (Controls).Aspect\|Aspect]] |  |
| [[ImageButton (Controls).BorderColor\|BorderColor]] |  |
| [[ImageButton (Controls).BorderWidth\|BorderWidth]] |  |
| [[ImageButton (Controls).Command\|Command]] |  |
| [[ImageButton (Controls).CommandParameter\|CommandParameter]] |  |
| [[ImageButton (Controls).CornerRadius\|CornerRadius]] |  |
| [[ImageButton (Controls).IsEnabledCore\|IsEnabledCore]] | Gets or sets the padding inside the image button. This is a bindable property. |
| [[ImageButton (Controls).IsLoading\|IsLoading]] | Gets or sets the color of the border around the image button. This is a bindable property. |
| [[ImageButton (Controls).IsOpaque\|IsOpaque]] |  |
| [[ImageButton (Controls).IsPressed\|IsPressed]] | Gets a value indicating whether the image button is currently pressed. This is a bindable property. |
| [[ImageButton (Controls).Padding\|Padding]] |  |
| [[ImageButton (Controls).Source\|Source]] |  |

## Methods

| Name | Summary |
|---|---|
| [[ImageButton (Controls).ChangeVisualState\|ChangeVisualState]] |  |
| [[ImageButton (Controls).MapRippleColor\|MapRippleColor]] |  |
| [[ImageButton (Controls).On{T}\|On<T>]] |  |
| [[ImageButton (Controls).OnBindingContextChanged\|OnBindingContextChanged]] | Gets or sets a value indicating whether the image should be rendered as opaque. This is a bindable property. |
| [[ImageButton (Controls).OnMeasure\|OnMeasure]] |  |
| [[ImageButton (Controls).OnPropertyChanged\|OnPropertyChanged]] |  |
| [[ImageButton (Controls).PropagateUpClicked\|PropagateUpClicked]] | For internal use by the .NET MAUI platform. Propagates the clicked event up the visual tree. |
| [[ImageButton (Controls).PropagateUpPressed\|PropagateUpPressed]] | For internal use by the .NET MAUI platform. Propagates the pressed event up the visual tree. |
| [[ImageButton (Controls).PropagateUpReleased\|PropagateUpReleased]] | For internal use by the .NET MAUI platform. Propagates the released event up the visual tree. |
| [[ImageButton (Controls).RaiseImageSourcePropertyChanged\|RaiseImageSourcePropertyChanged]] | For internal use by the .NET MAUI platform. Raises the property changed event for the image source. |
| [[ImageButton (Controls).SendClicked\|SendClicked]] | For internal use by the .NET MAUI platform. Triggers the `Clicked` event. |
| [[ImageButton (Controls).SendPressed\|SendPressed]] | For internal use by the .NET MAUI platform. Triggers the `Pressed` event. |
| [[ImageButton (Controls).SendReleased\|SendReleased]] | For internal use by the .NET MAUI platform. Triggers the `Released` event. |
| [[ImageButton (Controls).SetIsLoading\|SetIsLoading]] | For internal use by the .NET MAUI platform. Sets the `IsLoading` property. |
| [[ImageButton (Controls).SetIsPressed\|SetIsPressed]] | For internal use by the .NET MAUI platform. Sets the `IsPressed` property. |

## Events

| Name | Summary |
|---|---|
| [[ImageButton (Controls).Clicked\|Clicked]] | Occurs when the `ImageButton` is clicked or tapped. |
| [[ImageButton (Controls).Pressed\|Pressed]] | Occurs when the `ImageButton` is pressed. |
| [[ImageButton (Controls).Released\|Released]] | Occurs when the `ImageButton` is released. |

## Fields

| Name | Summary |
|---|---|
| [[ImageButton (Controls).AspectProperty\|AspectProperty]] | Bindable property for `Aspect`. This is a bindable property. |
| [[ImageButton (Controls).BorderColorProperty\|BorderColorProperty]] | Bindable property for `BorderColor`. This is a bindable property. |
| [[ImageButton (Controls).BorderWidthProperty\|BorderWidthProperty]] | Bindable property for `BorderWidth`. This is a bindable property. |
| [[ImageButton (Controls).CommandParameterProperty\|CommandParameterProperty]] | Bindable property for `CommandParameter`. This is a bindable property. |
| [[ImageButton (Controls).CommandProperty\|CommandProperty]] | Bindable property for `Command`. This is a bindable property. |
| [[ImageButton (Controls).CornerRadiusProperty\|CornerRadiusProperty]] | Bindable property for `CornerRadius`. This is a bindable property. |
| [[ImageButton (Controls).IsLoadingProperty\|IsLoadingProperty]] | Bindable property for `IsLoading`. This is a bindable property. |
| [[ImageButton (Controls).IsOpaqueProperty\|IsOpaqueProperty]] | Bindable property for `IsOpaque`. This is a bindable property. |
| [[ImageButton (Controls).IsPressedProperty\|IsPressedProperty]] | Bindable property for `IsPressed`. This is a bindable property. |
| [[ImageButton (Controls).PaddingProperty\|PaddingProperty]] | Bindable property for `Padding`. This is a bindable property. |
| [[ImageButton (Controls).SourceProperty\|SourceProperty]] | Bindable property for `Source`. This is a bindable property. |

## Remarks

`ImageButton` is similar to `Button` but displays an image instead of text. It supports all standard button features including commands, events, borders, and visual states.

## Guide

- 📖 Conceptual: [[imagebutton]]

## See also

- [[_Microsoft.Maui.Controls|Microsoft.Maui.Controls namespace]]
- [Online API docs](https://learn.microsoft.com/dotnet/api/microsoft.maui.controls.imagebutton)
